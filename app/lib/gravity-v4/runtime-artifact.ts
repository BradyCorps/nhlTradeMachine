/**
 * No fitted Gravity v4 artifact is authorized in this repository yet.
 * The offline export task may replace this null only after producing a
 * versioned, validated artifact from an approved event/shift dataset.
 */
export const GRAVITY_V4_RUNTIME_ARTIFACT: unknown = null;
