# Development Notes

- 2026-08-17: **Goalie stat regression in season rollover** (`app/lib/season-rollover.ts`). Goalies in the Cup Run sim previously only aged, retired, and decremented contracts — their GSAx and SV% were frozen across offseasons. Now each rollover regresses both stats toward population means using the backtest stability coefficients (GSAx r=0.13, SV% r=0.30), applies the goalie age curve (peak 28-30, steepest decline 34-36), and adds stochastic noise (±0.15 GSAx, ±0.005 SV%). Baseline GSAx drifts toward the regressed value with 40% weight, matching the skater anchor mechanic. A hot .925 SV% goalie will regress toward .912-.915 the next offseason, and an aging 35-year-old gets an additional 4.5% annual decline. Added `gsax`, `savePct`, `baselineGsax` to `RolloverPlayer` interface. Two new tests: regression toward means and steeper decline after 33. All 1,982 tests pass. Touched `app/lib/season-rollover.ts`, `__tests__/season-rollover.test.ts`.

- 2026-08-17: **Goalie aging curve in player timeline** (`app/lib/player-timeline.ts`). The contract projection timeline was applying the skater decay curve (peak 22-27, 3.5% annual decline at 28-30) to goalie GSAx projections. A 29-year-old goalie in his prime got the same decay as a 29-year-old skater past his peak. Replaced with a backtest-derived goalie curve: development through 23, near-plateau at 24-27, peak at 28-30, 3% annual decline at 31-33, 4.5% at 34-36, 8% at 37+. This matches the NAV engine's own `peakAge = 30` for goalies and the backtest aging data (131 goalies, 614 age-grouped pairs). Also fixed the backtest to source goalie birth years from signings.csv (131 matched) since the bios file only contains skaters. All 1,980 tests and typecheck pass. Touched `app/lib/player-timeline.ts`, `scripts/backtest/goalie-stability-backtest.ts`.

- 2026-08-17: **Goalie stability backtest** (`scripts/backtest/goalie-stability-backtest.ts`). Validates the goalie-percentiles stability coefficients across 769 consecutive-season pairs (1,031 goalie-seasons, 197 unique goalies, 2008–2025, minimum 1,000 minutes ice time). All 8 artifact r values match recomputed values exactly. Regression toward the mean improved prediction for every metric: MD SV% (r=0.06, MAE reduced 26.4%), GSAx/60 (r=0.13, 23.0%), LD SV% (r=0.23, 21.6%), GAA (r=0.34, 18.2%), SV% (r=0.30, 17.4%), HD SV% (r=0.40, 13.1%), freeze rate (r=0.72, 5.2%), rebound control (r=0.69, 3.8%). The noisiest metrics benefit most from regression — MD SV% is nearly random season-to-season. Stability hierarchy confirmed: freeze rate and rebound control are the most persistent goalie skills; GSAx/60 and MD SV% are noise. Hot goalie regression is strong: 78% of goalies with SV% ≥ .915 declined the next year (avg drop −0.925%). Workload stability r ≈ 0.47; 70% of starters stayed starters, 6% lost their job. Aging analysis unavailable — bios file contains skaters only. Run with `npx tsx scripts/backtest/goalie-stability-backtest.ts`.

- 2026-08-17: **Regression Watch + backtest credibility on the fantasy page.** Added a Regression Watch section to `/fantasy` that surfaces productive players (≥40 pts pace) whose model regression probability is ≥13% — the sell-high list. Position-aware narratives explain the driver (hot finishing, age decline, unsustainable pace) with evidence chips. Updated `BREAKOUT_BASE_RATE_PCT` from 10 to 16 to match the backtest finding. Both the Breakout Watch and Regression Watch now carry a credibility line citing the 5,741-player-season backtest: breakout watch at 1.3× the league rate, regression watch at 1.6× — the model's strongest signal. Touched `app/lib/fantasy-board.ts` (added `buildRegressionWatch`, `RegressionWatchEntry`, `REGRESSION_BASE_RATE_PCT`) and `app/fantasy/page.tsx`. All 25 fantasy-board tests and 427 feature canaries pass.

- 2026-08-17: **Breakout model backtest harness** (`scripts/backtest/breakout-backtest.ts`). Backtests the breakout/regression model across 5,741 player-seasons (2008–2023) using historical MoneyPuck data. For each season N, computes breakout signals and runs `computeBreakout()`, then checks whether the player's pts/82 pace actually increased or decreased by ≥12 pts or ≥20% in season N+1. Age sourced from 2025-26 bios file (848 birth dates) supplemented by signings.csv signAge (2,838 total). Results: breakout AUC 0.5805, regression AUC 0.5896, top-quintile breakout lift 1.27× over the 15.7% base rate, top-quintile regression lift 1.58× over 14.3% base. Age-group calibration is strong — prospects predicted at 23.1% vs 23.0% actual. Finishing luck and pedigree drivers show highest actual breakout rates (21.1% and 21.9%). Runs without EDGE data, prospect NHLe, or full draft positions, so measures the model's discrimination floor. Notable hits include Robert Thomas 2020→21 (+57.9 pts pace), Nathan MacKinnon 2016→17 (+55.2), Jack Hughes 2020→21 (+48.3). Run with `npx tsx scripts/backtest/breakout-backtest.ts`.

- 2026-08-07: **PL-4 upgraded the application from unsupported Next.js 14 to Next.js 16.3.0 Active LTS.** Migrated React 19 and its types, App Router request props, `middleware` to `proxy`, ESLint 9 flat configuration, and the compatible OpenGraph image route to Node; preserved the existing hook lint policy for PL-12 and retained the card renderer's required Edge runtime. All 1,961 tests, typecheck, lint with zero errors, the production build with 28 static pages, local Chromium smoke, and the Vercel preview deployment pass; production dependencies audit at zero vulnerabilities. Touched `package.json`, `package-lock.json`, `tsconfig.json`, `.eslintrc.json`, `eslint.config.mjs`, `middleware.ts`, `proxy.ts`, `app/admin/layout.tsx`, `app/admin/login/page.tsx`, `app/api/player-edge/[playerId]/route.ts`, `app/components/Header.tsx`, `app/components/MetricTip.tsx`, `app/error.tsx`, `app/lib/use-dialog.ts`, `app/players/[playerId]/page.tsx`, `app/t/[code]/opengraph-image.tsx`, `app/t/[code]/page.tsx`, `docs/PUBLIC_LAUNCH_TODO.md`, and `docs/DEVNOTES.md`.

- 2026-08-07: **PL-3 made Gravity v3 fail closed on insufficient evidence and removed unsupported cross-position rankings.** Profiles below 20 GP or two-thirds weighted coverage now return `INSUFFICIENT` with no tier or percentile, reliability cannot exceed evidence coverage, X-NAV/simulation ignore insufficient profiles, public/share contracts enforce the null tier/percentile invariant, and leaderboards use qualified within-position percentiles. Recalibrated separate forward/defense tiers from the hash-verified frozen 715-skater population, persisted an aggregate artifact plus deterministic checker, and documented provenance and limitations. 1,961 tests, typecheck, calibration check, and production build pass (two pre-existing hook warnings remain). Touched `app/lib/gravity.ts`, `app/lib/gravity-channels.ts`, `app/lib/card-payload.ts`, `app/api/admin/gravity-calibration/route.ts`, `app/api/card-image/route.tsx`, `app/components/GravityField.tsx`, `app/components/PercentileCard.tsx`, `app/components/TrendingPlayers.tsx`, `app/components/Footer.tsx`, `app/teams/page.tsx`, `app/methodology/page.tsx`, `app/glossary/page.tsx`, `scripts/gravity-calibration/calibrate-tiers.ts`, `data/gravity-calibration/2025-26/tier-calibration.json`, `__tests__/gravity.test.ts`, `__tests__/gravity-feature-gates.test.ts`, `__tests__/gravity-public-contract.test.ts`, `__tests__/gravity-tier-calibration.test.ts`, `docs/analytics/GRAVITY_V3_TIER_CALIBRATION.md`, `docs/analytics/GRAVITY_V3_RELEASE_A_POPULATION.md`, `docs/analytics/GRAVITY_POSITION_CALIBRATION.md`, `docs/GRAVITY_RELEASE_GATES.md`, `docs/PLAYER_GRAVITY_V4_IMPLEMENTATION_SPEC.md`, `docs/PUBLIC_LAUNCH_TODO.md`, `gravity.md`, `package.json`, and `docs/DEVNOTES.md`.

- 2026-08-07: **PL-2 separated Gravity display, X-NAV, and simulation into independent fail-closed release channels.** All three v3 consequences now default off, public live/export surfaces use only the display gate, X-NAV and simulation use distinct value gates, Gravity v4 is source-locked off, public copy states the disabled baseline, and `docs/GRAVITY_RELEASE_GATES.md` defines the held-out evidence required before activation. Added channel-independence and runtime/source coverage; 1,949 tests and the production build pass. Touched `app/lib/gravity-feature-flags.ts`, `app/lib/gravity-channels.ts`, `app/lib/gravity-v4/feature-flag.ts`, `app/lib/xnav-engine.ts`, `app/lib/nav-breakdown.ts`, `app/api/simulate/route.ts`, `app/api/card-image/route.tsx`, `app/players/page.tsx`, `app/players/[playerId]/page.tsx`, `app/teams/page.tsx`, `app/components/AssetCard.tsx`, `app/components/OffseasonPlayerAnalytics.tsx`, `app/components/PercentileCard.tsx`, `app/components/ResignPhase.tsx`, `app/components/TrendingPlayers.tsx`, `app/components/Footer.tsx`, `app/components/MetricTip.tsx`, `app/components/WelcomeModal.tsx`, `app/methodology/page.tsx`, `app/glossary/page.tsx`, `__tests__/gravity-feature-gates.test.ts`, `__tests__/gravity-v4.test.ts`, `__tests__/simulate-and-claude-routes.test.ts`, `__tests__/xnav.test.ts`, `__tests__/feature-canaries.test.ts`, `docs/GRAVITY_RELEASE_GATES.md`, `docs/PUBLIC_LAUNCH_TODO.md`, `docs/DEVNOTES.md`.

- 2026-08-07: **PL-1 made public X-NAV one canonical, reconciled calculation.** Added a typed raw-asset adapter shared by evaluation, player, trending, and team surfaces; moved the historical pedigree floor into the engine as an explicit production-only stage; propagated the live cap ceiling through league payloads with cap-aware caching; and added skater/goalie/prospect/pick route-parity and input-preservation coverage. 1,942 tests and the production build pass. Touched `app/lib/asset-nav.ts`, `app/lib/live-cap-settings.ts`, `app/lib/xnav-engine.ts`, `app/lib/nav-breakdown.ts`, `app/lib/player-data.ts`, `app/lib/team-cache.ts`, `app/api/evaluate/route.ts`, `app/api/league/route.ts`, `app/api/league/teams/route.ts`, `app/players/page.tsx`, `app/players/[playerId]/page.tsx`, `app/teams/page.tsx`, `app/components/PercentileCard.tsx`, `app/components/TrendingPlayers.tsx`, `__tests__/asset-nav.test.ts`, `__tests__/evaluate-route.test.ts`, `__tests__/xnav.test.ts`, `__tests__/feature-canaries.test.ts`, `docs/PUBLIC_LAUNCH_TODO.md`, `docs/DEVNOTES.md`.

- 2026-08-06: **Made Bulk Free Agents atomic, identity-safe, and truthfully reported.** Existing/new player writes now run in one transaction, require one returned row each, roll back together on failure, and clear caches only after commit; names that collapse to one player ID are written once and reported as skipped in the admin result. Added five integration tests. Touched `app/api/admin/fa-bulk/route.ts`, `app/admin/contracts/page.tsx`, `__tests__/fa-bulk-route.test.ts`, `docs/DEVNOTES.md`.

- 2026-08-06: **Made bulk contract-ingestion counts reflect rows actually written.** Insert, update, extension, and best-effort position-backfill reports now derive from database `RETURNING` rows; zero-row conflicts are listed in the Paste Signings result instead of being called successes, with five integration tests covering each write path. Touched `app/api/admin/contracts/route.ts`, `app/admin/contracts/PastePanel.tsx`, `__tests__/contracts-ingestion-counts.test.ts`, `docs/DEVNOTES.md`.

- 2026-08-06: **Made admin contract-term backfills and reconciliations atomic and truthfully reported.** The route now plans and writes inside one transaction, verifies every affected row before counting it, rolls the batch back on any failure, and clears roster caches only after commit; added five in-memory database tests covering both actions, rollback, zero-row writes, and dry runs. Touched `app/api/admin/contract-terms/route.ts`, `__tests__/contract-term-route.test.ts`, `docs/DEVNOTES.md`.

- 2026-08-06: **Reduced the production dependency audit from 38 advisories (including one critical) to the two high-severity findings blocked on a separate Next 14→16 migration.** Moved build/test tooling to `devDependencies`, removed the unused vulnerable Vercel CLI, upgraded direct PostCSS and Node types, and applied compatible transitive security fixes. Touched `package.json`, `package-lock.json`, `docs/DEVNOTES.md`.

- 2026-07-30: **Special-teams sheets are editable, and the lineup shows alternate positions.** Reported from play, and the first one was a real defect with a narrow cause. **The PP/PK sheets could not be changed** because the bench was gated `situation === "EV"`. The unit cells themselves were clickable — RL6 gave them a click-to-swap — but a swap needs two slots, so the only players reachable on a unit sheet were the ten `hydrateSpecialTeams` happened to place. Anyone it left off could never be put on a unit at all, which is not "hard to find", it is impossible. The bench renders in PP and PK now, holding every skater not already on that sheet, ranked by lineup contribution; picking one and tapping a slot places him. That is a different action from a swap and needed its own state — a bench player has no slot index to exchange with — so `placeFromBench` vacates his old slot if he is already on the sheet, because a unit cannot dress the same man twice. Goalies are excluded: a goaltender does not take a power-play shift. **Alternate positions were being computed and then hidden.** `displayPosition(position, secondaryPosition)` was already used in the cell's tooltip and on the even-strength bench chips, but the visible badge on the card printed the bare primary position — so a C/W read as "C" everywhere you would actually look. Four badge sites now show the alternate. **On captains, the reported asymmetry is not a bug.** `app/data/leadership.ts` is a deliberately partial curated table — 22 captains and 18 alternates league-wide — and `team-leadership.ts` says in its own comment that a club with no curated captain gets `null` rather than a promoted alternate, because inventing one would put a letter on a real player who does not wear it. Winnipeg is in the table (Lowry C, Scheifele and Morrissey A); Anaheim is not. So the app is behaving correctly and the data is simply incomplete. **I could not check whether the NHL API carries captaincy**: this container's network policy blocks `api-web.nhle.com`. What the codebase shows is that the app already fetches `/v1/roster/{team}/current` and reads no captain field from it, which matches my understanding of that payload — but I could not verify it, so the user has the one-line curl to settle it. Filling all 32 clubs from my own knowledge is the thing I specifically should not do: captaincies move every offseason and my knowledge ends before this season starts, so it would produce plausible, wrong letters — exactly what the module warns against. The override UI comes first; seeding follows it, from a source rather than from memory. 5 canaries, one older one repointed (it pinned `p?.position ?? "--"`, the expression the alternate-position fix replaces, rather than the guarantee that a position is shown). Production build verified. Touched `app/components/LineupEditor.tsx`, `__tests__/feature-canaries.test.ts`.

- 2026-07-30: **Brand kit V3 installed — the user-drawn Blue Paint mark replaces the generated V2 one.** Kit verified intact before anything moved: **91/91 checksums pass**. Its own `validate-kit.mjs` needs Inkscape, which this container does not have, so rendering was checked another way. **The finding that shaped the implementation:** `mark-primary.svg` is 221 KB of which **218 KB is a base64-embedded 1024×1024 paper-grain JPEG** — it carries four vector paths. The header lockup the handoff specifies is 246 KB, same cause. For scale the app's entire shared JS bundle is **87 KB**, so the textured lockup alone would be nearly three times the critical-path JavaScript, on every page. And the app renders its marks at **44px**, which downsamples that texture 23:1 — the grain is not perceptible at the size it would be paid for. The kit's own rule ("textured at 40px and larger when the grain is visible") lands right on 44px without settling it, so I put the tradeoff to the user rather than deciding it. Chosen: **clean vector in the UI, textured artwork for large display and export.** Nothing about the drawing changes; only whether an invisible grain overlay rides on the critical path. The textured files are still published — this is routing, not deletion, and a test asserts they remain. **Two files the app needs that V3 does not ship**, both DERIVED rather than drawn, by `scripts/derive-brand-assets.mjs` so a kit revision flows through by re-running it: a cream wordmark for the dark hero desk (ink→cream, red ampersand held), and an untextured header lockup (one masked `<image>` removed, **246 KB → 27 KB**). The script fails loudly if a substitution matches nothing, if any base64 survives, or if a single drawn element is lost — it strips a texture, never artwork. **V3 inverted the small-cut rule** and this was the one thing that could have shipped wrong silently: V2's small cut dropped the PUCK and kept the brackets; V3 keeps the puck and drops the BRACKETS, because at 32px the brackets thicken into the ink and the mark reads as a block. The old guard asserted the V2 rule, so left alone it would have passed while the app rendered the wrong mark. Geometry guards now compare against `mark-primary-clean.svg` (the cut actually rendered) and cover polygons as well as paths, since V3 draws the brackets as polygons and a `d`-only check would have missed half the symbol. Brand tokens are byte-identical between V2 and V3, and the metadata snippet is unchanged, so neither needed touching. **One older canary repointed**: it pinned the exact lockup filename, which is a delivery choice rather than the rule the kit states ("do not type the ampersand"); it now matches either cut. And the new critical-path guard tripped over the familiar trap — the component's own comment explains why it avoids the base64 texture — which the `stripComments` helper built two commits ago handled directly. Old `docs/cap-and-crease-brand-kit/` removed. 8 tests. 1530 passing, production build verified. Touched `app/components/BrandMark.tsx`, `app/components/Header.tsx`, `app/layout.tsx`, `public/brand/**`, `scripts/derive-brand-assets.mjs` (new), `__tests__/brand-kit.test.ts`, `__tests__/feature-canaries.test.ts`, removed `docs/cap-and-crease-brand-kit/`.

- 2026-07-29: **OFF7 — AI clubs now have to live under the cap too.** Every club resolves its own free agency and some come out of it over the ceiling. Until now nothing happened next: an AI club could sit $6M over the cap all season and the simulation ran anyway, so **the one constraint the whole app is about applied to the user and to nobody else.** The hard half is not making trades happen, it is making them ones a front office would actually make — the failure mode to avoid is a league that quietly launders its best players into whoever had room. Six rules, each earning its place by a specific bad trade it prevents: **only a club actually over the ceiling trades** (cap relief is the motive; without one this is just shuffling the league); **the buyer must want the player**, scored by `blockFitsTeam` — the same function the Partner Finder uses; **a club does not dump its core**, so a cap crunch costs you a middle piece rather than your first-line centre; **the smallest move that solves it wins**, because shedding $9M to fix a $2M overage is not cap management; **a body comes back**, which is structural rather than cosmetic — without a return the seller's roster shrinks every offseason and a three-year Cup Run runs clubs out of players; and **neither club may end up outside the payroll range**, checked with `findCapBreaches`, the same rule CXH6 applies to the user's trades. Two more limits after a dry run against a league-shaped population: one player out per club per offseason, and at most two in — a rebuilder with $40M of room would otherwise become the league's waste-disposal site in a single July. **The threshold was measured, not chosen.** `blockFitsTeam` returns ~38 for a club with the room, the right phase and the right age profile, 8 when the age is wrong for the phase, and 3 when there is no room; 30 admits the first and rejects the other two. Picking a round number without looking would have either frozen the feature or opened it to everything. **Deterministic by construction** — clubs, candidates and partners all in a total order, no seeded RNG, because which trades happened should be a fact about the league rather than a roll. A test asserts the result is unchanged when the input arrays are reversed. **Two of my own tests were wrong in instructive ways.** One computed the "protected core" by `ptsPace` while the rule uses `lineupContributionScore` — asserting against a proxy for the rule instead of the rule. The other assumed a narrow payroll range left no legal move when in fact a small one existed; the code was right and the test was describing a scenario it had not actually constructed. **Honest limitation:** in a uniform synthetic league every buyer scores exactly at the threshold, so the "who wants him most" ranking falls through to the id tiebreak. Real rosters vary — positional thinness and declared needs push scores to ~58 — and there is a test proving a club with a genuine hole outbids one with mere room, but the discrimination is only as good as the roster variety it is given. 24 tests + 8 canaries. Production build verified. **The audit document is now closed — 0 items remain.** Touched `app/lib/ai-trades.ts` (new), `app/lib/free-agency.ts`, `app/armchair-gm/useOffseasonFlow.ts`, `__tests__/ai-trades.test.ts` (new), `__tests__/feature-canaries.test.ts`, `ARMCHAIR_AUDIT.md`.

- 2026-07-29: **CX7c — a simulation now reports the season it actually played.** A Cup Run plays three consecutive seasons, and every response stamped itself with the four static `SEASON` constants. So Year 3 came back labelled the same season as Year 1, and the recap prompt then instructed Claude to write *"an end-of-season recap of the PROJECTED 2026-27 season"* over results from 2028-29 — **while the same prompt's Cup Run preamble listed the prior years under their correct labels.** The model was handed two incompatible accounts of when it was and asked to write one story out of them. Four things move with the run year and all are derivable from it: the season, how it is described, the completed season the stats baseline comes from (for Year 2 that is Year 1 of the run, not 2025-26), and which offseason executed trades belong to. `app/lib/sim-season.ts` derives all four, and **Year 1 reproduces the configured constants exactly** — asserted in a test rather than assumed, which is what makes the change safe for ordinary single-season play, since that is Year 1. The run year already existed client-side; it was folded into the seed and never sent as data. It now travels on the request. **A second contradiction fell out while reading the prompt**, and it sat in the section headed LOCKED FACTS: past Year 1 the most recent COMPLETED season is the previous run year, whose champion the preamble had just listed — but the prompt asserted the real-world 2025-26 season and Carolina underneath it. Those lines are derived from the run's prior season when there is one, and the two real-world-only facts (the Conn Smythe winner, the Florida disclaimer) are omitted rather than restated for a season that is no longer the latest. **What I deliberately did not touch:** the seed. `scenarioSeed` already folds the run year in separately, and the route's fallback seed uses `SEASON.simulationMode` — changing that would silently reroll every simulation that omits a seed, which is a different item than what a response calls itself. 12 tests + 5 canaries. Production build verified. **1 audit item remains (OFF7).** Touched `app/lib/sim-season.ts` (new), `app/api/simulate/route.ts`, `app/api/claude/route.ts`, `app/armchair-gm/useSimDispatch.ts`, `app/lib/sim-request-schema.ts`, `__tests__/sim-season.test.ts` (new), `__tests__/feature-canaries.test.ts`, `ARMCHAIR_AUDIT.md`.

- 2026-07-29: **CXS6 — filed reports can be reopened, and only real trades can be filed.** Two defects. **(1) The archive was a dead end.** A saved scenario offered rename and delete and nothing else, so looking at a trade again meant rebuilding it by hand — which makes the feature a scrapbook rather than a tool. **(2) A selected club counted as a trade.** The gate was `teams[0] || teams[1] || blocks[0].length || blocks[1].length`, and Armchair GM selects a home club at startup, so **Save was live from the moment the page loaded** and would file a report reading "nothing" against "nothing". The card even had a rendering branch for it, which is the tell that empty saves were happening and had been accommodated rather than prevented. Saving now requires an asset on a block; sharing deliberately stays looser, because the URL carries the partner selection and is worth copying before any asset is down. **The reason loading is not trivial is worth recording.** A `SavedScenario` is a LOSSY projection — id, name, position, cap hit, age, retention, and none of the paces, baselines or EDGE signals the valuation engine reads. Rebuilding an `Asset` from those stored fields would produce a player who prices at **zero**, which is exactly the failure CXH3 hit when walked free agents were dropped from `db.players`. So a restore is a **lookup, not a reconstruction**: resolve each saved id against the live pool and take the real asset. The one thing the scenario keeps authority over is `retainedPct`, because retention is a term of the deal rather than a property of the player — and it is applied as a copy, so restoring cannot scribble it onto the pooled asset. **A scenario also outlives the league it was filed against**, sitting in localStorage across trades, rollovers and resets; players retire, move, or vanish when the season rolls. Dropping them silently would load a DIFFERENT trade than the one saved and say nothing, so anything unresolvable comes back named and the bar reports it. The name fallback matches through `canonicalNameSlug`, so the Björck/Bjorck case that has bitten the draft reconcile does not read as a different player, and the id path wins when both are present. 14 tests + 4 canaries — the first written against the new `readSource()` helper. Production build verified. 2 audit items remain. Touched `app/lib/scenario-restore.ts` (new), `app/components/TradeHistoryBar.tsx`, `app/armchair-gm/page.tsx`, `__tests__/scenario-restore.test.ts` (new), `__tests__/feature-canaries.test.ts`, `ARMCHAIR_AUDIT.md`.

- 2026-07-29: **Canary tooling — a fix and its own documentation can now live in the same file.** Canaries assert on source TEXT, so the usual shape is "this string must no longer appear". But the comment explaining *why* a string was removed quotes that string, which means the canary sees it and fails a fix that is actually correct. Twelve canaries had each grown a private `.replace(/\/\/.*$/gm, "")` to cope — a rule every future author has to remember, where the one who forgets ships a canary that breaks the moment somebody documents a fix. One `readSource()` now does it centrally, and the twelve ad-hoc strips are gone. **The stripper is string-aware, and that is not fussiness**: the naive line-comment regex those canaries used also eats the rest of any line containing a URL, so `"https://capandcrease.com"` silently became `"https:` — an assertion that would fail for a reason nobody would guess. **Writing its tests first caught a real flaw in my own implementation**: removing a block comment collapsed `foo/* c */bar` to `foobar`, inventing a substring the source never contained, which could make a not-contains canary fail or — worse, because it is silent — a contains-canary pass. It emits a separator now, and keeps interior newlines so line numbers stay put. Two verifications beyond the unit tests. **A negative control**, because a helper that quietly neuters every canary would also make the suite green: putting the forbidden phrase back as a comment leaves the canary passing, putting it back in rendered copy makes it fail. And the helper lives in `__tests__/support/source.ts` rather than being exported from the canary suite — importing it out of a `*.test.ts` re-ran all 348 canaries inside the importing file, which showed up as the suite jumping from 1444 tests to 1804. 11 tests. Production build verified. Touched `__tests__/support/source.ts` (new), `__tests__/strip-comments.test.ts` (new), `__tests__/feature-canaries.test.ts`.

- 2026-07-29: **CXS2 — the reported symptom does not happen, and the real defect was a promise nothing held to.** The item reads "Draft Night says picks stay tradeable, but completion removes all current-year picks." I checked it against the code before changing anything, and the opposite is true: **completion removes zero picks.** `draftYearForCupYear(1)` is `SEASON.draftYear` = 2026, and `buildDraftPickInventory` starts at `SEASON.firstTradablePickYear` = 2027, so `dropSpentDraftPicks(players, 2026)` matches nothing in the pool. Verified by running it — 15 picks in, 15 out. The reason is sound rather than accidental: the 2026 draft **was held in June 2026, before the scenario begins**, which is exactly why its picks are not assets. Draft Night is a replay of that class, and the first-rounder it hands you is the pick your club already spent in June, not a free asset. So the behaviour is right and the item's diagnosis is wrong. **What was actually wrong is the sentence.** "First round complete — your picks stay tradeable assets" is a blanket promise about "your picks", and it was true only by accident of which years the inventory happens to carry. Nothing tied the claim to the behaviour, so a change to either would have turned it into a lie with no test noticing — and the completion comment claimed to "spend this year's picks", which for Year 1 it cannot do. The copy now names what is and is not affected, deriving the year from config so it cannot drift: this draft's picks were spent when it was held, and your 2027-and-later picks are untouched. The masthead year comes from `SEASON.draftYear` too, rather than three hard-coded "2026"s that would survive a season rollover. **The `dropSpentDraftPicks` call stays**, and a test pins why: it is a no-op for Year 1 by design, it is not decoration — a pool that ever did carry a stale current-year pick must have it dropped rather than left tradeable — and it genuinely spends picks in Cup Run Years 2 and 3, where the draft is real. **One canary caught me out in the familiar way**: the comment I wrote explaining the fix quotes the forbidden copy verbatim, so the canary strips comments before asserting. 4 tests + 4 canaries. Production build verified. 3 audit items remain. Touched `app/components/DraftNight.tsx`, `app/armchair-gm/page.tsx`, `__tests__/draft-picks.test.ts`, `__tests__/feature-canaries.test.ts`, `ARMCHAIR_AUDIT.md`.

- 2026-07-29: **CXH6 — both ends of the payroll range are now checked the same way, for both clubs.** The audit tested the two limits three different ways and only got the ceiling right. **(1) The floor was checked for the home club only.** The ceiling had a flag for each side; the floor had one, `vetoesSide: 0`. So a partner that took on nothing and shipped salary out could be dropped under the floor and the audit said nothing — the floor is a league rule, and it applies to whoever the trade moves, not to whoever happens to be sitting in the user's chair. **(2) The floor used a hard-coded `SEASON.capFloor` while the ceiling used the live value.** This is worse than stale: there is a `cap_floor` override in the admin settings, `/api/admin/settings` reads and validates it, and the trade audit silently ignored it — so raising the ceiling for a what-if scenario left the floor behind and the two ends of the range described different leagues. `getLiveCapFloor` now mirrors `getLiveCapCeiling`. **(3) The floor only fired when a club shed more than $3M.** A club sitting just above the floor that sheds $2M is below the floor, and was cleared. That threshold was standing in for a question it never asked — *did this trade cause it?* — which is now asked directly: only a trade that REDUCES payroll can breach the floor, which is what keeps a club already under it (LTIR, a thin roster, a data gap) from being flagged on every trade it makes, **including the ones adding salary to climb back out**. `app/lib/cap-limits.ts` holds the rule: compute each club's payroll after the trade and compare it to both ends of the same range. It also distinguishes **causing** a breach from **deepening** one, so the sentence shown matches what happened rather than asserting the trade did something it did not. **A modelling invariant surfaced while writing the tests**, and it is worth recording: `capSpace` is measured *against* a ceiling — `buildTeamCapSpaceMap` computes it as `ceiling - used` — so payroll is the invariant and cap space is not. My first attempt at "honours a raised ceiling" held cap space fixed while moving the ceiling, which silently describes a different club. Deriving payroll back out is exact only because the app builds cap space from the same live ceiling the audit uses. 14 tests + 4 canaries. Production build verified. 4 audit items remain. Touched `app/lib/cap-limits.ts` (new), `app/api/evaluate/route.ts`, `__tests__/cap-limits.test.ts` (new), `__tests__/feature-canaries.test.ts`, `ARMCHAIR_AUDIT.md`.

- 2026-07-29: **CXH5 — Partner Finder folders now mean one thing each, and the scan can be acted on.** The tier ladder tested a cap condition in the middle of a score ladder: `OVER → BLOCKED`, `score ≥ 60 → LEAD`, **`capFit === "FITS" → CAP_CLEAR`**, `score ≥ 35 → LONG_SHOT`, else `BLOCKED`. Two wrong things follow from that third line. **A club scoring ZERO was filed as CAP_CLEAR purely for having cap room**, beside a club scoring 59 — "they can absorb the money" and "they might actually want this" collapsed into one folder, when cap space is not interest and a rebuilding club has plenty of the former and none of the latter. And **LONG_SHOT was unreachable for any club whose cap fits**, because FITS was caught a line above; the only route down there was a TIGHT cap with a middling score, so the folder labelled "long shot" actually meant "tight cap" — the tier reporting a different axis than its name. The two axes are independent and both already ride on every result, so they are separated: `capFit` answers whether the money works, `fitTier` answers how interested the club is. **BLOCKED is reserved for the cap making it impossible** and nothing else; above that it is a monotone ladder on score alone. `CAP_CLEAR` is renamed **POSSIBLE**, because a folder should not be named after a condition it is not measuring, and the panel now prints one line saying what the open folder claims — a club under "Long Shot" has little reason to engage, which is not a statement about its cap. Two properties are tested rather than spot-checked: the ladder is **monotone in score** (more interest can never file a club in a worse folder) and **never returns BLOCKED on score alone** at any score from 0 to 100. **The scan was also read-only** — it named a partner and then made you go find them in the team picker. Each row has an Open Trade button now; it sets the partner and **keeps the shopped package on the block**, since that package is the entire subject of the scan, and clears only the return, which belonged to whichever club was selected before. **One existing test needed repointing** and it is the interesting one: `match-route.test.ts` asserted a TIGHT-cap club landed in LONG_SHOT — true under the old ladder, but only because LONG_SHOT was where tight-cap clubs went. The test was pinning the exact conflation this item removes. It now asserts the club's score sits in the POSSIBLE band and that the cap constraint is reported by `capFit` and the retention warning, which it already checked. 17 tests + 5 canaries. Production build verified. 5 audit items remain. Touched `app/lib/match-fit.ts` (new), `app/api/match/route.ts`, `app/armchair-gm/MatchResultsPanel.tsx`, `app/armchair-gm/page.tsx`, `__tests__/match-fit.test.ts` (new), `__tests__/match-route.test.ts`, `__tests__/feature-canaries.test.ts`, `ARMCHAIR_AUDIT.md`.

- 2026-07-29: **CXH4 — proposal generation stops depending on the network.** Three defects, one of which made the feature quietly non-reproducible. **(1) The surviving package was chosen by whichever audit came back first.** `fitScore` is computed once per club and copied onto each of that club's packages, so every package from one team carries an identical score — and the de-duplication kept the incumbent unless a challenger scored *strictly* higher. Among a club's own packages that is never true, so the winner was simply the first of six concurrent audits to resolve. Same block, same league, same everything: a different proposal each run. Fixed with a **total order** — fit, then package index, then standing, then name, then id — so any two distinct candidates compare unequal and nothing is left for arrival order to settle. A test asserts totality across a matrix of candidates rather than spot-checking a pair. **(2) It paid for packages it would never show.** Four packages per club were audited but only one per club is ever displayed, and the audit list was sorted by fit alone — so a handful of high-fit clubs could spend the entire 36-audit budget on their own alternatives while clubs further down were never contacted. Auditing is **breadth-first** now: every club's preferred package before any club's second. A slightly worse fit at a club you have not contacted is worth more than a fifth variation on one you have. On top of that, the run stops once eight distinct clubs have a viable offer — but **only at a settled wave boundary**, never mid-flight, because checking as results land would hand the cutoff straight back to the network and undo (1). **(3) A broken audit was indistinguishable from an honest answer.** Every exception was swallowed and returned as "not viable", so an unreachable API produced the confident message *"No realistic trade partners found"* — the app asserting a fact about the league when it had learned nothing. Failures are now counted separately from rejections, an abort is not counted as a failure at all, and `summariseAudit` returns a discriminated outcome: even **one** unanswered check downgrades the result from NONE_VIABLE to INCOMPLETE, and the panel says the audit could not be completed and that this is a connection problem rather than a verdict. The scheduling rules live in `app/lib/proposal-plan.ts`, pure and testable without a network; the component supplies candidates and performs fetches. One older canary asserted `AUDIT_CONCURRENCY`/`MAX_AUDIT_CANDIDATES` by name — both moved into the new module, so it now pins that the audit stays bounded and batched rather than where the constants live. 23 tests + 6 canaries. Production build verified. 6 audit items remain. Touched `app/lib/proposal-plan.ts` (new), `app/components/TradeProposal.tsx`, `__tests__/proposal-plan.test.ts` (new), `__tests__/feature-canaries.test.ts`, `ARMCHAIR_AUDIT.md`.

- 2026-07-29: **Press Box — the guess history was in the wrong phase, so it was invisible when it mattered.** Reported from the live site with a screenshot: Attempt 2 of 5, and nowhere on the page were the four cards picked on Attempt 1. The feature shipped in the previous commit was real, but it lived **inside the `phase === "SCORED"` panel** — so it vanished the instant the player pressed Try Again and went back to the table. It was on screen only while they were reading the result they had just been given, and gone during the one activity it exists to support. Lifted into an `AttemptHistory` component and rendered in both phases, sitting directly above the card table during drafting so the player reads what they have tried and then looks at the cards. The scored panel keeps it at `minAttempts={2}`, where a single row would only restate the breakdown above it. **Two pieces of stale copy fell out of the same screenshot.** The drafting instruction said *"Use the scoring breakdown to improve your hand"* — pointing at a breakdown that is not rendered in the drafting phase, which is the same defect stated in prose; it now points at the history that is actually there. And *"Draft 4 players into your lineup. Waive the other 2"* was left over from the six-card deal and should have been caught when the deal grew to eight; it reads from `CARDS_DEALT` now rather than a literal, so it cannot drift again. 3 canaries, one of which pins that `<AttemptHistory>` appears in both phases rather than merely existing. Production build verified. Touched `app/press-box/page.tsx`, `__tests__/feature-canaries.test.ts`.

- 2026-07-29: **Press Box — the rules now describe the game that is actually being scored, plus the answer on failure and a visible guess history.** Reported from play: the How-to-Score text did not match the breakdown beside it. It did not, and the reason was structural rather than a typo. **`scoreHand` builds a FIVE-card hand** — `[...picks, callUp]` — and Teammates, Draft Class, Pipeline, Country Club and Position Group all count all five. Only Division Flush counts the four picks alone. Nothing in the rules said this, which is why a breakdown could read "2x OTT = 2" when only **one** of the player's picks was a Senator: the second Senator was Shane Pinto, the call-up. Confirmed against the engine rather than guessed. **Two more text errors found the same way.** Pipeline claimed "1 pt per card in a run of 3+ consecutive draft years" while the engine pays per distinct YEAR — four cards spanning 2019/2019/2020/2021 score 3, not 4. And Country Club and Position Group are flat: three, four or five sharing the trait all score 3, which is literally consistent with "3 pts for 3+" but reads as broken next to a breakdown printing "4x Forwards +3". Every rule now states how many cards it counts, and the flat ones say they are flat. **The Teammates/Call-Up Bonus overlap is real and kept.** One pick sharing the call-up's club scores 2 as a Teammates pair AND 1 as Call-Up Bonus — the same relationship, three points, two rows. That is not a bug: cribbage does exactly this, where the cut card participates in every combination and nobs is a separate point on top. It was simply never explained, so the rule now says "on top of the Teammates pair it already made". **The text was also duplicated verbatim in two places**, which is how it drifted from the engine to begin with; it is one `HowToScore` component now, and a canary fails if a second copy appears. **The answer is revealed when the attempts run out** — a daily puzzle that never tells you what you missed teaches nothing, so five attempts ended and the player left no better at it. Shows the four cards and the breakdown that earned the optimum, noting when more than one perfect hand existed. **Every guess stays on screen**, with the names picked and how many belonged. This was the one the user was unsure about; it is not a new hint — the X/4 overlap was already shown for the latest attempt — it removes the memory load of recalling five sets of four, which is bookkeeping rather than deduction. Every guessing game worth playing keeps its guesses visible. **One live bug caught while wiring it:** the pool guard still read `players.length >= 7` after the deal grew to eight plus a call-up, so a thin API pool would have been accepted and then dealt from with nothing left to be the call-up. Also switched the page to `hand.optimal` — the curator already solved the deal, and recomputing was a second answer that could disagree with the accepted one. 7 canaries. Production build verified. Touched `app/press-box/page.tsx`, `__tests__/feature-canaries.test.ts`.

- 2026-07-29: **Press Box — the deal is constructed now, not drawn.** An external design pass proposed a rebuild; six of its nine recommendations already shipped (the Call-Up is already hidden until the first submission, attempts are already 5, the win condition is already "find any maximum-scoring hand", the feedback already carries both the categories and X/4-cards-in-a-perfect-hand, the deal is already shared, the streak already requires the perfect hand). What it did stumble onto were three real defects, and measuring 365 days of deals made them worse than described. **(1) `MAX_SCORE = 15` was a lie the scoring never respected.** The real optimum ran **3 to 19, mean 9.2**, so a player who found the perfect hand was typically shown "8/15 ★★★★★ PERFECT HAND" — told they scored 53% for winning — and on 3% of days the optimum sat past the end of a board only 15 pegs long, where `Array.from({ length: MAX_SCORE + 1 })` could not render the target at all. **(2) On 2% of days (8 of 365) every one of the fifteen combinations scored identically**: every pick simultaneously perfect, no puzzle, roughly one day every six weeks. **(3) The deal comment claimed curation the code did not do** — it promised "at least 2 different teams, at least 2 positions, spread of divisions" over `shuffled.slice(0, 6)`. **Eight cards, on evidence rather than on "more combinations is harder":** unique best hand 42%→54%, ≤2 hands tying 51%→68%, days with no puzzle 2%→0%, and the one that matters — **three or more scoring categories in the answer, 74%→95%**, which is the difference between deducing a hand and spotting the obvious group. **The curator** proposes deals and rejects them until one is worth playing: optimum inside a 12–18 band, at most two perfect hands, three or more categories, a runner-up close enough to tempt but not to tie, and — the rule that does the most work — **the most visible single-trait grouping must be a decoy, never the answer**, so four-of-a-team cannot be both the obvious read and correct. Measured against the live pool: 32% of random deals pass, median 2 candidates, worst case 19, ~1.5ms per deal, and over 500 days **every** deal was curated with none falling back. A `dealPenalty` fallback exists anyway, because the pool drifts as rosters change and a slightly-off puzzle beats a blank page. Result: optimum now 12–18 (mean 13.8) and 75% of days have a single unique perfect hand, up from 42%. The board keeps a constant length — what the fixed 15 was actually for — while the target peg sits on today's real optimum, so a perfect hand reads as complete because it is. Peg dimensions trimmed 11px→10px since 19 pegs at the old sizing overflowed a 320px screen. Saved-state version bumped to 3: a v2 save holds picks naming cards no longer on the table, and discarding beats restoring a board that cannot be drawn. **Deferred:** the proposed communal statistics (solve distribution, trap cards) need a backend that does not exist — the most expensive item on the list and the least central to whether the puzzle is good. 17 tests + 4 canaries. Production build verified. Touched `app/lib/press-box-engine.ts`, `app/press-box/page.tsx`, `__tests__/press-box-engine.test.ts`, `__tests__/feature-canaries.test.ts`.

- 2026-07-29: **Front-of-site sweep — the CXH1 Sim jump only worked once, plus five verified copy/data bugs.** An external pass over the live site found that the first executed trade opens Sim but a **second trade in the same session leaves the user on Roster** while the Sim tab quietly grows a second entry. That is my regression and the diagnosis was right: I keyed the jump on `showSimPanel`, which is set true on execute and only cleared on reset, so there is **no false→true edge for trade #2**. The edge detection I added to stop a remount yanking the user off a tab was the very thing that swallowed every trade after the first. It now keys on `executedTrades.length` rising — a count can answer "did another trade just happen", a latched boolean cannot, and comparing against the previous count still handles remount (no change, no jump) and reset (count falls, no jump). **`showSimPanel` is deleted outright.** Its only consumer was this jump, and it duplicated a fact already derivable from `executedTrades` — two pieces of state describing one event, which is exactly how the second trade got lost. A canary now greps all three files to keep it gone. **Five more, each verified in source before touching:** (1) the home page renders "Cap & Creaseexclusive" — JSX drops whitespace between an expression container and the next line, so `{BRAND.name}` newline `exclusive` concatenates; fixed with an explicit `{" "}` and the missing article. (2) The Trade Machine labelled `team.capSpace` as "Current Cap" and a *net* `receives.nav - sends.nav` as "Package NAV" — both are now named for what they hold: Current/Projected **Cap Space** and **Net NAV**. (3) The public Docket printed `ROSTER OVERLAY` / `UI ONLY` — those describe what publishing did to this app's own roster state, an admin concern with no meaning to a reader, and they stay in the admin view. (4) **The Seth Jarvis line was a real logic bug, not a copy bug**: `coldFinish` ORed two different measurements — overall goals against overall xG, and high-danger conversion against the league rate — then headlined both with the overall claim, so "Finishing cold vs expected" ran directly above "37 G on 36 xG", the evidence contradicting the claim it was offered as proof of. A player can beat expectation overall while converting his best looks below his own rate; those are now separate branches with separate wording, and the high-danger case cites the high-danger number instead of borrowing the overall one. (5) The sitemap omitted `/fantasy` and `/glossary` and stamped `new Date()` on every entry — a page that always looks freshly modified tells a crawler exactly as much as one that never does. Data-driven routes now move with the build; the two prose pages carry a revision date meant to be bumped by hand. 4 tests + 2 canaries. Production build verified. **Not addressed, and listed for the user:** the `www` TLS/502 (DNS-side), the ~21s cold load on /teams, per-route canonical URLs and titles, the duplicated Press Box card names, the Icon Key page length, and the offseason-mode copy contradiction. Touched `app/armchair-gm/GmAnalysisTabs.tsx`, `app/armchair-gm/useTradeBench.ts`, `app/armchair-gm/page.tsx`, `app/lib/gm-tabs.ts`, `app/lib/fantasy-board.ts`, `app/components/QuickTradeMachine.tsx`, `app/docket/DocketClient.tsx`, `app/page.tsx`, `app/sitemap.ts`, `__tests__/fantasy-board.test.ts`, `__tests__/feature-canaries.test.ts`.

- 2026-07-29: **Fix — CXH9 broke the Armchair GM by capping the wrong field.** `/api/evaluate` returned **400 on a 3.1 MB request** and the page came up with no NAV for anything. The cause is mine: CXH9 bounded `assets` at `MAX_PACKAGE` (40), and on this endpoint **`assets` is not the trade package — it is the NAV batch**. `fetchNavMap` posts a whole league there to price every player at once, which is what Armchair GM does on boot and what the admin trade tool does on load. The trade package is `tradeOutgoing`/`tradeIncoming`, and those were already capped correctly at 40; I read one field's name and applied the neighbouring field's ceiling. `assets` is now `MAX_PLAYERS` (3000) — still bounded, since the endpoint is unauthenticated, but bounded at the size of the thing it actually carries. `/api/match`'s `assets` **is** a package (the Partner Finder asks "who wants this package"), so `MAX_PACKAGE` stays right there; the two fields share a name and not a meaning. **The reason this shipped is that the schema was unprovable.** It lived inside the route file, and a Next.js route can export nothing but its handlers, so no test could import it — every CXH9 test asserted against `PUBLIC_LIMITS` and against source text, and not one of them ever fed a realistic payload to the actual contract. It now lives in `app/lib/evaluate-request-schema.ts`, following the precedent `sim-request-schema.ts` already set, and the regression test posts a **league-sized batch** at it. That test fails against the previous commit. 11 tests + 1 canary that pins the distinction between a package ceiling and a batch ceiling by name. Production build verified. Touched `app/lib/evaluate-request-schema.ts` (new), `app/api/evaluate/route.ts`, `__tests__/evaluate-request-schema.test.ts` (new), `__tests__/feature-canaries.test.ts`.

- 2026-07-29: **CXH1 — the analysis deck stops blanking.** Two defects that are really one. The deck disables Compare and Breakdown when there is nothing on the blocks, and **each of those panels also renders nothing without assets** — two independent gates on the same condition with nothing reconciling them. Execute a trade from either tab and `executeTrade` clears the blocks, so the tab the user was reading became disabled *while still being the active one*: a greyed-out header over an empty panel, which reads as the trade having broken the page. The second half is that **`showSimPanel` was passed into the deck and never read.** It is set the moment a trade executes, and its whole purpose was to move the user to where the consequences are — so the prop that would have prevented the dead panel was sitting in the props type doing nothing. `app/lib/gm-tabs.ts` holds both rules, pure and tested without a DOM. `visibleTab` is a **derivation, not an effect**: a repair effect paints the empty panel for a frame before fixing it, and overwriting the selection throws the user's choice away permanently, whereas deriving means put the assets back and the tab they were reading returns on its own. `nextTab` skips disabled tabs — arrowing onto one lands on a panel with nothing in it — and enters at an end when the current tab is no longer usable, which is precisely the state this item is about. The Sim jump is a genuine event response rather than a derivation, so it stays an effect, but it fires **only on the false→true edge** via a ref: keying it on the value alone would yank the user off a tab they had chosen since, on every remount. One older canary needed repointing — it pinned `useState<GmTab>("roster")` verbatim, and the literal moved into the new module as `GM_TAB_FALLBACK`; it now asserts the default through the name and pins the name's value where it lives. 13 tests + 5 canaries. Production build verified. 7 audit items remain. Touched `app/lib/gm-tabs.ts` (new), `app/armchair-gm/GmAnalysisTabs.tsx`, `__tests__/gm-tabs.test.ts` (new), `__tests__/feature-canaries.test.ts`, `ARMCHAIR_AUDIT.md`.

- 2026-07-29: **Admin-recorded extensions now reach the Armchair GM.** Leo Carlsson and Macklin Celebrini both had their long-term deals entered in the admin contracts panel, and both still showed up as **RFAs about to hit the market**. The cause is that the app had two different things called an extension and only one of them reached the contract logic. `pendingExtension` is signed inside a session by the offseason flow and is read by the cap horizon, the eligibility gate and the rollover. `extensionCapHit`/`extensionYears` are real-world facts entered by an admin — and they were read by the **valuation engine and nothing else**. `deriveContractStatus` never asked about them, so a player under contract for five more years derived as a pending free agent, was zeroed to a $0 cap hit and 0 years, and was handed to `resolveLeagueOffseason` to be re-signed or to walk. **The decision is one question, so it is now asked once.** An extension is a signed contract that begins when the current one ends, so whether it is *future money* or *the contract* depends only on whether that current deal has run out. `resolveRecordedExtension` in `app/lib/extensions.ts` returns NONE / PENDING / ACTIVE and nothing else, and everything follows from which: **ACTIVE** is folded into `capHit`/`yearsRemaining` and the extension fields are cleared — leaving them set would have the valuation engine read a live AAV as a raise still to come and the card badge a current contract as an extension; **PENDING** keeps the current deal and is written to `pendingExtension` as well, which is the actual propagation — the signing now reaches the cap horizon, blocks a second extension, and matures into a contract at rollover instead of being a number only one engine could see. Either way `expiresThisOffseason` is false, which is the reported bug. **`expiryStatus` is deliberately left untouched** rather than cleared on signing: it is the record of what he would have been, it is what prices the extension, and destroying it would make "would have been an RFA" unsayable. Two follow-ons. The teamless free-agent pool injection needed the same guard, or an extended player with no roster row would be dumped straight into free agency by a second path. And **Hot Off the Press would have lost its two biggest signings the moment they took effect** — the feed filtered on `hasExtension`, which ACTIVE clears — so `signedAav`/`signedTerm` read whichever pair currently holds the deal. A deal beginning is not a deal being un-signed. 25 tests + 4 canaries. Production build verified. Touched `app/lib/extensions.ts`, `app/lib/roster-assembly.ts`, `app/lib/fresh-ink.ts`, `app/players/page.tsx`, `__tests__/extensions.test.ts`, `__tests__/derive-contract-status.test.ts`, `__tests__/fresh-ink.test.ts`, `__tests__/feature-canaries.test.ts`.

- 2026-07-29: **CXH2 — lineup state stops leaking and stops churning.** Four defects in one panel, three of them the same class of mistake: state that outlives what it describes. **(1) No team key.** `TeamLineup` is rendered twice with the same shape, so React matched the old instance to the new one across a franchise change and kept the previous club's order on screen under the new club's name — the sheet showed players the team did not have. Keyed to `teamId` now, so a change of club is a remount rather than a re-render. **(2) The swap ran inside a state updater.** `clickSlot` performed the order swap, the lock move and the edited flag from within a `setSelected` updater. An updater must be pure: React StrictMode double-invokes it in development, and **a swap performed twice is a swap not performed at all** — the bug concealed itself, which is why it survived. Both handlers read the selection directly and call each setter once from the event handler now. **(3) `Cell` and four siblings were components declared in render.** Every render created a new component type, so React could not match old tree to new and unmounted and remounted all 18 cells on any state change — including the `selected` change a click itself makes. That throws focus away mid-interaction, undoing the CXH8 keyboard work in the one panel that most needs it. They are plain render helpers called as functions now, so React sees returned `<td>` elements and reconciles them normally. **(4) `resetTrades` cleared the goalies but not the orders.** Reset restores the original database, so a hand-built forward group left behind was re-applied to a roster that no longer contained those players. The canary for (2) scans by **balancing parentheses** rather than a fixed character window, so it covers a whole updater body however long it grows, and it distinguishes the updater form from `setX(value)`, which is fine. **Two older canaries needed repointing**, both for the familiar reason — they pinned the exact `setSelected` updater line that this item removes, rather than the guarantee it provided. They now assert the intent: clicking across groups reselects instead of swapping, and a lock follows its player through a swap. 4 canaries. Production build verified. 8 audit items remain. Touched `app/components/LineupEditor.tsx`, `app/armchair-gm/useTradeBench.ts`, `__tests__/feature-canaries.test.ts`, `ARMCHAIR_AUDIT.md`.

- 2026-07-29: **CXH9 — public endpoints validate before they work.** `/api/simulate` was hardened in audit #4; the other three were not. **None of these endpoints authenticate**, which is the whole point: "the client only ever sends thirty players" is an assumption about a program that is not the one making the request. `/api/match` cast `req.json()` straight to its request type and then iterated whatever arrived against all 32 clubs. `/api/evaluate` had a Zod schema but no bounds in it — unbounded ids and names, unbounded arrays, and every numeric field accepting **NaN and ±Infinity**, both of which `JSON.parse` produces happily and which then propagate through the valuation engine as poisoned arithmetic rather than failing anywhere visible. `app/lib/public-request-bounds.ts` holds the ceilings so three routes cannot each pick a different limit for the same field, and follows the precedent `sim-request-schema.ts` set: permissive on FIELDS (`.passthrough()`, because the engine reads ~30 optional inputs no route enumerates, and stripping them would silently degrade valuations) and strict on STRUCTURE. **`/api/claude` had the one with real teeth.** It rate-limited *before* validating. Rate limits there exist to protect the upstream bill, and a malformed request never reaches upstream — so counting it against the **global** 2000/day counter let a stream of garbage bodies deny the feature to every real user at no cost to the sender. Parsing is bounded and cheap by comparison, so validation runs first now. Also added a 60s abort on the upstream fetch, because a call with no timeout holds the serverless invocation open until the platform kills it — the slowest and most expensive way to fail — and stopped returning the upstream error message to a public caller, since it can carry request detail. It logs server-side and returns a generic 502, or 504 on timeout. 17 tests + 5 canaries, one of which greps all four routes for the `await req.json() as T` pattern so a new endpoint cannot reintroduce it. Production build verified. 9 audit items remain. Touched `app/lib/public-request-bounds.ts` (new), `app/api/match/route.ts`, `app/api/evaluate/route.ts`, `app/api/claude/route.ts`, `__tests__/public-request-bounds.test.ts` (new), `__tests__/feature-canaries.test.ts`, `ARMCHAIR_AUDIT.md`.

- 2026-07-29: **CXH8 — overlays are usable by keyboard.** Six overlays opened over the page with no dialog semantics, no focus trap, no Escape and no focus restore. A keyboard user could tab straight out of an open modal into the page behind it and operate controls they could not see; a screen reader was never told a dialog had opened at all. Only one of the six (`ModeSelectModal`, which I wrote) had `role="dialog"`, and even that had no Escape. The fix is **one hook, not six patches** — `useDialog` supplies semantics, initial focus, the trap, Escape, focus restore and the scroll lock, and all six now use it. The cycling rule lives in `app/lib/focus-trap.ts`, pure and tested without a DOM: wrapping in both directions IS the trap, and a test sweeps every (count, current, shift) combination to prove it can never return an out-of-range index. Two judgement calls: `ModeSelectModal` and `DraftNight` are given **no** `onClose`, because a mode has to be chosen and a draft has to be completed — an Escape key with nothing safe to fall back to is worse than no Escape. And focus restore matters as much as trapping; without it a keyboard user is returned to the top of the document and has to find their place again. **The scroll-lock bug was a design problem, not an oversight.** The lock was a hand-maintained list of every overlay flag at page level, and the memo modal and Cup resume prompt were missing from it — which is what a list you must remember to edit eventually does. Binding the lock to `useDialog` deletes the list for converted overlays. Also: season rows carried a click handler and nothing else, so the valuation breakdown was unreachable by keyboard — the player name is a real button now, one focus stop, with the row click preserved for a mouse. And the GM tab deck used `aria-pressed` buttons: a pressed button is a *toggle*, a tab *selects a panel*, and only the latter is announced as "N of M" with arrow-key navigation. It is a proper `tablist` with a roving tabindex now. **Three canaries needed repointing**, all for the same reason — they pinned a mechanism rather than a guarantee. Two asserted the page-level scroll-lock argument list (rewritten to accept either mechanism and fail only if an overlay satisfies neither), and one asserted `aria-pressed={active}`, which is precisely the thing being replaced. 15 tests + 5 canaries. Production build verified. 10 audit items remain. Touched `app/lib/focus-trap.ts` (new), `app/lib/use-dialog.ts` (new), six overlay components, `app/armchair-gm/page.tsx`, `app/armchair-gm/GmAnalysisTabs.tsx`, `app/armchair-gm/SeasonResultsPager.tsx`, `__tests__/focus-trap.test.ts` (new), `__tests__/feature-canaries.test.ts`, `__tests__/offseason-phases.test.ts`, `ARMCHAIR_AUDIT.md`.

- 2026-07-29: **CXH7 — the analytics visuals now agree with the models they display.** Five places where the picture said something the model did not. **(1) The contention quadrant drew its crosshair at 5.0/5.0 while the classifier split at 6.5 and 5.5.** A club could be plotted in the "win now" half and captioned "Window Open" directly beneath it — and between a chart and a caption, people believe the chart. `CONTENTION_THRESHOLDS` and `classifyContention` are exported now, and the chart derives its gridlines, its shading **and** its labels by calling the classifier at each region's own centroid. That makes disagreement structurally impossible rather than merely fixed. It also surfaced that the classification is **not a 2×2**: present has three bands (rebuild / playoff-calibre / contender) and future two, so there are six regions. A single crosshair could never have been right, whatever value it was drawn at. **(2) "Future strength" excluded the assets that are the future.** The `qualified` filter drops picks and anyone under 10 games — correct for PRESENT strength, since those players cannot help you win now — but the same filter was applied to the future rating, so a rebuild holding five first-rounders scored identically to one holding none. Picks now contribute at `PICK_FUTURE_WEIGHT` (0.6, discounted because a pick is a lottery ticket on a person who does not exist yet) and sub-10-GP youth contribute their upside. A test pins that present strength is unchanged by adding a pick, since that was the thing the filter was right about. **(3) Team STRAND reference lines** were drawn at one uniform amplitude — the mean of the reference profile's traits — so a club above the flat "playoff" line could be under the playoff threshold on a specific trait and the picture said it was clear. They follow the same per-trait sections the team's own strand does now. An averaged contour wearing a threshold's colour is not a threshold. **(4) PlayerComparison** summed a linear package NAV while the verdict, TugBar and audit all quote the compressed value, and it folded goalies into skater rates — a goaltender's `ptsPace` is structurally near zero and his TOI is a different quantity, so any trade involving one read as a downgrade. **(5) A league-average EDGE value was painted red**: the tone was a binary split on the threshold, and half the league is below average by construction, so ordinary clubs were flagged as problems and the genuinely bad cases became indistinguishable from them. A 4% band around the threshold is neutral now. 12 tests + 7 canaries. Production build verified. 11 audit items remain. Touched `app/armchair-gm/contention.ts`, `app/components/ContentionQuadrant.tsx`, `app/components/TeamStrand.tsx`, `app/components/PlayerComparison.tsx`, `app/armchair-gm/TeamEdgeTiles.tsx`, `__tests__/contention-thresholds.test.ts` (new), `__tests__/feature-canaries.test.ts`, `ARMCHAIR_AUDIT.md`.

- 2026-07-29: **CX8 — one canonical trade-value model.** Three ways the app's stated trade terms failed to reach its own valuation. **(a) The bench reimplemented package compression, and the copy had drifted.** The engine's `compressPackage` clamps EACH marginal asset at zero; the bench's `compressBlock` summed decay and slot penalties separately and clamped the difference ONCE. Those agree only while every secondary asset carries positive marginal value — add a low-value 35-year-old and the engine drops him to zero while the copy let his slot penalty eat into the star beside him, so the TugBar and the verdict quoted different numbers for the same trade. The engine function is pure with no server-only dependencies, so there was never a reason for a copy; it is deleted and the bench calls the engine. A test keeps the old formula around **solely to prove the divergence**, and pins that the two agreed on the common case — which is why the drift went unnoticed. **(b) Pick protection was decoration.** A visible toggle that `calcPickNAV`, the share URL and execution all ignored: protected picks transferred unconditionally and cost the acquiring club full price. Protection now discounts the pick, steepest where it actually bites — a bottom-five club's first is largely a lottery ticket and protecting it removes most of what the buyer wanted (0.55), a contender's late first rarely lands in the range at all (0.88). It also serialises: `id:pct:P`, with the retention slot keeping its position so links written before CX8 parse unchanged and, importantly, do **not** come back protected. The flag is omitted rather than written false, so an unprotected package's link is byte-identical to before. **(c) Retention limits only applied inside a Cup Run.** Three slots, 50% a contract, a share of the ceiling — those are league rules, not run rules, and the check sat inside `if (cupRun?.status === "ACTIVE")`, so single-season play could retain 50% on any number of players with nothing to stop it. One check, two ledgers now: the run's persists across seasons, a session ledger covers single-season play, and the limits are identical. Lint caught a real bug in my first version — `executeTrade` read `sessionRetention` without depending on it, a stale closure that would have checked against an old ledger and let a fourth retention through. Reading through a ref keeps the callback stable and always current. 19 tests + 4 canaries. Production build verified. 12 audit items remain. Touched `app/lib/xnav-engine.ts`, `app/lib/trade-share.ts`, `app/armchair-gm/page.tsx`, `app/armchair-gm/useTradeBench.ts`, `__tests__/package-compression.test.ts` (new), `__tests__/feature-canaries.test.ts`, `ARMCHAIR_AUDIT.md`.

- 2026-07-29: **RL6 — special teams, and §2 is closed.** The lineup sheet was 5-on-5 only: four lines, three pairs, two goalies. Every club spends roughly a fifth of the game shorthanded or on the man advantage, and the gap between a first and second power-play unit is most of the difference between a 60-point winger and an 80-point one — a lineup editor that cannot express that is not really setting a lineup. `app/lib/special-teams.ts` holds the geometry (PP 10 slots sliced into two units of five, PK 8 into two of four), the defaults, and the sim contribution. **The two sheets rank on different things**, which is the point of having both: the power play is picked on scoring, the kill on `pkTimeShare`/`defRate`/ice time, and a test asserts they do not simply produce the same names in the same order. Composition follows the real thing — four forwards and a defenceman on a power-play unit, two and two on a kill. Units survive a trade the way the 5-on-5 sheet does: departed players drop out, the gap closes, and defaults backfill, because a unit with a hole in it would silently play short. **The half that matters is the sim.** Units ride on the existing `SkaterDeployment` record rather than a second parallel lookup, so `projectSkaterOutcome` reads one object: PP1 +14% scoring, PP2 +5%, PK a small scoring *cost* (kill time displaces even-strength time) and a games bonus (a coach who uses you shorthanded dresses you more often). Deliberately smaller than the line multipliers, which span 0.90-1.10 — a test pins the whole special-teams range inside 0.9-1.2 so the sheet matters without swamping the lines it sits beside — and everything still passes through the existing `ptsCeiling`. **One wrong turn worth recording:** I first gated the sim's `pkTimeShare` heuristic on whether an explicit sheet existed, to avoid double-counting killers. That was the wrong place. `onIceValue` is a memoised per-player function feeding *team strength*, while the sheet feeds the *individual skater projection* — different channels, so they never double-counted, and threading a flag through would have broken the memo and eleven call sites for nothing. Reverted, with a note at the site explaining which channel is which so the next person does not repeat it. 21 unit tests + 5 canaries; one canary needed fixing after I wrote a malformed assertion that mangled `export function X` into `export X` — the exports are a mix of `const` and `function`, so it matches either now. Production build verified. **§2 Roster/cards/lineups is complete (RL1-RL8).** 13 audit items remain, all Codex CX. Touched `app/lib/special-teams.ts` (new), `app/lib/lineup-order.ts`, `app/components/LineupEditor.tsx`, `app/api/simulate/route.ts`, `__tests__/special-teams.test.ts` (new), `__tests__/feature-canaries.test.ts`, `ARMCHAIR_AUDIT.md`.

- 2026-07-29: **Roster and Season Review differentiated (RL2 follow-up).** Owner's call: keep both, but make the difference legible. Worth recording the subtlety, because my first framing of it was wrong — the split is **not** simply before/after. The Roster tab *does* show simulated numbers once a season has been run; that is RL2's "refresh from the prior sim season", and a roster that ignored the year you just played would be the bug. The real distinction is **carried-forward state versus a one-season verdict**: Roster answers "who do I hold and what is their current form", Season Review answers "how did that season compare with what the model expected". Both surfaces show a points column, so a reader who cannot tell them apart will reasonably conclude one of them is broken. Each now states its job in a line — "Who you hold." / "How it went." — and points at the other, so the overlap reads as two views rather than a contradiction. The roster header also names the season its numbers come from (`2026-27 results` vs `2025-26 baseline`) instead of the vaguer "simulated / pre-season", since the useful question is not *whether* the numbers are simulated but *which season* they are. One canary needed repointing: it pinned the literal strings "Simulated season" and "Pre-season baseline", which is the tab's wording rather than its guarantee, and the wording changed the first time the label improved. It now asserts the view distinguishes the two cases and names the season. 3 canaries. Production build verified. Touched `app/armchair-gm/RosterTab.tsx`, `app/armchair-gm/SeasonResultsPager.tsx`, `__tests__/feature-canaries.test.ts`.

- 2026-07-29: **RL2 — the roster gets its own tab.** The roster listing lived inside Season Results under "Team Numbers", which meant you could only read your own roster **after** simulating a season, three clicks from the trade bench where roster questions actually get asked. `RosterTab` is now the deck's first tab, ahead of Lineups, because you read a roster before you set lines. Ordered by points, ties broken on games then name so rows cannot jitter between renders. Rows expand into the **shared** `ExpandedStats` from OFF4 rather than a second implementation — a hand-rolled expander here would drift from the offseason screens, which is precisely what extracting that component was for — with `PlayerOutlook` composed alongside rather than pushed *into* it: this is a GM surface so it wants the analytics-desk read (the same call RL3 made for the trade card), but the offseason screens should not silently change shape because the roster tab wanted a panel. `AssetBadges` supplies the icons, and the leadership letters from RL4 appear here too. The "refresh from the prior sim season" half is `buildRosterRows`, which merges the simulated season **per player** rather than all-or-nothing, so a call-up with no projection still shows a baseline row instead of blanks. One unit problem worth recording: the baseline is an 82-game *pace* while a simulated season is a played *total*, and showing "82 pts" beside "61 GP" in the same column would be nonsense — the baseline is scaled to games actually played, and a test pins that G + A always equals the points column so rounding cannot make the row disagree with itself. The header states plainly whether the numbers are a simulated season or the pre-season baseline; a roster still reporting last year's points after you simulated a year would misstate the one thing the tab exists for. **On "Replace Team Numbers":** the Ledger Line there carries ΔXP (did he beat his preseason expectation) and NOIV, which only mean anything once a season has been played — genuinely a *retrospective*, not a roster. Retitled it "Season Review — Performance vs Expectation" and collapsed it by default rather than deleting information the owner may still want; flagged for a decision. 16 unit tests + 5 canaries. Production build verified. 14 audit items remain. Touched `app/lib/roster-view.ts` (new), `app/armchair-gm/RosterTab.tsx` (new), `app/armchair-gm/GmAnalysisTabs.tsx`, `app/armchair-gm/SeasonResultsPager.tsx`, `__tests__/roster-view.test.ts` (new), `__tests__/feature-canaries.test.ts`, `ARMCHAIR_AUDIT.md`.

- 2026-07-29: **Palette cleanup — orphan accents retired, and a second navy caught.** Asked for a ten-minute gold/violet cleanup; it turned up more than that. **Gold** (`#946914`, one call site) was a near-duplicate of amber `#8a5c00` and is now amber — "change of scenery" is a caution-level signal, which is what amber already means across its other 70 usages. **Violet** (`#5b4a9b`, one call site) coloured the YNG bar for a positive age contribution against amber for a negative one; that positive/negative pairing is green/amber everywhere else in the app, so it is green now. The only other emerald bar is G-NAV, which renders in the goalie-only branch, so the two never appear together. Both tokens dropped from `globals.css` and the Tailwind palette. **The bigger find:** yesterday's ice rotation was done by token *name*, so `--blue #2b3f66` — **31 usages**, hue 220°, the same cold navy — survived it untouched. My claim that "the app's blue now belongs to the brand" was therefore incomplete when I made it. Rotated on the same terms: hue 195°, lightness unchanged, `#2b5766`, 6.68:1 on paper. Also discovered the app carries **two parallel token families** — `--ledger-*` and a shorter `--red/--green/--amber/--blue` set — holding near-duplicate values, and `--amber` was holding the *retired gold hex*, so the two families disagreed about what amber is. Pointed it at `#8a5c00` so they agree. The rest of that duplication is left alone deliberately: consolidating two live palettes is a bigger change than this task, and worth doing on purpose rather than as a side effect. Added a guard that walks every `.ts/.tsx/.css` under `app/` and fails on either retired navy hex, plus an assertion that every blue token in the file is cyan-leaning — a 220° navy cannot pass it. That is the check that would have caught `--blue` yesterday. 7 tests. Production build verified. Touched `app/globals.css`, `tailwind.config.ts`, `app/components/AssetBadges.tsx`, `app/components/AssetCard.tsx`, `app/components/MicroBar.tsx`, `app/components/TradeProposal.tsx`, `app/components/TradeBlockPanel.tsx`, `__tests__/brand-kit.test.ts`.

- 2026-07-29: **The app's blue joins the brand — as a two-step ice ramp, not a swap.** Owner asked to change the existing blue to the kit's ice blue `#79afc1`. Straight substitution would have been wrong twice over. **Measured, not assumed:** `#79afc1` on `--paper` is **2.04:1**, against WCAG AA's 4.5:1 minimum for text — and the ~95 `--ledger-navy` references are overwhelmingly `color:`, i.e. labels, chips and links. The kit says as much itself: ice blue is "reserved for the crease and related data accents", which is a fill instruction. So the fix keeps the intent (the blue should belong to Cap & Crease) without the breakage: `--ledger-navy #1a2e5c` was **hue 222°**, a cold navy unrelated to anything in the identity, and it is now `--ledger-ice #1a4b5b` at **hue 194.8°** — the kit ice blue's hue to within a fifth of a degree — at the *same* lightness (22.9% vs 23.1%). A pure hue rotation: the blue visibly changes, the readability does not (8.07:1 on paper, 6.73:1 on card; the navy was 11.17:1, so this is a small and deliberate reduction). `--cc-ice-blue #79afc1` stays as the fill step for the crease and data accents. Renamed the token rather than leaving a lie behind — a variable called "navy" holding a teal is exactly the fault I filed as audit item (6) this morning — which meant the Tailwind palette entry, six `text-ledger-navy` utility classes my CSS-variable rename had missed, and a `RINK_NAVY` constant. Three incidental finds: `LedgerDropdown` referenced `var(--navy)`, **a token that has never existed**, so the Bubble phase chip has been rendering with no colour at all; `RINK_NAVY` collided on rename with an existing `RINK_ICE` (the pale ice *surface*), so the blue line is now `RINK_BLUE_LINE` and both are documented; and the global sed rewrote **my own explanatory comment** in `globals.css` into a statement of the opposite, which is a good argument for reading a mechanical rename's diff rather than trusting it. Added a contrast test that computes WCAG luminance directly, asserts the deep step clears 4.5:1 on both paper surfaces, asserts the kit value *cannot* carry text, and checks the hue is cyan-leaning rather than merely blue — so a future "let's just use the kit colour" cannot land silently. 5 tests. Production build verified. Touched `app/globals.css`, `tailwind.config.ts`, `app/lib/gravity-rink.ts`, `app/api/card-image/route.tsx`, `app/components/TeamStrand.tsx`, `app/components/LedgerDropdown.tsx`, `app/components/AssetCard.tsx`, `app/components/DevelopmentProfilePanel.tsx`, `app/players/loading.tsx`, ~20 further files by rename, `__tests__/brand-kit.test.ts`, `__tests__/feature-canaries.test.ts`.

- 2026-07-29: **Front page on the brand kit.** The home page carries **two** nameplates — the dark scroll hero (`ScrollNameplate`) and the broadsheet sheet below it — and both were Libre Baskerville with a typed `&`, the same violation the header had. Both are the kit wordmark now, with the `<h1>` retained as `sr-only` on the sheet so the front page keeps a real heading, and both images marked decorative so a screen reader is not told the name three times on one page. The mark sits above the nameplate as a crest, which is the traditional broadsheet position for a device. One thing the kit does not ship: a **cream wordmark**, and the hero sits on the dark desk. Checked whether the lettering was recolourable before assuming — `IMPLEMENTATION.md` says the wordmark is outlined, and it is: no `<text>` elements, ten real `<path>`s, with the `font-family` on the groups being an inert leftover and `fill:#1c140a` living on two `<g>` styles the paths inherit. So `cap-and-crease-wordmark-cream.svg` is derived by swapping those two fills, keeping the ampersand red rather than washing it out. Because that is a derived asset rather than a kit one, a test asserts it stays **path-identical** to the kit wordmark and differs only in the ink fill and its title — otherwise a kit revision would update the sheet nameplate and silently leave the hero on a superseded wordmark. Also moved the one remaining prose mention to `BRAND.name`. Not touched, deliberately: the `buymeacoffee.com/hockeyledger` handle, which remains the owner's external account to migrate. 3 tests + 5 canaries, one of which scans both nameplate files for a typed name with comments stripped. Production build verified. Touched `app/page.tsx`, `app/components/ScrollNameplate.tsx`, `public/brand/svg/cap-and-crease-wordmark-cream.svg` (new, derived), `__tests__/brand-kit.test.ts`, `__tests__/feature-canaries.test.ts`.

- 2026-07-29: **Brand kit implemented — the crease mark ships.** The owner delivered `docs/cap-and-crease-brand-kit` (v1.1.0) and the mark is now live across the app. Assets copied to `public/brand/`; the kit's five tokens added to `globals.css`, with **four of them aliased rather than duplicated** — `--cc-ink`/`--cc-paper`/`--cc-red`/`--cc-brass` already existed under ledger names at identical hexes, so restating them would create two values that can silently disagree at the next kit revision. Ice blue `#79afc1` is the only genuinely new one, reserved per the kit for the crease and data accents, never a page background. **Two deliberate departures from the supplied implementation, both for concrete reasons.** The kit's `BrandMark.tsx` renders through `next/image`, which refuses to optimise SVG unless `dangerouslyAllowSVG` is set in `next.config` — and that flag applies to *every* image the optimiser touches, which is a large surface to open for a logo. The marks are ~700 bytes, so `app/components/BrandMark.tsx` inlines them instead: no network request, no config flag, and the one-colour variants can take a colour prop. Inlining copies the kit's path data, so `__tests__/brand-kit.test.ts` reads the published SVGs and asserts every `d` attribute still appears in the component — a kit revision now fails loudly rather than leaving the app rendering a superseded mark. Second departure: the size threshold. The kit's prose says primary at 40px and up, small at 32 and under; its supplied code switches at `size <= 32`, leaving 33-39 on the primary cut. Followed the prose (`SMALL_CUT_BELOW = 40`), which is the more conservative reading since the small cut is the optically corrected one. **The masthead was violating the brand guidance** and this fixed it: the kit is explicit that the red ampersand is a custom vector and must not be recreated as a typed `&`, but the header was rendering `Cap & Crease` as text in whatever serif the browser had. It is the horizontal lockup now, with the `<h1>` retained as `sr-only` so the page keeps a real heading for search and screen readers, and the image marked decorative rather than announcing the site a second time. Metadata merged from the kit's snippet — icons, manifest, mask-icon, OG/Twitter image — while titles stay driven by `BRAND`, so the name still lives in one file. `app/favicon.ico` replaced with the kit's so Next's automatic `.ico` route and the metadata icons cannot disagree. The mark also signs off the footer at 44px. 15 tests + 5 canaries; one canary needed comment-stripping again, having matched the very comment explaining what it forbids. Production build verified. Touched `public/brand/` (new, 53 files), `app/components/BrandMark.tsx` (new), `app/components/Header.tsx`, `app/components/Footer.tsx`, `app/layout.tsx`, `app/globals.css`, `app/favicon.ico`, `__tests__/brand-kit.test.ts` (new), `__tests__/feature-canaries.test.ts`.

- 2026-07-28: **RL5 — line locks.** Lineup order is recomputed on every roster change: a trade, a signing, a Cup Run rollover all re-run the merge. That is right for players you never touched and destructive for the one decision you cared about — put a specific winger on the top line, acquire a centre, and the reflow quietly undoes it. A lock pins one player to one slot. `app/lib/lineup-locks.ts` is pure and carries the rules: `applyLocks` re-seats pinned players and flows everyone else around them in their existing relative order, never dropping or duplicating anyone; `pruneLocks` clears a lock whose player left, since a lock that outlives its player re-seats whoever inherits the slot and is invisible in the UI (there is no player on it to show a badge); `swapLocks` moves a lock **with its player** on a manual swap, because following the slot instead means a manual move is silently undone by the next hydrate. Locks constrain automatic ordering only — a manual drag is the user overruling themselves, so it should not be blocked. The detail that makes it real rather than decorative: locks are honoured on **both** auto-order paths, the roster-change merge and Best Lines. Honouring only the first would produce a lock that survives a trade but not a button, which is worse than no lock because it looks like it works. Two things I caught in my own implementation. `seatLocks` reads locks through a ref, so the roster effect still fires on roster identity rather than on every lock toggle — listing `locks` as a dependency would re-seat the sheet on each click. And my first `toggleSlotLock` called `setLocks` inside a `setOrders` updater to read the current slot: a state update nested in another updater is a side effect in a reducer, StrictMode double-invokes it, and it is the exact impurity **CXH2** flags a few lines away in the same file. Reads `orders` directly now. 23 unit tests + 6 canaries. Production build verified. 15 audit items remain. Touched `app/lib/lineup-locks.ts` (new), `app/components/LineupEditor.tsx`, `__tests__/lineup-locks.test.ts` (new), `__tests__/feature-canaries.test.ts`, `ARMCHAIR_AUDIT.md`.

- 2026-07-28: **RL3 — the trade card trades DEV for Outlook and Gravity (RL1 closed too).** `DEV` showed `DevelopmentProfilePanel` — dynasty score, boom/bust signal — on the card a GM reads while evaluating a trade. That is a fantasy question, and the panel still serves it on the fantasy and docket surfaces; what belongs on a trade card is the analytics-desk read. `OUTLOOK` renders `PlayerOutlook` (PA12: trajectory + next season, NHL EDGE as the leading indicator) off the same `developmentProfile` gate, so nothing lost coverage — the same players who had a DEV tab have an OUTLOOK tab. `GRAVITY` is the bigger win: the model this app is built around was a **one-line `CompactGravity` strip at the bottom of `STATS`**, while `GravityField` — the full zone-mass diagram — was reachable nowhere from the card. It is a tab now. One implementation note that a canary caught: I added a fresh `gravityProfile` memo and the card ended up computing gravity twice, because `gravProfile` already existed with a *stricter* gate (no picks, no goalies, 10+ GP). The existing gate is the correct one — a goalie has no gravity profile and under 10 games there is not enough signal to draw a field from — so the duplicate was removed rather than the gate loosened, and the tab, its panel and the strip all read the one memo. A tab that opens onto nothing is worse than no tab. An older canary pinned the literal `type AssetCardView = "STATS" | "STRAND" | "TIMELINE" | "DEV"` string; it was pinning the tab's *name* when its actual guarantee was "a tab gated on the profile existing, with NAV controls untouched". Repointed at that, plus a new assertion that the fantasy panel is gone from the trade card and still present on the two surfaces that legitimately use it. Also closed **RL1** — the owner confirmed Codex fixed it earlier, and a re-audit agrees: season tabs `#e8dab8`, `TeamSelectModal` on `--ledger-card` with a red selected border, `LineupEditor` cream throughout. 3 new canaries, 1 repointed. 16 audit items remain. Production build verified. Touched `app/components/AssetCard.tsx`, `__tests__/feature-canaries.test.ts`, `ARMCHAIR_AUDIT.md`.

- 2026-07-28: **RL4 leadership letters + RL7 goalie tandem.** **RL4:** `app/data/leadership.ts` has carried curated C/A letters since the sim was written, but only as a *scoring* input — a bonus inside `lineup-ranking` and the season sim. Nothing ever rendered them, so the roster read as an undifferentiated list of names where the real thing has a captain and two alternates on every sweater. `app/lib/team-leadership.ts` resolves one C and at most two A's per roster, ranked by the same `lineupContributionScore` the lineup itself orders on, so the letters agree with the sheet beneath them. Two judgement calls worth recording: the curated table is deliberately partial, so a club with no curated captain gets `captain: null` rather than a promoted alternate — inventing a captain puts a letter on a real player who does not wear one — and `letterFor` reads the *resolved* leadership rather than the curated role, because a curated "A" who is third in line on a deep roster wears nothing when only two dress. Lookup also moved to a `canonicalNameSlug` index: the raw `LEADERSHIP[name]` index was the exact failure ST3 found in `SECONDARY_POSITIONS`, where accented feed names silently resolved to null. **RL7:** the sim projected a starter at 48-68 starts and stopped, leaving up to 34 games a season unaccounted for and every recap showing one goalie. `projectOneGoalie` is now shared by both halves of the tandem — a separate, simpler backup formula would have drifted from the starter's on the next tuning pass, and the difference between the two would stop meaning "these are different goalies" and start meaning "these use different math". `splitGoalieStarts` divides all 82. My first version floored the starter at `82 - MAX_STARTER_STARTS` = 14, which a test caught inverting the tandem: a low draw handed the *backup* 68 games. Floor is now 42 — a starter starts more than his backup, which is what the word means — and `MIN_BACKUP_STARTS` is derived from the ceiling rather than being an independent constant that could disagree with it. The start draw is taken **before** either projection so the starter occupies the same `rand()` sequence position it always did; adding a backup must not reproject every existing seed, and the full suite passing unchanged is the evidence. 22 tests + 4 canaries. Touched `app/lib/team-leadership.ts` (new), `app/lib/goalie-workload.ts` (new), `app/data/leadership.ts`, `app/components/LineupEditor.tsx`, `app/api/simulate/route.ts`, `app/armchair-gm/SeasonResultsPager.tsx`, `__tests__/team-leadership.test.ts` (new), `__tests__/goalie-workload.test.ts` (new), `__tests__/feature-canaries.test.ts`, `ARMCHAIR_AUDIT.md`.

- 2026-07-28: **Audit close-out batch 1 — RL8, CXS1/4/5, plus reconciling two items already closed.** Starting the run at the remaining ARMCHAIR/Codex items. **RL8:** lineup rows read `34G · 72A · 19.2 TOI` instead of `106 P82`. A per-82 rate is the right unit for comparing players on different games-played totals, but a lineup card asks "who is this winger", and counting stats answer that in the language the sport actually uses; `goalsPace`/`assistsPace` sit on the same 82-game basis, so the row still totals to `ptsPace` and the default ordering is untouched. **CXS1:** `TeamSelectModal` called `.sort()` directly on its `teams` prop — which is shared store state, and `.sort()` mutates — so opening the franchise picker reordered the league for every other consumer as a side effect. Copies first now. **CXS4:** recap headings were matched against an allowlist of prefixes (`'**THE '`, `'**EDMONTON'`, `'**AROUND'`), so a recap for any club except Edmonton had its club-name heading rendered as body text. Detected structurally now — a fully-bold line, or a markdown `##`. **CXS5:** asset expiry read `new Date().getFullYear() + yearsRemaining`. The audit filed this as a Cup Run bug, and it is, but it is worse than filed: the app projects `SEASON.label`, not the calendar year, so the two agree today only by coincidence and part company every January — on 2 Jan 2027 the wall clock reads 2027 while the app still simulates 2026-27 and every contract on every card silently gains a year. `app/lib/contract-expiry.ts` anchors to the season and takes the Cup year, threaded through `TradePanel`; a test pins that its labels agree with `seasonLabelForYear`, so a contract and the Cup Run header can't disagree about what year it is. Also checked off **OFF6** (closed by the FA_POOL work) and **CXS3** (closed by OFF1) — both were done and still showing open. 20 items remain. 10 tests + 4 canaries. One canary needed comment-stripping: it matched the very comment explaining the prefixes it had removed. Touched `app/lib/contract-expiry.ts` (new), `app/lib/lineup-order.ts`, `app/components/LineupEditor.tsx`, `app/components/AssetCard.tsx`, `app/components/TradePanel.tsx`, `app/armchair-gm/page.tsx`, `app/armchair-gm/TeamSelectModal.tsx`, `app/armchair-gm/SeasonResultsPager.tsx`, `__tests__/contract-expiry.test.ts` (new), `__tests__/feature-canaries.test.ts`, `docs/July23_Armchair_GM_Claude_Code/July23_Armchair_GM_Claude_Code/ARMCHAIR_AUDIT.md`.

- 2026-07-28: **Imagery policy made explicit: headshots on the site, never in the export.** Owner's call, and a narrowing of what I had previously claimed. When league imagery was removed I reported that "the app ships no league-owned imagery" and wrote a canary asserting it — but that canary greps source files for the literal string `assets.nhle.com`, so it is structurally blind to a URL that arrives as **data** at runtime. `/players` (both layouts), `/players/[playerId]` and `/press-box` all render `<img src={player.headshot}>` from the league payload and always did. The claim was too broad; what I had actually verified was the trade machine and the export card. Surfaced by the owner reading a cached payload in the Upstash data browser and spotting the `assets.nhle.com` URL sitting in it. The decision is to **keep** hotlinking on the site — that displays the league's image from the league's own server, in context — while the downloadable PNG stays clean, because baking a copy into a branded file built to travel is redistribution and travelling is the card's whole purpose. That asymmetry was already the shipped behaviour, so no rendering changed; what changed is that the code now states it. The stale comment on `CardImagePayload` still described the deleted inlining path and now documents the policy instead. One real gap closed along the way, caught by a test I wrote expecting it to pass: the schema tolerates `headshotDataUrl` so a stale client bundle isn't rejected by `.strict()`, but zod passes unknown-but-declared keys straight through, so the validated object still **carried** it into the renderer. The policy held only for as long as nobody wrote `data.headshotDataUrl`. The validator now destructures it away at the boundary, making it unavailable rather than merely unused. Canary block renamed from the overclaim to what is actually enforced. 3 tests. Touched `app/lib/card-payload.ts`, `__tests__/gravity-public-contract.test.ts`, `__tests__/feature-canaries.test.ts`.

- 2026-07-28: **Redis credentials resolve under either provisioning name.** Setting up Upstash through the Vercel marketplace integration provisions the database correctly and then names the variables `KV_REST_API_URL` / `KV_REST_API_TOKEN` — not `UPSTASH_REDIS_REST_URL` / `_TOKEN`, which is all `app/lib/redis.ts` read. So a properly configured integration still produced `redis === null`, and because every cache write is wrapped in `.catch(() => {})`, the failure was **completely silent**: no error, no log, just the ~40s roster assembly rebuilding on every request as though no cache had ever been configured. That is a bad way to find out, and it would have made the whole SWR cold-load effort look like it did nothing. `app/lib/redis-credentials.ts` (pure, testable without a client) matches the two pairs explicitly and prefers `UPSTASH_` when both exist. Deliberately **not** accepted: `KV_URL` and `REDIS_URL`, which sit in the same panel but are `rediss://` strings for a TCP client where the SDK speaks HTTP; and `KV_REST_API_READ_ONLY_TOKEN`, which is the genuinely dangerous one — reads would all succeed, every `setex` would be rejected, and the swallowed catch would hide it, leaving a cache that looks alive and never fills. A `rediss://` value in the REST slot is rejected at resolution rather than passed through to fail per-request, on the principle that the config error should surface where the config is read. 8 tests + 3 canaries; README env table documents both namings and why the other three are ignored. Touched `app/lib/redis-credentials.ts` (new), `app/lib/redis.ts`, `README.md`, `__tests__/redis-credentials.test.ts` (new), `__tests__/feature-canaries.test.ts`.

- 2026-07-28: **Three field-naming bugs from the live-site audit: regulation wins, Docket direction, and the two meanings of `phase`.** All three are the same failure in different clothes — one name doing two jobs. (1) **Regulation wins read 0 for every club.** The NHL's stats REST endpoint spells it `winsInRegulation`; `api-web`'s standings endpoint spells it `regulationWins`. Both routes read only the second, off rows from the first, so Buffalo showed 50 wins and 0 RW. The visible bug was cosmetic; the invisible one was not, since RW is the NHL's **first tiebreaker after points** and a field that always reads 0 silently disables it — clubs level on points were being ordered by whatever sequence the feed happened to return. `app/lib/nhl-standings-fields.ts` accepts both spellings, because the mistake was not a typo: it was the same typo made independently in two routes. (2) **The Docket printed every trade backwards.** Each panel was headed `{team} RECEIVED VALUE` and listed `side.assetsGiven` — the assets that club *sent*. Anyone reading the grade beside it drew the opposite conclusion from the one the grade supports. "Received" is not a field on the record, it is derived: in a two-team trade what a club receives is exactly the other club's `assetsGiven`. That swap has no meaning in a three-way, where the record does not say where each asset landed, so those are now labelled **SENT** — what the data actually is — rather than guessed at. (3) **`Team.phase` carried two different facts.** `/api/league` computes it from live standings (conference rank, division rank, points percentage) and Team Analytics renders it as the tier chip and filters on it; Armchair GM then **overwrote that same field in local state** with a read of the current roster's valuations. Both are legitimate; storing them in one field was not. A club could be a Contender by the table and Rebuilding by its roster, and `team.phase` reported whichever meaning wrote last — and the overwrite was destructive, so once Armchair GM ran the standings tier was gone. Split into `phase` (standings tier) and `rosterWindow` (live roster), read through `teamWindow()`. The reason this stayed invisible for so long is the asymmetry: almost every consumer wants the *window* — trade willingness, pick value, sim baselines, Cup Run difficulty, prospect development all reason about the roster in front of them — and only Team Analytics wants the tier, so the wrong field was usually close enough. Ten call sites now say which one they mean. 20 tests + 9 canaries. Touched `app/lib/nhl-standings-fields.ts` (new), `app/lib/team-window.ts` (new), `app/lib/docket-view.ts`, `app/lib/trade-types.ts`, `app/lib/need-targets.ts`, `app/lib/cup-run.ts`, `app/lib/draft-pick-inventory.ts`, `app/lib/roster-assembly.ts`, `app/api/league/route.ts`, `app/api/league/teams/route.ts`, `app/api/match/route.ts`, `app/api/simulate/route.ts`, `app/docket/DocketClient.tsx`, `app/armchair-gm/page.tsx`, `app/armchair-gm/GmAnalysisTabs.tsx`, `app/armchair-gm/TeamSelectModal.tsx`, `app/armchair-gm/useTradeBench.ts`, `app/armchair-gm/useSimDispatch.ts`, `__tests__/nhl-standings-fields.test.ts` (new), `__tests__/team-window.test.ts` (new), `__tests__/docket-view.test.ts`, `__tests__/feature-canaries.test.ts`.

- 2026-07-28: **Cap space reconciled across routes; Cup Run panel gated on mode.** From an external audit of the live site. (1) **`/api/league` and `/api/league/teams` disagreed by exactly $8.5M for all 32 clubs** — Winnipeg $5.0M in Team Analytics and $13.5M in the Trade Machine, Colorado −$1.9M and +$6.6M. The constant figure gave it away: 8.5 is 104.0 − 95.5. The teams route rebased the curated `TEAMS_DB.capSpace` values onto the live ceiling; the league route used them raw. Two pages quoting different cap space for the same club is the kind of contradiction that costs trust in every other number on the page, so the rule now lives in `app/lib/team-cap-space.ts` and both routes call it. One subtlety preserved: a *scraped* CapWages figure is already measured against the current ceiling, so it is used as-is — rebasing it would double-count the delta. (2) **The Cup Run panel is now rendered only when `gameMode === "CUP_RUN"`.** My own mode-select change was incomplete: choosing Single Season still left the legacy start button on the board, and pressing it restores the entry baseline — discarding the offseason just played, which is precisely what asking the question up front was meant to prevent. The Decision-A canary pinned the ceiling-delta arithmetic inline in the teams route and was repointed at the shared module, plus an assertion that both routes use it. 7 new tests. Touched `app/lib/team-cap-space.ts` (new), `app/api/league/route.ts`, `app/api/league/teams/route.ts`, `app/armchair-gm/page.tsx`, `__tests__/team-cap-space.test.ts` (new), `__tests__/offseason-phases.test.ts`, `__tests__/feature-canaries.test.ts`.

- 2026-07-28: **Rebrand — The Hockey Ledger → Cap & Crease.** The old name collided with two live hockey products (hockeyledger.com, a hockey database for the same audience; thehockeyledger.ca), so search and brand confusion were guaranteed no matter which TLD was bought. **Cap & Crease** names both halves of what the app became — contracts, trades and roster construction on one side, on-ice performance and analytics on the other — without the "hockey + publication noun" construction that produced the collision in the first place. Domain will be `capandcrease.com`; the ampersand stays in the masthead and "and" carries it in the URL, which is the settled convention (barnesandnoble.com). 31 files rebranded, and the name now lives in `app/lib/brand.ts` rather than in forty inline strings — this rename is exactly the argument for that constant, and the next one should touch a single file. Metadata, robots and sitemap read from it. Deliberately **not** named "NHL <anything>": the mark is used descriptively in the subtitle (`NHL Trade Machine, Player Analytics & Armchair GM`) where nominative use is defensible, never as identity, and a non-affiliation line now sits in the footer. The welcome-modal storage key was bumped so a returning visitor is told the paper changed its name once rather than silently finding a different masthead. Two things deliberately left alone: the `buymeacoffee.com/hockeyledger` handle, which is an external account under the old name and the owner's to migrate — breaking the link would be worse than the stale slug — and the dated audit/devnote history, since rewriting a record to say something it did not say falsifies it. Canaries: the brand comes from one definition, metadata is built from it, the footer states non-affiliation, the name never contains NHL, and a walk of `app/` fails on any surviving "hockey ledger" outside that one external link. Production build verified. Touched `app/lib/brand.ts` (new), `app/layout.tsx`, `app/robots.ts`, `app/sitemap.ts`, `app/components/Footer.tsx`, `app/components/WelcomeModal.tsx`, 25 further app files, `README.md`, `ANALYTICS.md`, `CHANGES.md`, `__tests__/feature-canaries.test.ts`.

- 2026-07-28: **Removed all league-owned imagery — drawn marks instead.** Two redistribution paths existed. Player headshots were proxied same-origin through `/api/headshot` **and inlined as data URLs into the exported shareable card**, which is distribution of NHL-hosted photography inside a branded asset built to travel — structural exposure, not incidental, since travelling is the card's whole purpose. Club logos were hotlinked straight from `assets.nhle.com`: registered marks, served by their owner, rendered under our brand. Replaced rather than hidden. `PlayerAvatar` (new) draws an engraved bust plus the player's initials in Courier Prime on the ledger palette — goalies get a cage so the one position whose job looks different reads differently — and `playerAvatarSvgMarkup` gives the server-rendered export the same drawing with literal colours, since the export has no CSS custom properties. `TeamMark` (new) sets the three-letter club abbreviation in the masthead face. The `/api/headshot` route is deleted outright, so there is no fetch path left to reintroduce. Swapped in `TrendingPlayers` (×2), `CapProjection`, `PlayerComparison`, `AssetCard`, `PercentileCard` (in-app and export), `LedgerDropdown` and `TeamSelectModal`. Side benefits worth noting: nothing is fetched, nothing 404s when a player has no photo, and the card now looks more like the newspaper it is pretending to be — the full-colour cutout was always the one element fighting the aesthetic. Two canaries pinned the old behaviour (`/api/headshot?u=`, `readAsDataURL`) and were repointed at the replacement, plus a new canary that walks `app/` and fails if any file hotlinks an NHL asset host or if the proxy route returns. Touched `app/components/PlayerAvatar.tsx` (new), `app/components/TeamMark.tsx` (new), `app/api/card-image/route.tsx`, `app/components/PercentileCard.tsx`, `app/components/TrendingPlayers.tsx`, `app/components/CapProjection.tsx`, `app/components/PlayerComparison.tsx`, `app/components/AssetCard.tsx`, `app/components/LedgerDropdown.tsx`, `app/armchair-gm/TeamSelectModal.tsx`, `app/api/headshot/` (deleted), `__tests__/feature-canaries.test.ts`.

- 2026-07-28: **Mode select moved before the offseason (UX).** The Cup Run started from the trade bench — i.e. *after* the offseason had already been played: draft, every re-signing, every offer sheet. Starting a run resets the league to its entry baseline, which ST1 made correct and necessary, and the direct consequence was that all of that work was discarded and had to be repeated. The bug was not the reset; it was asking the question in the wrong place. Franchise selection now leads straight into a mode modal — **Single Season** or **Cup Run Challenge** — and the offseason is gated on the answer (`modeChosen`), so it resolves once, for the mode the user actually wants. Choosing Cup Run starts the run at that moment, so the offseason about to run *is* year one rather than something thrown away when the run begins. `restoreEntryBaseline` clears the choice too: a new run replays the offseason, so the question has to return with it or the second run silently inherits the first one's answer. Mode copy and length live in `app/lib/game-mode.ts` so the modal, the HUD and any later summary describe the same thing. A canary pinned the body-scroll-lock argument list verbatim and failed on the new modal; rewrote it to assert every overlay flag appears in the call, so adding a modal is still required but no longer breaks the test for the wrong reason. 6 tests. Touched `app/lib/game-mode.ts` (new), `app/armchair-gm/ModeSelectModal.tsx` (new), `app/armchair-gm/page.tsx`, `app/armchair-gm/useOffseasonFlow.ts`, `__tests__/offseason-phases.test.ts`, `__tests__/feature-canaries.test.ts`.

- 2026-07-28: **OFF5 — contract extensions, human and AI.** Every contract ran to expiry, so the only route to keeping a player was the free-agency scramble; the move a front office makes most often — locking someone up a year early — simply did not exist. `app/lib/extensions.ts` (pure): eligible = the **final year** of a deal, once only, picks and already-expired contracts excluded. Terms come from the same market model as free agency, aged a year forward and discounted 5%, because signing early trades upside for certainty and the player accepts without hearing 31 other offers; a restricted player is already discounted by the projection's own team-control factor, so only the early-signing discount stacks. **The defining property is that an extension costs nothing this season** — it begins when the current deal ends — which means a cap-space readout cannot see it at all. That is what makes it dangerous and why it needed the OFF3 work first: `buildCapHorizon` now charges `pendingExtension` in the seasons it covers, so extending a winger at $9M × 6 visibly fills the season your defenceman's own extension falls due. AI clubs extend their best expiring players in priority order while **next** season's books can carry the raise — judged against next year rather than this one, since a club flush today would otherwise extend its way into a wall it cannot see (there is a test for exactly that). One ordering detail that would have silently broken it: the rollover activates a matured extension *before* the expiry flagging runs, or the contract it was meant to replace marks the player a pending free agent and he reaches the market despite having signed. UI is an Extensions panel sitting directly above the cap horizon on the re-sign screen, so the consequence is beneath the decision. 24 tests. Touched `app/lib/extensions.ts` (new), `app/lib/trade-types.ts`, `app/lib/cap-horizon.ts`, `app/lib/cup-run.ts`, `app/lib/free-agency.ts`, `app/components/ResignPhase.tsx`, `app/armchair-gm/useOffseasonFlow.ts`, `app/armchair-gm/page.tsx`, `__tests__/extensions.test.ts` (new).

- 2026-07-28: **OFF4 — RFA offer-sheet rows get the market's analytics.** The offer-sheet screen showed a name, position, age and asking price. Nothing else. So the screen where you commit **picks and cap** to a player — a decision that can also be matched and leave you with neither — showed less about him than the UFA screen immediately before it. Fixed by extraction, not by copying: `StatLine`, `ExpandedStats` (X-NAV split, production, impact, goalie panel, STRAND, development profile, Gravity), `PlayerMeta`, `Terms` and a new `AnalyticsDisclosure` moved out of `ResignPhase` into `app/components/OffseasonPlayerAnalytics.tsx`, with both screens rendering the same components. "Match the FA analytics" is only a guarantee if there is one implementation; two copies would have drifted by the next change. `AnalyticsDisclosure` is deliberately header-only — name button plus a 44px `aria-hidden` chevron sharing one focus stop — so the caller places `ExpandedStats` full-width at the bottom of the row; my first version rendered the panel inline and nested a full-width grid inside the name column. The offer-sheet screen also needed a `navMap` prop wired from the page, or the panel would render blank, and its last four 9px labels were raised to the 10px floor. The OFF3 canary pinned the chevron's location in `ResignPhase` and was repointed at the shared module, where the touch-target guarantee now covers both screens. 6 tests. Touched `app/components/OffseasonPlayerAnalytics.tsx` (new), `app/components/ResignPhase.tsx`, `app/components/OfferSheetPhase.tsx`, `app/armchair-gm/page.tsx`, `__tests__/offseason-phases.test.ts`, `__tests__/cap-horizon.test.ts`.

- 2026-07-28: **Cold-load part 2 — `/api/league/teams` reads through the same policy.** Applied `swrCache` to the teams route and, more importantly, cached the **whole response** rather than just the inner team list. The old `loadTeams` cache only covered the NHL-standings assembly; even on a warm cache every single request still read the cap ceiling from the database, ran `ensureNewTables()`, selected pick overrides, and rebuilt ~800 pick objects (32 teams × 5 years × 5 rounds). That is the work Team Analytics was waiting on. New key `LEAGUE_TEAMS_PAYLOAD_CACHE_KEY`, registered in `teamCacheKeys` so `clearTeamCaches` drops it — without that an admin cap change would leave a day-old ceiling in the response, which is exactly the kind of silent staleness worth more than the speed. Fresh window 30 min, stale window 24h; a teamless payload is treated as a failed upstream and never cached. Also extracted the Redis adapter to `app/lib/swr-store.ts` so both routes share one implementation of the refresh lock (`SET nx ex`) rather than each defining their own — the duplication would have drifted. 19 tests on the module. Touched `app/lib/swr-store.ts` (new), `app/api/league/teams/route.ts`, `app/api/league/players/route.ts`, `app/lib/team-cache.ts`, `__tests__/swr-cache.test.ts`.

- 2026-07-28: **Cold-load: serve stale instantly instead of blocking on a 40-second rebuild.** An external crawl measured ~20-25s to interactive on the trade machine and ~30s on team analytics. Cause was the league API being cache-or-block: `/api/league/players` held a 15-minute TTL over a roster assembly its own comment describes as `~40s cold` (32 live NHL roster fetches, timeline pulls, MoneyPuck CSV parse, valuation for ~900 players). On a hit it served instantly; on a miss the visitor awaited the entire rebuild. On a site without constant traffic that miss is the *common* path — every 15 minutes the next person to arrive pays 40 seconds. Added `app/lib/swr-cache.ts`: `cacheDecision` (pure policy — fresh / stale / expired / miss) plus `swrCache`, which serves a stale payload immediately and refreshes behind the request under a lock, so ten visitors arriving together on a quiet site don't start ten assemblies. Fresh window stays 15 min; stale window is 24h. Only two cases still block: a completely empty cache (first request after a deploy or flush) and data older than a day, which would be misleading rather than merely stale. The trade-off is deliberate — the upstream feeds move on the order of hours, so the "stale" payload is almost always byte-identical to what a rebuild would produce, and serving it now beats serving the truth in 40 seconds. A failed background rebuild leaves the good payload in place rather than overwriting it, and `isHealthyRoster` still gates writes so a half-empty roster can't be cached for 24 hours. 15 tests. The task-#21 canary pinned `redis.get`/`redis.setex` literally and was rewritten to pin the guarantee instead, plus a new canary asserting the stale path returns without awaiting. Touched `app/lib/swr-cache.ts` (new), `app/api/league/players/route.ts`, `__tests__/swr-cache.test.ts` (new), `__tests__/feature-canaries.test.ts`.

- 2026-07-28: **AI cap realism — position-relative cuts, and the free-agent pool is not a club.** Follow-up to the Year-2 report (LAK/NSH/CGY over the cap, no club above $0.8M space, Makar/Faulk/Carlson stranded). Two further fixes, one of which corrected my own first attempt. (1) **The cut metric could not protect stars, and no amount of position-awareness fixed that.** Value-per-dollar structurally nominates the most expensive player, because he costs the most — which is precisely why the original points-per-dollar ranking waived elite D. Replaced it with `cutEfficiency`: contribution *lost* per cap dollar *saved*, measured against a **per-position replacement baseline** (`replacementScore` — the cheapest third of that position group on that roster). That is where the position-awareness genuinely lives. I briefly layered a within-position percentile on top and a test caught it making things worse: eight near-replacement forwards dragged the forward scale until a $1.2M third-pair D outranked a $6.5M albatross as "worst in his group". Removed. Also raised the cuttable floor to $2.0M — a near-replacement body has near-perfect cut efficiency (he costs nothing to lose) but frees $0.2M, so without a floor the plan spends its cuts achieving nothing. Stars are ultimately protected by the explicit untouchable core, not by the metric; that is the honest mechanism and it is now documented as such. (2) **`FA_POOL` was rendering as a ghost team (OFF6).** The needs panel showed "Cale Makar · D · FA_POOL · 28y · $9.0M" — an internal id shown as a club, and his *expired* cap hit shown as his price. Added `app/lib/fa-pool.ts` (`FA_POOL_TEAM_ID`, `isFreeAgent`, `teamLabelFor`); free agents now read "Free Agent" and are priced at `projectFreeAgentContract(...)` (~$14.5M for Makar, not $9.0M), and `attainability` treats an unsigned player as **Available / signable now** at 0.95 rather than "Unknown team / Possible" at 0.40 — a free agent is the most attainable target there is, since nobody has to agree. 24 tests. Touched `app/lib/ai-cap.ts`, `app/lib/fa-pool.ts` (new), `app/lib/need-targets.ts`, `app/components/WhatWeNeed.tsx`, `__tests__/ai-cap.test.ts`.

- 2026-07-28: **Cap Horizon extended to the Trade Machine.** Same missing future tense as the offseason: `/trade-machine` reported Current Cap / Projected Cap / Cap Delta, all single-season, so a one-year rental and five years of term produced *identical* numbers. Added `withProjectedTrade` to `app/lib/cap-horizon.ts` — outgoing contracts leave the books entirely, incoming ones arrive with their remaining term and any retention the sending club kept, then the horizon is rebuilt from the post-trade roster. Both clubs' horizons now render side by side under the balance strip, labelled as a projection while a package is on the bench. The partner's blocks are mirrored (`incoming: outgoing, outgoing: incoming`), since what one side sends is what the other receives — a test pins that, because reversing it would show both clubs absorbing the same contracts and look plausible. `CapHorizon` gained an optional `title` so it can name the club. The engine test that matters: two incoming contracts at the same AAV, one with a year left and one with three, assert an identical committed figure this season and divergent space in the next two. 29 tests total on the module. Touched `app/lib/cap-horizon.ts`, `app/components/CapHorizon.tsx`, `app/components/QuickTradeMachine.tsx`, `__tests__/cap-horizon.test.ts`.

- 2026-07-28: **OFF3 — the offseason gets a future tense (cap horizon).** Two mechanical fixes and one substantive one. The advanced-stat expander chevron was a ~10px glyph with `padding: 0` — nowhere near the 44px touch minimum — and now carries `tap-target` plus horizontal padding; the expanded panel already had `role="region"`/`aria-label`, and its text reached the 10px floor under OFF2. The real item was the third: **every offseason decision was judged against a single number, this season's cap space.** Signing a $12M winger read as "fits" even when it guaranteed you could not extend your own top-pair defenceman two summers later — the consequence existed, nothing surfaced it. Added `app/lib/cap-horizon.ts` (pure) and `app/components/CapHorizon.tsx`: a roster-by-contract-year table in the shape cap sites already use — one row per player, one column per season, the AAV in each year the deal covers, and the last contracted year marked with the rights (RFA/UFA) you'd be negotiating against that summer — plus committed / ceiling / space per season and a **Decisions Ahead** block naming who expires after each. That last part matters as much as the money: space in 2028-29 means nothing if the player it has to re-sign is the one expiring that summer. `withProjectedSigning` and `firstSeasonOverCap` exist for what-if overlays and point-of-decision warnings. Ceilings come from `projectedCapCeiling` so the escalator lives in season-config only. One implementation note: the summary rows sum the **unrounded** cap hits and round once — summing per-contract rounded values compounds error across a 20-man roster — while space derives from the rounded total so the column adds up as displayed. 20 tests, including the literal case the user described (Kucherov's six years eating the season Morrissey's extension falls due). Touched `app/lib/cap-horizon.ts` (new), `app/components/CapHorizon.tsx` (new), `app/components/ResignPhase.tsx`, `__tests__/cap-horizon.test.ts` (new).

- 2026-07-28: **OFF2 — Re-Sign screen AA pass + RFA-before-UFA ordering.** Accessibility: 24 sub-floor labels (8px ×8, 9px ×16) raised to the 10px minimum; `outline-none` removed from both search inputs, where it was suppressing the global `:focus-visible` ring and leaving keyboard users with no indicator at all; both inputs given real `aria-label`s, since a placeholder vanishes on typing and is not a label; cap space made an `aria-live="polite"` region because it is the number every decision on the screen is judged against and it changes under the user after each signing; the two section labels promoted to `<h3>` and the pending list to `<ul role="list">`. Dialog semantics and the global focus rule were already right, so the fixes are narrower than the audit item implies. **Ordering (user-requested):** pending free agents now show RFAs first, then UFAs, via `sortPendingByRights` in `app/lib/free-agency.ts` — that is the real offseason calendar (qualify and re-sign restricted players, then the UFA market opens) and it also matches consequence: walking an RFA surrenders team control, where a UFA was leaving regardless. Within each group the largest cap commitments lead, since those determine the room left for everything under them; ties break on name so the order can't jitter between renders. Each row now carries an RFA/UFA chip in words rather than relying on colour, and a heavier top rule separates the groups without needing a second heading. 12 tests, including an AA floor check that scans every `text-[Npx]` occurrence in the file so a new sub-10px label fails loudly. Touched `app/lib/free-agency.ts`, `app/components/ResignPhase.tsx`, `__tests__/resign-phase-order.test.ts` (new).

- 2026-07-28: **OFF1 — the offseason CTA names where you actually go.** The offer-sheet screen ended with `Done — Proceed to Free Agency →`, but the real order is Draft Night → **Re-Sign** → RFA Offer Sheets → season, and free agency is the open-market panel *inside* the re-sign phase. So the last button promised a screen that had already happened two phases earlier; `finishOffseason` just closes the offseason and hands over to the trade bench. Now reads `Done — Start Armchair GM →`. Fixed structurally rather than as a string edit: four phase screens (DraftNight, CupRunDraftSummaryModal, ResignPhase, OfferSheetPhase) each hardcoded their own description of the next step, with nothing tying a label to the transition its handler performs — exactly the seam that produced ST1–ST3. All four now read from `OFFSEASON_FLOW` in `app/lib/offseason-phases.ts`, which pairs each phase's CTA with its real destination in one table. 10 tests: each screen is asserted to use the shared lookup, each label must name the phase it truly leads to, and following `next` from any phase must terminate at `SEASON`. Touched `app/lib/offseason-phases.ts` (new), `app/components/OfferSheetPhase.tsx`, `app/components/ResignPhase.tsx`, `app/components/DraftNight.tsx`, `app/armchair-gm/CupRunDraftSummaryModal.tsx`, `__tests__/offseason-phases.test.ts` (new).

- 2026-07-28: **ST3 — alternate positions persist into Lineups (§1 complete).** The eligibility engine was already right from AG3: `isC`/`isW`/`isF` in `app/lib/lineup-order.ts` check primary *and* secondary position, are shared by `LineupEditor` and the default ordering so they cannot drift, and are deliberately forward-flex only (honouring a cross-group secondary would place one id in two group orders and double-deploy him in the sim). What was broken sat one layer below it: `SECONDARY_POSITIONS[p.name]` was a raw exact-string lookup, while every other name match in `roster-assembly.ts` goes through `canonicalNameSlug` — that file even carries a `hasDiacritics` helper. So any mapped player whose feed name carries accents silently resolved to `null`: **Teuvo Teräväinen** and **Tomáš Hertl** are spelled without accents in the table, and Teräväinen is the exact player `lineup-order.ts` names as the reason the feature exists. Replaced the index with `secondaryPositionFor()` over a slug-keyed map (accent-, case- and punctuation-insensitive). Then added the coverage the "persist" claim never had, since ST1 and ST2 both turned out to be fields dropped by reconstruction rather than broken logic: `secondaryPosition` survives `advanceSeason` across three consecutive rollovers, and a flexed centre stays in his saved wing slot when the roster changes under `hydrateLineupOrdersForRoster`. One test asserts every curated row resolves, so adding an accented name to the table later cannot silently rot. 9 unit tests + 2 canaries. Touched `app/data/secondary-positions.ts`, `app/lib/roster-assembly.ts`, `__tests__/secondary-position-persistence.test.ts` (new), `__tests__/feature-canaries.test.ts`.

- 2026-07-28: **ST2 — spent draft picks leave every selector.** An off-by-one in the prune boundary. Rolling into Cup Run year N *holds* the draft for `SEASON.draftYear + N - 1` (Year 2 → 2027), but the rollover kept picks with `year >= currentDraftYear` — preserving exactly the picks it had just converted into rookies, so a traded 2027 first stayed tradeable after it had already conveyed and Year 2 could still offer 2027 picks. The rule was also written twice with two different comparisons: the rollover's `>=`, and Draft Night's `p.year === SEASON.draftYear`, which is a silent no-op because the pick inventory starts at `firstTradablePickYear` (2027) — the 2026 draft having already happened in reality. Both call sites now route through `dropSpentDraftPicks` / `draftYearForCupYear` in `app/lib/draft-picks.ts`, where the boundary is inclusive of the draft just held and a pick with no year is kept rather than silently deleted. Pruning at the source is what makes "leave *every* asset selector" true — they all read `db.players`, so no per-selector filtering was needed. Pick ownership already carried through the rollover correctly (`players: [...nextPlayers, ...picks]`, with first-round rookies mapped to the pick's current owner), so removing the spent year is the whole fix. 8 unit tests + 3 canaries. One canary initially failed against the new lib's own explanation of the old bug; rewrote it to assert the actual expression rather than the absence of a substring, which is the more durable form. Touched `app/lib/draft-picks.ts` (new), `app/armchair-gm/useCupRunLifecycle.ts`, `app/armchair-gm/page.tsx`, `__tests__/draft-picks.test.ts` (new), `__tests__/feature-canaries.test.ts`.

- 2026-07-28: **ST1 — a new Cup Run restarts on the league as first loaded.** `originalDb` was doing two incompatible jobs: "the league at the start of the CURRENT season" (overwritten by every Cup Run rollover) and "the league to reset to". Once a run advanced a year, no pristine 2026-27 league survived anywhere, so starting a second run began on a league a previous run had already aged — retirements applied, rookies drafted, cap ceiling stepped up — while the run state read Year 1. That is the audit's "the old sim continues while only the draft resets". Added an immutable entry baseline: `entryBaselineRef`, captured once at load through `cloneLeague` (`app/lib/league-baseline.ts`, entity-level copies so a later in-place field assignment on a live player can't rewrite history), never written again. Starting **or** abandoning a run restores it — the abandon case is the CX5 follow-up deferred earlier. Also merged the two parallel reset lists (season rollover vs new run) into one shared `resetSeasonState`, since keeping two in sync by hand is how the drift started; it now additionally clears `matchResults` and a stale `cupDraftSummary`, which could otherwise pop a previous run's draft-summary modal over a fresh run. Fixed a regression I introduced with the earlier CX5 patch: `onStart` called `resetTrades()`, which sets `showTeamSelect(true)` and reopened the franchise picker over the team the user had just pressed Start for. The CX5 canary pinned that old `onStart` body and was updated to pin the stronger behaviour rather than the behaviour it happened to ship with. 5 unit tests + 5 canaries. Touched `app/lib/league-baseline.ts` (new), `app/armchair-gm/page.tsx`, `__tests__/league-baseline.test.ts` (new), `__tests__/feature-canaries.test.ts`.

- 2026-07-27: **Gravity v4 stage 1 COMPLETE — 319,806 verified stint rows.** The full 2025-26 slate passes all nine gates. 807 of 1312 games reconstructed (the other 505 have no shift chart at source), 396 rows per game, zero tiling gap, 100.00% roster join, 93,947 shots all carrying a shooter id, 99.67% of play-by-play events landing in a stint, 8.7 MB gzipped. Two results matter beyond this stage. **Team coverage spread is 6.1 points** (median 62.2%, BOS 58.5% to MTL 64.6%) — the missing 38% is nine contiguous calendar windows, so every club loses a similar share and the gap costs precision rather than validity; had it been concentrated on a few teams, every player effect fitted from it would have been biased by club. **Event attribution measured 99.16% (trailing) against 95.34% (leading)** — 9,686 events the naive containment rule would have credited to the wrong five skaters, concentrated on goals and special-teams transitions. Also fixed a denominator impurity found while reading the final output: foreign-game rows were counted in `shiftRows`, putting them in the roster-join denominator and flattering the very metric whose job is to notice rows that do not belong; the team check now runs before the count. Stage 1 is a clean stopping point — the dataset is reproducible from the manifest (schema version, settings, covered/uncovered game ids, per-team coverage, sha256 of the uncompressed stream). Next stage, `build-possession-states`, is blocked on an xG decision: train one on these shots, or join MoneyPuck shot-level values. Touched `scripts/gravity-v4/core.ts`, `scripts/gravity-v4/build-stints.ts`, `scripts/gravity-v4/README.md`, `__tests__/gravity-v4-stints.test.ts`.

- 2026-07-27: **Gravity v4 — a shift chart containing another game's rows.** The full-slate run's roster-join gate failure traced to exactly one game: 2025020565 (BUF @ NJD), 2166 shift rows where a normal game has ~770, 30.8% unresolvable. `--inspect` (new, reads cache only) showed why — the NHL's shift chart for that game carries **667 rows from a Vegas–San Jose game** (Eichel, Stone, Pietrangelo / Granlund, Toffoli, Ceci) plus a duplicated copy of the real game's rows, which the exact-duplicate dedupe had already absorbed. The severity is not the failed gate: `buildStints` assigns `isHome = teamId === homeTeamId` and treats **everything else as the away team**, so those skaters would have been placed on New Jersey's ice and the lineups would have been silently wrong. Nothing checked that a shift row belonged to the game it was requested for; the roster join caught it only incidentally and misreported it as a join miss. `parseShifts` now takes the two playing team ids and counts foreign rows as their own category, checked *before* the roster join so that rate stays honest, with contamination reported prominently and recorded in the manifest. Four tests, including one that pins the corruption the filter prevents (without it the away side reconstructs as 9v5). Touched `scripts/gravity-v4/core.ts`, `scripts/gravity-v4/build-stints.ts`, `scripts/gravity-v4/coverage-spike.ts`, `scripts/gravity-v4/probe.ts`, `scripts/gravity-v4/README.md`, `__tests__/gravity-v4-stints.test.ts`.

- 2026-07-27: **Gravity v4 — absence handling: escape the retry loop, negatively cache it.** The "cut empty-payload retries to 3 attempts" fix never took effect. The `throw` ending that path sat *inside* the fetcher's own `try`, so its outer `catch` swallowed it and resumed the full 6-attempt transport ladder — 4s + 8s + 16s of backoff per sourceless game, ~35s each, ~5 hours across 501 games. That is why the run looked frozen at the cached/uncached boundary both times. Introduced `PayloadAbsentError`, rethrown immediately by the catch, so an absence exits as a conclusion rather than being retried as a transport failure; `build-stints` now discriminates on the type instead of regex-matching an error message. Two related fixes: the host cooldown no longer ratchets on empty payloads (they aren't throttling, so slowing every other request helps nothing), and absences are **negatively cached** as `absent-<key>.json` markers — without that, every rerun re-requests all 501 games forever. `--refresh-absent` re-requests them if the NHL ever backfills. Offline, an empty cached payload now also reports as an absence rather than a hard failure, so cached and live runs classify games identically. Touched `scripts/gravity-v4/nhl-source.ts`, `scripts/gravity-v4/build-stints.ts`, `scripts/gravity-v4/probe.ts`.

- 2026-07-27: **Gravity v4 — the NHL has no shift charts for 38% of the season; coverage gates replace completeness gates.** My throttling diagnosis was wrong. A live probe with the cache bypassed showed `shiftcharts` answering `HTTP 200` with `{"data":[],"total":0}` — 21 bytes in <100ms — for 501 of 1312 games, while the same games' play-by-play returns normally (307 plays, 40 rosterSpots). Re-probed empty vs populated side by side: empties still empty, populated still return 750–856 rows. It is an absence at the source, not rate limiting, and no amount of pacing fixes it. The gaps are **nine contiguous blocks of game ids** (median 50, largest 111); ids run in schedule order, so those are date ranges — roughly nine one-to-two-week windows. That is the benign shape, since every club plays across every window, but it is now measured instead of assumed. Changes: play-by-play is fetched **first** so a game with no shift chart can still be attributed to the two teams that played it (without that, missing games are invisible to the coverage check and bias cannot be measured); a missing shift chart is a recorded expected outcome, not a failure; the "games emitted ≥95%" gate now measures against *reconstructable* games; and two new gates ask the right question — **every team ≥30 covered games** and **no team below 70% of the median team's coverage**. The manifest records `coveredGameIds`, `noShiftChartGameIds` and per-team coverage, so stage 2 reads the real sampling frame rather than assuming a full season. Also added `scripts/gravity-v4/probe.ts` (cache audit + gap-shape mapping + live `--compare`), because probing only the failures cannot distinguish "no data" from "endpoint down right now". Touched `scripts/gravity-v4/build-stints.ts`, `scripts/gravity-v4/probe.ts` (new), `scripts/gravity-v4/nhl-source.ts`, `scripts/gravity-v4/README.md`.

- 2026-07-27: **Gravity v4 emitter — empty-200 detection and a gate that was lying.** The first full-slate run (1312 games) reported `games emitted 1312/1312` while producing only 319,806 rows — roughly 800 games' worth. Under sustained load the NHL endpoints don't answer 429, they answer **HTTP 200 with an empty body**; those cached as if real, so ~500 games contributed nothing and the run still looked complete. Two separate defects. (1) *Caching:* payloads are now content-validated (shift charts must have rows; play-by-play must have plays, rosterSpots and team ids) — an invalid payload is backed off and retried like a 429 and **never cached**, and a *cached* payload that fails validation is deleted and refetched, so a cache poisoned by an earlier throttled run heals itself on the next online run rather than producing an empty dataset forever. `api.nhle.com` pacing also went 250ms → 400ms (`--shiftgap`). (2) *The gate:* "games emitted" counted "no exception thrown", so a game that reconstructed to zero stints passed. It now counts games that produced stints, a zero-stint game throws, and a new independent gate requires ≥97% of play-by-play events to land in a stint — whole missing games show up there immediately regardless of how the game count is defined. Also added roster-join offender reporting (the aggregate percentage was hiding which games lost rows) and failure grouping by cause. The one gate that did fire on the bad run was roster join ≥99.9%, which is the only reason it wasn't fitted on. Touched `scripts/gravity-v4/nhl-source.ts`, `scripts/gravity-v4/build-stints.ts`, `scripts/gravity-v4/coverage-spike.ts`, `scripts/gravity-v4/README.md`, `__tests__/gravity-v4-stints.test.ts`.

- 2026-07-27: **Gravity v4 stage 1 producer — the stint emitter.** The coverage spike only wrote *metrics*, so passing its gates left us with no fittable data. `scripts/gravity-v4/build-stints.ts` now emits the dataset: one row per constant-lineup stint carrying everything §5 of the spec says the OZ/NZ/DZ fits condition on — both lineups + goalies, strength, score at stint start, zone start (flipped into home terms from the play-by-play's owner-relative `zoneCode`, so rink orientation is never assumed), duration, and the on-ice shot events **with shooter ids**, which is what makes "exclude the focal player's own offense" possible at all. Shots carry coordinates and type but no xG value; attaching expected-goal weight is the next stage's job so the rows stay valid when the xG source changes. Output is gzipped NDJSON + a manifest carrying the schema version, settings, coverage and the sha256 of the *uncompressed* stream (verifiable determinism without depending on gzip framing); the same five reconstruction gates run on the emitted set and the script exits non-zero when one fails, so a bad dataset can't quietly feed stage 2. **The substantive design call is event attribution.** An event that *causes* a stoppage was played by the lineup on the ice up to that second, so it belongs to the stint ending there — `(start, end]`; an event that *resumes* play (faceoff, period start) belongs to the lineup taking the ice — `[start, end)`. Using one rule for both credits a goal to the five who came over the boards after it. That isn't asserted: every run prints situationCode agreement under *both* rules over the same events, and a test pins the case where they diverge. Also: a blocked shot is owned by the **blocking** team in the NHL feed, so the attempt is credited to the other side. Fetch/cache/pacing moved to a shared `nhl-source.ts` so the spike and the emitter cannot drift apart. 19 new tests (38 in the file). Touched `scripts/gravity-v4/core.ts`, `scripts/gravity-v4/build-stints.ts` (new), `scripts/gravity-v4/nhl-source.ts` (new), `scripts/gravity-v4/coverage-spike.ts`, `scripts/gravity-v4/README.md`, `__tests__/gravity-v4-stints.test.ts`.

- 2026-07-27: **Gravity v4 stage 1 (`build-stints`) passes its coverage gates.** The stint reconstruction spike ran clean over 50 games of 2025-26: 50/50 games rebuilt, 38,569 shift rows kept (100.00% roster join by NHL id), 20,073 stints, **zero tiling gap**, 9 impossible skater counts (0.045%), 5v5 = 75.7% of stint time, and strength agreement against the game's own `situationCode` at 96.49% strict / **99.75% boundary-tolerant**. All five gates PASS, so the reconstruction is trustworthy enough to fit on. The interesting finding is *what* the residual disagreements are: 98.1% sit exactly on a stint boundary, and the event mix is **penalty 66.9% / goal 15.6% / stoppage 12.2%** — i.e. strength *transitions*, not reconstruction error. At the instant a penalty is called both teams are still even strength (play-by-play stamps 5v5) but the shift chart has already ended the penalised player's shift on that second, so the rebuild reads 4v5; a power-play goal is the mirror image. Both sources are right about opposite sides of the same transition, which is why the gate is boundary-tolerant — and why the tolerance is pinned narrow by test (a mismatch away from a boundary still fails). Residual to revisit only if the fitted model misbehaves on special teams: 11 disagreements (0.066%) were not on a boundary and 41 (0.25%) were unresolved even allowing either adjacent lineup. Next step is the league-wide backfill. Touched `scripts/gravity-v4/README.md`, `scripts/gravity-v4/core.ts`, `scripts/gravity-v4/coverage-spike.ts`, `__tests__/gravity-v4-stints.test.ts`.

- 2026-07-27: **Player Gravity Release A baseline identity parity.** Replaced raw-directory discovery with a fingerprinted 12-source manifest, generated and consumed runtime baselines by stable NHL player ID (including same-name players), preserved existing out-of-scope enrichments, and added regression coverage without changing tier thresholds or activating v4. Touched `.codexignore`, `scripts/moneypuck-baseline-sources.json`, `scripts/process-moneypuck-baselines.ts`, `app/data/moneypuck_baselines.json`, `app/lib/player-baselines.ts`, `app/lib/roster-assembly.ts`, `__tests__/player-baseline-identity.test.ts`, `docs/PLAYER_GRAVITY_V4_IMPLEMENTATION_SPEC.md`, and `docs/DEVNOTES.md`.

- 2026-07-24: **Player Gravity Release A frozen calibration population.** Added deterministic, cached 2025-26 NHL/MoneyPuck/EDGE/OtherData aggregate acquisition, stable-ID qualification and exact crosswalk handling, machine-readable provenance/coverage evidence, and behavioral population tests without changing tiers or runtime behavior. Touched `.gitignore`, `scripts/gravity-calibration/`, `data/gravity-calibration/2025-26/manifest.json`, `docs/analytics/GRAVITY_V3_RELEASE_A_POPULATION.md`, and `__tests__/gravity-calibration-population.test.ts`.

- 2026-07-24: **Player Gravity Release A public contract.** Corrected public terminology and mixed-situation provenance across the live field, glossary/methodology, card payload, and exported image; added strict v3 runtime validation that rejects caller-supplied v4 Gravity values plus behavioral contract tests. Touched `ANALYTICS.md`, `gravity.md`, `app/lib/gravity.ts`, `app/lib/gravity-rink.ts`, `app/lib/card-payload.ts`, `app/api/card-image/route.tsx`, Gravity public components/pages, and `__tests__/gravity-public-contract.test.ts`.

- 2026-07-24: **Player Gravity Release A core.** Made the v3 X-NAV residual transition-only at the existing ×45 conversion with a ±20 cap, removed QoC/TOI rate inflation, added `signalStability`/`reliability` compatibility aliases and explicit coverage, and added regression tests; Gravity v4 remains inactive and tier thresholds are unchanged. Touched `app/lib/gravity.ts`, `app/lib/xnav-engine.ts`, `__tests__/gravity.test.ts`, and `__tests__/xnav.test.ts`.

# JULY 24
# ARMCHAIR_AUDIT + CODEX_SIM_AUDIT
- 2026-07-24: **CX1–CX4 — Armchair state integrity.** Four trade-bench / shared-link fixes. CX4: `executeTrade`'s `setDb` returned only `{players, teams}`, dropping `capCeiling` + metadata after the first trade (later NAV/audits reverted to the static cap) and cleared only the sim narrative, not `simData` — now `{ ...prev, ... }` + `resetSimulation()`. CX2: `resolveTradeShareAssets` resolved refs globally and fabricated phantom picks for missing ids — a crafted URL could move a third team's asset or a nonexistent one; added an optional `expectedTeamId` so the execute path only resolves assets that exist AND belong to the expected team (preview keeps its placeholder). CX1: the state→URL sync fired on the default-partner boot and `replaceState`d over a shared query before it was read — a `urlHydratedRef` gate blocks the sync until the cold-load parse runs. CX3: a package change cleared the verdict but didn't abort the in-flight audit/memo/match, and match cleared only on outgoing changes — the `tradeInputKey` effect (home + partner + both blocks) now aborts all three + clears the match list, and `runEval`/the Claude memo carry stale-response guards. Regression for CX2 ownership; canaries pin all four. Touched `app/lib/trade-share.ts`, `app/armchair-gm/useTradeBench.ts`, `app/armchair-gm/page.tsx`.
- 2026-07-24: **CX5 (partial) — Cup Run: reconcile the user's cap on rollover + start runs clean.** Two lifecycle bugs that hit every run. (1) The user's cap was frozen across years — `reconcileAiTeamCapSpaces` explicitly skipped the user team, so their cap never got the ceiling growth or their rolled roster's commitment changes. Renamed to `reconcileTeamCapSpaces` and now recomputes every team including the user: new ceiling − committed − the user's still-active retained-salary obligations (paid on players they moved, which `committedCap` can't see). (2) Dirty start — starting a Cup Run didn't clear pre-run trades or a pre-run sim, so retained salary evaded the Cup retention ledger and a pre-run sim could count as a Cup season; `onStart` now resets trades + simulation first. Remaining CX5: an immutable pre-run baseline so abandoning after a rollover restores the true Year-1 league (needs an entry-baseline snapshot). Touched `app/lib/cup-run.ts`, `app/armchair-gm/useCupRunLifecycle.ts`, `app/armchair-gm/page.tsx`.
- 2026-07-24: **CX6 (partial) — RFA offer-sheet compensation conveys picks + fixes original-team cap.** The offer-sheet handler paid compensation with "any owned pick of the right round" (including picks acquired in trades, ignoring draft year), DELETED those picks from the league instead of conveying them to the club that lost the RFA, and double-counted the original club's cap (freed the new AAV but also added the already-expired old cap hit back). Added `resolveOfferSheetCompensation` (pure): the signing team's OWN picks only (current + original owner, parsed from the `pick-{origTeam}-{year}-{round}` id), soonest year first, one per required round, with a shortfall report. `signOfferSheet` now conveys those picks to the original club (teamId change, not deletion) and frees only the RFA's current AAV. Remaining CX6: original-team MATCH right (`resolveOfferSheet` exists but unwired) + the user's-own-walked-RFA market button. Touched `app/lib/free-agency.ts`, `app/armchair-gm/useOffseasonFlow.ts`.
- 2026-07-24: **AI3 / CXH3 — walked free agents relocate to the FA pool, not deleted.** The offseason filtered walked players out of `db.players` entirely while the NAV effect rebuilds `navMap` from `db.players` only, so open-market players displayed/sorted as NAV 0 and vanished from the sim by Year 3. Extracted `applyOffseasonToRoster` (pure): re-signings take their new deal, AI market signings move teams, and players who walked to the open market are relocated to `FA_POOL` instead of deleted — keeping a NAV and staying signable. `signMarketPlayer` already filters by id before re-adding, so no duplicate on a later signing. Touched `app/lib/free-agency.ts`, `app/armchair-gm/useOffseasonFlow.ts`.
- 2026-07-24: **AI1 / AI2 — AI teams always retain their RFAs (cap reserved first).** `resolveLeagueOffseason` let an AI RFA walk whenever its market raise didn't fit tracked cap, and the market-signing pass only re-signs UFAs, so a walked RFA (Celebrini) was never picked up again — and the freed cap could go to an external UFA (Kucherov at Celebrini's expense). RFAs carry team control, so they are now always re-signed (as the existing "always re-signs RFAs" test intended); booking the raise reserves that cap ahead of the UFA-market pass. Shared resolver, so it holds for single seasons and Cup Run rollovers. Touched `app/lib/free-agency.ts`.
- 2026-07-24: **VAL2 — Duehr box-score consistency + thin-sample role gate.** The card rounded G, A, and PTS each independently from its own per-82 pace, so a 3-GP sample showed "0 G · 0 A · 1 PTS". Extracted `boxScoreFromPace` (app/lib/box-score.ts): round goals and assists, then PTS = G + A, always self-consistent. Separately, a 3-GP call-up was labelled "2ND LINE WINGER" off a noisy pace — a forward under `MIN_ROLE_SAMPLE_GAMES` (15) now reads UNPROVEN; franchise/elite reads fire earlier so injured stars keep status. The third sub-item (~+21 NAV) is the `REPLACEMENT_NAV` thin-sample anchor in the shared xnav core, deferred by choice (VAL3 already separated Duehr from Barkov). Touched `app/lib/box-score.ts` (new), `app/components/AssetCard.tsx`, `app/components/AssetBadges.tsx`.
- 2026-07-24: **Codex #8 — PlayerComparison metric truthfulness.** "Avg TOI" actually summed TOI; TOI and age are now averaged per skater (Pts/xG stay summed). Bar geometry scaled by `Math.abs(value)`, so a worse, more-negative NAV drew the longer bar — now anchored at 0 so a more-negative higher-is-better value is shorter. An empty (picks-only) side averaged to 0 and won lower-is-better stats (age/cap) — averages return null for a skaterless side and a missing side shows "—", draws no bar, never wins. Bar/winner math extracted to `app/lib/stat-bar-compare.ts` (pure, tested). Touched `app/lib/stat-bar-compare.ts` (new), `app/components/PlayerComparison.tsx`.
- 2026-07-24: **Codex #4 — validate + bound the simulate request.** /api/simulate cast req.json() straight to its type (no shape/bounds/finite/unique checks — crashes + soft DoS). Added `app/lib/sim-request-schema.ts`: Zod `safeParse` → 400, permissive on fields (players/teams `.passthrough()` so ~30 engine inputs survive) but strict on structure — required string ids, array caps, finite seed, unique team/player ids, resolvable team references. Exact 32-team canonical check left to the future server-side-league refactor. Touched `app/lib/sim-request-schema.ts` (new), `app/api/simulate/route.ts`.
- 2026-07-24: **Codex #6/#7 — trade UI state bugs.** #6: Quick Trade could stick on "Auditing" forever — a package change aborts the in-flight verdict but the aborted request's finally won't reset `evaluating`, and nothing restarts it; the package-change effect now clears the flag. #7: generated proposals used a fail-open gate ("anything but BLOCKED/DECLINED") that passed IDLE/PENDING/undefined — replaced with an explicit FAIR/WIN/LOSS whitelist (`app/lib/trade-proposal-audit.ts`), and the proposal audit now passes the live `capCeiling`. Touched `app/lib/trade-proposal-audit.ts` (new), `app/components/QuickTradeMachine.tsx`, `app/components/TradeProposal.tsx`, `app/armchair-gm/page.tsx`.
- 2026-07-24: **Codex #2/#3 — SIM RNG determinism.** #2: awards, Calder voting, and playoffs shared one sequential random stream, so adding an award-eligible rookie (more Calder draws) rerolled unrelated playoff series — split into independent named streams (`mulberry32(seed + hashString("awards"|"calder"|"playoffs"))`). #3: Cup Run seasons reused one seed (year + run seed omitted) — `{cupRunSeed, cupRunYear}` now fold into the scenario seed (only inside a run, so single-season seeds are unchanged). Touched `app/api/simulate/route.ts`, `app/armchair-gm/useSimDispatch.ts`, `app/armchair-gm/page.tsx`.
- 2026-07-24: **Codex #1 — retention-aware SIM cap deltas.** The SIM summed raw `capHit` for traded players while the trade UI applies `capHit * (1 - retainedPct)`, so a 50%-retained $10M player read as a $10M swing to the SIM but $5M to the UI (mis-firing the over-cap penalty). Exported the canonical helper as `effectiveCapHit` from cap-delta.ts and used it on both sides of the SIM's per-trade cap delta. Touched `app/lib/cap-delta.ts`, `app/api/simulate/route.ts`.
- 2026-07-24: **VAL1 — drafted rookie keeps its context through the accent dedup.** The drafted "Viggo Björck" carried draftOverall + NHLe pace, but collided with an accent-stripped seeded "Viggo Bjorck"; the old dedup dropped the drafted rookie, leaving the context-less copy to value at 0. `reconcileDraftedRookies` (app/lib/draft-reconcile.ts) backfills the draft context onto the existing roster entry instead of dropping the rookie. Touched `app/lib/draft-reconcile.ts` (new), `app/armchair-gm/page.tsx`.
- 2026-07-24: **VAL3/VAL4 — an injury year no longer erases a star's floor.** A pedigreed elite who missed most of a season showed depressed counting stats; the historical (pedigree) floor read that as decline and collapsed via both the availability/production multiplier and the decline gate, so Barkov fell to a depth scrub's value (tying Duehr at +21). `isInjuryShortenedPrime` (games < 55 && age ≤ peakAge+2) treats a prime-age low-games sample as injury not decline — the floor keeps only its age decay; a player past his peak with the same sample still decays (Karlsson unchanged). Touched `app/lib/player-data.ts`.
- 2026-07-24: **SIM1 — playoff bracket advances by visual adjacency.** The bracket tree stacks four R1 series top-to-bottom and centers each R2 series over the two above it, but the engine paired r1[0] with r1[2] and r1[1] with r1[3], so the third series' winner "jumped" into the top R2 slot (Mammoth + Blackhawks wins → Wild–Blackhawks). Extracted the bracket to `app/lib/playoff-bracket.ts` (pure, testable) and paired R2 by adjacency — winners of rows 0+1 meet, rows 2+3 meet. Touched `app/lib/playoff-bracket.ts` (new), `app/api/simulate/route.ts`.

# JULY 14

# POSTCODEX_AUDIT
- 2026-07-15: **Gravity — zone pull fix: three-way OZ/NZ/DZ split, not binary.** EDGE zone time (`edgeOzPct`) is a three-way split where OZ + NZ + DZ = 100%, with league-average OZ ~43%. The gravity engine was comparing against 50% (a binary OZ-vs-DZ assumption that ignores neutral zone), which turned McDavid's 47.7% OZ (98th percentile) into a *negative* zone pull (-0.12). Fixed `LEAGUE_AVG_OZ_EDGE` to 0.43. Also fixed the same baseline error in the transition control mechanism. The `dzPct` fallback path (MoneyPuck EV faceoff zone starts) correctly uses 0.50 since that IS a binary metric. Touched `app/lib/gravity.ts`.
- 2026-07-15: **Gravity v2.3 — inline SVG tier icons + icon key + orbital physics fixes.** Space-themed tier icons replace Unicode symbols: spiral galaxy (Supermassive), star burst (Star), sun with rays (Main Sequence), orbiting body (Satellite), irregular rock (Asteroid), dark void with accretion ring (Black Hole). `TierIcon` component exported from `GravityField.tsx` for reuse. Footer icon key updated with all 6 gravity tier entries using the inline SVG icons and plain-language descriptions. Orbital physics inverted so stronger gravity orbits closer to center. SVG diagram centered. Grav Ast and tier labels repositioned to fix overlap. Glossary descriptions added for all metrics. Touched `app/components/GravityField.tsx`, `app/components/Footer.tsx`.
- 2026-07-14: **Gravity Engine v2 — five analytical layers + AA accessibility pass.** Complete rewrite of the gravity engine from a derivative stat to a causal, predictive, and actionable metric. Five new layers: (1) **Partner Independence** — multi-season NOIV stability isolates real gravity from borrowed linemate effects; D-pair driver score for defensemen; dampened toward 1.0 for small samples. (2) **Context Adjustment** — corrects raw NOIV for quality of competition, zone-start deployment, and PP inflation (QoC × zone × PP multiplier). (3) **Mechanism Decomposition** — four 0–1 sub-scores explaining WHERE gravity comes from: space creation, transition control, pace manipulation, defensive warping. (4) **Gravity Assist** — invisible creation beyond the scoresheet: fraction of team uplift NOT explained by individual shooting, weighted by assist share. (5) **Predictive Stability** — year-over-year signal confidence dampened when NOIV magnitude is near-zero (prevents trivially-stable flat-zero players from showing 100% confidence). Enhanced force formula: `F = noivLift × (1 + zonePull) × creationAmplifier × playerMass × partnerIndependence + burstBonus + suppressionBonus + gaBonus`. **Visualization v2.1 (AA pass):** removed `maxWidth: 760` so component fills its parent; bumped all text from 7-8px to 10-11px minimum for AA readability; added `role="region"`, `role="list"`, `role="listitem"`, `role="meter"`, `role="group"` ARIA attributes throughout; comprehensive `aria-label` on SVG diagram covering all metrics; mechanism bars have `aria-valuenow/min/max`; CSS grid layout (2-column) replaces flex-wrap for consistent filling; SVG viewBox expanded to 320×310 and "Creation" dot moved to angle 0.65π to prevent label overlap with bottom title; gravity assist and signal confidence boxes in a grid column instead of fixed-width flex. Touched `app/lib/gravity.ts`, `app/components/GravityField.tsx`.
- 2026-07-14: **Gravity system — physics-based gravitational pull metric.** New player evaluation dimension measuring how much a player warps the game around himself (the McDavid/Curry effect — elevating linemates and dragging play into the offensive zone). Rooted in real physics: `F = NOIV_lift × (1 + zonePull) × creationAmplifier × playerMass + burstBonus + suppressionBonus`. Output is a signed decimal (+0.52), not a 0-100 index — the magnitude has concrete interpretation. Three core components: (1) **NOIV Lift** — linemate differential (`xgRelTM` blended 40/60 with `baselineXgRel`, normalized to ±1), (2) **Zone Pull** — offensive zone attraction from EDGE `edgeOzPct` relative to league average 50%, with `dzPct` fallback, (3) **Creation Amplifier** — ratio of team uplift (NOIV) to individual production (OPS), boosted by assist share. Player mass (TOI × production) scales the field so a 4th-liner with fluky splits can't register elite gravity. Burst bonus from EDGE explosiveness, suppression bonus from xGA. Six gravity tiers: SUPERMASSIVE (+0.50+), STAR (+0.35-0.50), MAIN_SEQUENCE (+0.15-0.35), SATELLITE (+0.05-0.15), ASTEROID (±0.05), BLACK_HOLE (<-0.05). Visualization: `GravityField.tsx` renders a scientific orbital field diagram (concentric equipotential rings around a central mass node, three component markers on orbital paths, ring density/count scales with force magnitude, negative fields use dashed red rings). Two modes: full (SVG diagram + formula equation + component breakdown + tier description) and compact (inline force + tier badge). Integrated in three surfaces: (1) Teams page — "Gravity Leaders" section showing top 3 players per team with full diagram for #1 and compact for #2/#3, plus team gravity average and "Gravity" sort option, (2) Players page — new "Gravity" tab in the expanded player panel, (3) Armchair GM — compact gravity readout on every skater's AssetCard stats view. Touched `app/lib/gravity.ts` (new), `app/components/GravityField.tsx` (new), `app/teams/page.tsx`, `app/players/page.tsx`, `app/components/AssetCard.tsx`.
- 2026-07-14: **Teams page — division standings as default view.** Replaced the flat ranked list with a division-grouped default layout. Teams now appear under their division header (Atlantic, Metro, Central, Pacific), grouped by conference (Eastern, Western), with a division rank number (1-8) next to each card. Teams dynamically move position within their division based on live standings data. Added "Division" as the default sort button (first in the row), renamed the old "Rank" to "All Teams" — clicking any other sort flattens back to a single sorted list. Division groups are phase-filterable (clicking a phase chip still filters across all divisions). Touched `app/teams/page.tsx`.
- 2026-07-14: **Teams page polish — division/conference, streak/L10, playoff tags, cap breakdown.** Four additions to the Teams page. (1) **Division & Conference chip** — each team card header now shows a chip like "CEN · WEST" or "ATL · EAST" next to the phase badge, giving geographic context at a glance. Derived from a module-level `DIVISIONS` map (moved from inside the stats API try block). (2) **Recent form & streak** — always fetch `/v1/standings/now` (not just as a fallback) to extract `streakCode`/`streakCount` and `l10Wins`/`l10Losses`/`l10OtLosses`. Displayed as "L10: 7-2-1" and "W3" (green) / "L2" (red) in both the collapsed header and the expanded Season Record section header. OT streaks show in amber. Also added regulation wins to the Record stat cell sub-line. (3) **Playoff position tag** — derived from `divisionRank` (≤3 = "Div #1/2/3") and `conferenceRank` (7-8 = "WC1"/"WC2"). Clinch indicators from the NHL API (`x`=clinched, `y`=division champ, `z`=presidents', `e`=eliminated) override with appropriate labels and colors. Shown as a chip in the header row. (4) **Cap breakdown** — enhanced the CapWages scrape to extract `ltirUsed`/`deadCap`/`totalCapHit`/`bonuses` from the `teamSummary` payload (tolerant of field-name variations). Rendered below the cap bar as color-coded line items: LTIR Relief (amber), Dead Cap (red), Bonuses, Total Cap Hit. Only shown when non-zero. Touched `app/api/league/route.ts`, `app/teams/page.tsx`.
- 2026-07-14: **Homepage layout — 2x2 feature grid + 2x1 In Development.** The "Inside This Edition" grid was `repeat(3, 1fr)` on desktop, so the 4 non-lead feature cells (Trade Machine, Player Analytics, Team Analytics, The Docket) filled 3 + left 1 alone on its row — dead space. Changed to `repeat(2, 1fr)` so the 4 cells form a clean 2x2 grid. Same fix for the "In Development" briefs grid — was `repeat(3, 1fr)` with only 2 cards, now `repeat(2, 1fr)` so both cards fill the row. Lead cells still span full width. Touched `app/globals.css`.

# JULY 13

# POSTCODEX_AUDIT
- 2026-07-13: **Cap bar fix + team record stats + NHL API standings fallback.** Three fixes to the Teams page and league API. (1) **Cap space bar bug** — the bar showed full red for teams with positive cap space (e.g., Winnipeg +$12M displayed as over-cap). Root cause: bar width used `capCommitted` (raw sum of all roster `capHit` values, ~$127M for WPG due to LTIR/buried contracts) divided by `capCeiling`, producing >100%. Now derives the bar from `capCeiling - team.capSpace` (the API's `capSpace` accounts for LTIR/buried/bonus nuances), and reads `capCeiling` from the API response instead of hardcoding $104M. Color keys off `team.capSpace` sign. (2) **Team record stats** — the NHL stats API `team/summary` endpoint was already being fetched for standings but W-L-OTL, GF/GA, PP%, PK%, shots/game, FO%, and regulation wins were all discarded after the fetch. Extended the standings map to capture all fields, passed them through as a `record` object on each team, and surfaced them on the Teams page: new "Season Record" section in expanded cards (6-stat grid: Record+pts, Goal Diff with GF/GA sub, PP%, PK%, SF/Game+SA, FO% — all with color-coded performance thresholds), W-L-OTL in the collapsed header row, and "Goal Diff" sort option. (3) **Standings API fallback** — added `api-web.nhle.com/v1/standings/now` as a secondary standings source in both `/api/league` and `/api/league/teams` routes. Fires only when the primary `api.nhle.com/stats/rest` endpoint returns <28 teams (network failure / API deprecation). Maps `teamAbbrev.default`, `conferenceSequence`, `divisionSequence`, `pointPctg`, `goalFor/goalAgainst`, `powerPlayPctg`, `penaltyKillPctg`, `faceoffWinPctg` from the newer web API schema. Logged as a recovery event. Touched `app/api/league/route.ts`, `app/api/league/teams/route.ts`, `app/teams/page.tsx`.
- 2026-07-13: **Secondary positions across the entire application.** Added `secondaryPosition` to the `Asset` type and wired it through the full data pipeline. Vilardi (WPG) is listed as C by the NHL API but played RW on the top line with Connor-Scheifele — without secondary positions the lineup builder incorrectly slotted him as 2C. Created `app/data/secondary-positions.ts` (curated map of ~20 players: Vilardi→W, Point→W, Bennett→W, Henrique→W, Hertl→W, Lehkonen→C, Teravainen→C, etc.), `app/lib/display-position.ts` (`displayPosition()` returns "C/W" when secondary exists), and populated via `roster-assembly.ts` at the single point where player objects are constructed. The Teams page `buildTeamLines` now separates pure centers from flex centers (those with `secondaryPosition: "W"`), slotting flex centers as wingers when enough true centers are available. Updated 15 display components with `displayPosition()`: AssetCard (5 spots), AssetDropdown, PlayerComparison, TradeProposal (2 spots), QuickTradeMachine (2 spots), LineupEditor (2 spots), PercentileCard, ResignPhase (2 spots), OfferSheetPhase, TradeBlockPanel, DocketClient, GmAnalysisTabs, SeasonResultsPager, players/page.tsx (2 spots). Touched `app/lib/trade-types.ts`, `app/data/secondary-positions.ts` (new), `app/lib/display-position.ts` (new), `app/lib/roster-assembly.ts`, `app/teams/page.tsx`, and 15 component files.
- 2026-07-13: **Teams page: team DNA strands, projected lines, roster detail, sort button fix.** Major expansion of the Teams page expanded card. Added `computeRosterStrand` + `TeamStrand` visualization ("Team DNA" section), `buildTeamLines` for projected forward lines / defense pairs / goalie tandem ("Projected Lines" section using `lineupContributionScore` ranking with secondary-position-aware flex center logic), roster overview strip (avg age, roster size, pending UFA/RFA counts), expanded top players from 8→10 with `displayPosition`, and fixed sort button selected state (was black-on-black in dark mode — changed to `var(--ledger-red)` background with white text). Touched `app/teams/page.tsx`.
- 2026-07-13: **Ship-readiness audit + Team Analytics page + infrastructure.** Full audit of the codebase for public launch readiness. Found 14 green items (build, tests, auth, security, SEO), 5 amber (fix-before-ship), and 3 red (key gaps). Fixed the amber blockers: (1) **Global error boundary** (`app/error.tsx`) — catches unhandled JS errors with a branded "Press Stop" recovery page instead of a white screen. (2) **Custom 404** (`app/not-found.tsx`) — "No ruling on file" page in the Ledger style. (3) **robots.txt + sitemap.xml** (`app/robots.ts`, `app/sitemap.ts`) — blocks `/admin/` and `/api/` from crawlers, maps all public pages for search engines. (4) **Homepage copy fix** — moved the 3-Year Simulation Model from "Coming Soon" to live (it exists as Armchair GM Cup Run); renumbered features. (5) **Team Analytics page** (`app/teams/page.tsx`) — new `/teams` route showing all 32 franchises with: contention window (Present/Future ratings from `computeContention`), competitive stage (phase chip), roster NAV (top 10 skaters + starter), EDGE profile (OZ Time, Top Speed, 20+ Bursts, HD Finish from `computeTeamEdgeProfile`), cap situation (committed vs ceiling bar), and top 8 roster assets by NAV. Sortable by rank/present/future/NAV/cap space/speed/name, filterable by phase. Expandable cards with full detail. (6) **Nav header updated** — "Teams" link added to site-wide navigation. 3 new canary tests. 512 tests pass, tsc clean. Touched `app/error.tsx` (new), `app/not-found.tsx` (new), `app/robots.ts` (new), `app/sitemap.ts` (new), `app/teams/page.tsx` (new), `app/teams/loading.tsx` (new), `app/page.tsx`, `app/components/Header.tsx`, `__tests__/feature-canaries.test.ts`.
# POSTCODEX_AUDIT
- 2026-07-10: **SIM audit → trust cluster + archetype mislabel.** From a full 3-year run: (1) **Prospect X-NAV inflation** — an unproven kid was carrying franchise-tier value (Daxon Rudolph, 19, 2 pts → 302; Mrtka 244). Capped `calcProspectNAV` at a blue-chip `PROSPECT_CEILING = 240` and clamped certainty ≤ 1.0 (a prospect can't be worth MORE than his own draft slot until he produces — he still carries bust risk). A true #1 overall stays elite (~240) but no longer outvalues a proven cornerstone. (2) **Archetype shooter/playmaker mislabel** — `fArchetype` split on `ops/(ops+dps)` (an offense-vs-defense ratio), so an assist-heavy scorer (Scheifele 28G/79A) read as a **SNIPER**. Now split on the real goals/assists share (`goalsPace`/`assistsPace`, threaded through `AssetInput` + the evaluate route): goal-heavy → SNIPER, assist-heavy → PLAYMAKER. (3) Confirmed the believable-season **156 ceiling** is in code (earlier 156 was a stale deploy). Garrett-Brown "BOTTOM SIX" on a D is a code-verified-correct path (classifyRosterTier handles isD, blend preserves it) — likely deploy timing / a data anomaly; re-check on a fresh run. **NOTED for later (bigger builds the user called out):** a **modern archetype taxonomy** to replace the EA-style SNIPER/SCORER labels — line raisers / establishers / finishers, impact, clutch, defensive, speed-burst, space-openers, and **gravity** (McDavid/Curry-style — a star who pulls defenders and opens space for teammates); a real **prospect 3-year development arc** (30→45→60 rookie curve); and the **sim-stats carry-forward bug** — after a season a prospect (Björck: 6G-25A) still shows 0 pts/0 TOI on the lineup card, meaning simmed production isn't written back to the roster, so a prospect can't actually develop year over year (this blocks the progression ratchet from Phase 2 #5 from ever engaging on real sim output). Touched `app/lib/xnav-engine.ts`, `app/api/evaluate/route.ts`, `__tests__/xnav.test.ts`, `__tests__/feature-canaries.test.ts`.
- 2026-07-09: **SIM audit → Phase 3 (Cup Run rules) — in progress.** (1) **Cap-gated re-signing.** There was no cap check on re-signing your own expiring players — you could pile salary well past the ceiling. The pending re-sign rows now compute `affordable = aav <= capSpace` and disable the Re-Sign button (projected-cap readout + reason), mirroring the market-signing gate; no room → let him walk or free space first. (2) **Prune spent draft picks on year-roll.** The pick inventory was built once from the static `SEASON.draftYear` and never advanced, so a spent pick stayed tradeable through later seasons (a 2027 pick in the 2028-29 season). Each Cup Run year-roll now drops picks from a draft that already happened (`year < SEASON.draftYear + currentYear - 1`). **Remaining Phase 3:** the simulated draft actually consuming the picks the user traded FOR (acquired picks feeding the draft order) — deeper, still open. Also from the Scheifele audit: **real career peak** (best actual NHL season from snapshots via `careerPeakFromSnapshots`, so a stale curated pedigree constant can't understate it — 103 not 88) and a **believable-season ceiling** in the sim (the breakout/variance/deployment/burst multipliers stacked to ~1.7×, letting a 33-yo post 156; now capped at a role-appropriate multiple — 1.9× for ≤23 young breakouts down to 1.22× at 33+, so Scheifele tops ~113). Touched `app/components/ResignPhase.tsx`, `app/armchair-gm/useCupRunLifecycle.ts`, `app/lib/development-profile.ts`, `app/components/AssetCard.tsx`, `app/api/simulate/route.ts`, tests.
- 2026-07-09: **SIM audit → Phase 2 (young-player realism) — done.** (1) **Opportunity unlocks a young player's ceiling.** Season production was anchored to a player's established pace, so a talented 20-yo (Björck) projected ~15 pts no matter how good his deployment was (the 2nd-line multiplier is only ×1.04). New pure helper `app/lib/young-opportunity.ts` (`opportunityPace`): when a young upside player is handed a scoring role, his pace blends UP toward what that role supports (F lines ~66/48/30/16, D pairs 44/30/18), gated by draft pedigree + prospect signal so a fringe AHLer doesn't get a star's line. It only ever lifts — a buried young player is still handled by the deployment penalty, and veterans are untouched. Björck on the 2nd line now projects ~30 instead of ~15. Extracted to a lib because Next.js route files can't export helpers for tests. (2) **Progression curves / development ratchet.** In the Cup Run year-rollover (`advanceSeason`) a developing player (≤23) who proves a higher level now BANKS 60% of it into his baseline (vs the flat 40% drift), so a real breakout compounds year over year instead of evaporating back to his old anchor (next season's `stablePts` is 60% baseline). A down year still regresses normally — the ratchet only accelerates upside. Touched `app/lib/young-opportunity.ts` (new), `app/api/simulate/route.ts`, `app/lib/season-rollover.ts`, `__tests__/young-opportunity.test.ts` (new), `__tests__/season-rollover.test.ts`.
- 2026-07-09: **SIM audit → Phase 1 (trust & correctness) — in progress.** Ran a full 3-year Cup Run and catalogued issues; Phase 1 targets the ones that undermine trust in the numbers. **Done:** (1) the expanded Ledger Line valuation breakdown now **reconciles to X-NAV** — off/def/age/cap/upside could never sum to the header (total is those components run through scarcity/development multipliers + franchise/prospect floors, and upside re-exposed age/cap), so it's now `Offense + Defense + Age + Contract + Model Adj. = X-NAV` with a reconciliation line, and prospects with no market AAV read "ELC / n/a" not "FMV $-M"; (2) **defensemen get pairing labels** — `classifyRosterTier` was position-blind (a 24-min/49-pt D read "1ST LINE HIGH 2C", i.e. a second-line *center*); added an `isD` branch → ELITE_1ST_PAIR/TOP_PAIR/SHUTDOWN_D/SECOND_PAIR/THIRD_PAIR; (3) **breakout vs career year** — a 32-yo over-pace season was tagged BREAKOUT; split by age (≤26 breakout, older = CAREER_YEAR); (4) **de-collapsed the flat franchise floor** — it was three flat tiers (180/220/260 F, 160/200/240 D) so every qualifying star clamped to the identical number (the wall of "180"s); added a production slope above the qualification bar so distinct producers get distinct floors (still a pure floor, never lowers a value); (5) GSAx display to 1 decimal. **Remaining in Phase 1** (calibration-sensitive — validate against real numbers after re-running the sim on-branch): goalie GAA/SV%-vs-NAV framing, young/RFA contract-surplus sanity (Sennecke +93), elite-D ceiling sanity (Makar 351 — note the floor change above may have shifted this), and small polish (pick "age 19", seed leak). **Full backlog / phase plan** (Phases 2–6: young-player production + progression curves; Cup Run cap re-sign + pick inventory; C/A UI + 3-year recap arc; Team DNA strand redesign + roster-needs targets + AA sweep; realism backlog = injuries / in-season AI trades / special teams / clause enforcement) lives in the session thread. Touched `app/armchair-gm/SeasonResultsPager.tsx`, `app/lib/xnav-engine.ts`, `app/lib/trade-types.ts`, `app/api/simulate/route.ts`, `__tests__/xnav.test.ts`, `__tests__/feature-canaries.test.ts`.
- 2026-07-09: **Contract + Outlook tabs — context and a rework.** The Contract Projection was a fixed 280px bar SVG stranded in the left third of a full-width panel, with unlabeled bars (`+269/+257…` — nothing said they were per-year trade value) and 6–7px text, and "Projected Next $15.2M × 3yr" had no context. Rebuilt `PlayerTimeline` as an accessible, full-width layout: a **Current Deal** strip with a bargain/overpay chip (Market AAV vs actual cap hit — `+$4.1M vs market · BARGAIN` / `−$8.5M · OVERPAY`); a semantic `<table>` (caption, `scope`) of **trade value (NAV) by contract year** with a labelled bar per year (negative years extend left from a zero marker — Huberdeau reads −61/−79/−98 in red) and a direction chip (RISING/HOLDS STEADY/DECLINING); and a **Projected Next Contract** block that explains the number in plain English (signs at age X as a UFA/RFA, the AAV is a fair-market midpoint at today's cap — not a max or a floor). A value-aware read sentence: negative-value deals are called out as an "anchor contract that's hard to move," not "stable/movable." Works for goalies too. For the Development Outlook (`DevelopmentProfilePanel`) — already data-rich with a key — added an **always-visible 0–100 scale line** so the wall of indices reads without opening the key, and relabelled the collapsible "What does each score mean?" (matching the strand). Deferred (noted for next): a goalie Development Outlook (the panel still returns null for G) and a deeper Outlook visual rework. Touched `app/components/PlayerTimeline.tsx`, `app/components/DevelopmentProfilePanel.tsx`, `__tests__/feature-canaries.test.ts`.
- 2026-07-09: **NAV consistency — Player Card vs Armchair GM, and a real goalie engine pass.** Two paths compute NAV (client `calcNAV` in the analytics Player Card, server `/api/evaluate` in Armchair GM) and they disagreed hard. Root cause: the card fed `calcNAV` an *incomplete* asset while evaluate feeds the full context plus a pedigree floor. Fixes: (1) **Huberdeau read +80 in Armchair, −64 on the card.** The evaluate floor (`getHistoricalFloor`) was flooring the *bottom-line total* and dumping the entire lift into the **cap** component — so an overpaid vet ($10.5M, $2M FMV) read as cap-POSITIVE (+58). Now the floor applies to **talent** (`total − cap`), the real contract drag is re-applied, and any lift is booked to `upside` — cap stays honest. (2) **Decline gate on the pedigree floor**: the floor exists to protect a star's *down year*, not resurrect a multi-year decline — a `declineGate` (scaled by current-pts / peak-pts, skaters only) collapses the floor toward current value when a player produces far below his peak (Huberdeau 41 vs 115 peak → no lift; a dip to ~85% of peak still floors). Huberdeau now reads ≈−58 in **both** paths. (3) **Goalie card showed `1.307822854307652`** — `calcGoalieNAV` returned an unrounded `total` (and `def`/`age`); now `Math.round`ed. (4) **Elite goalie down years cratered** (Hellebuyck 5.5 GSAx → ~0): single-season GSAx is a weak predictor, so when a real career baseline exists starter confidence is capped at 0.68 (≈32% career weight, 0.62 for ≤26) instead of 0.80 — a proven elite regresses toward his track record (down-year total ~0 → ~23, def +51) while **baseline-less** goalies are unchanged (Wolf/volatility guardrails preserved). (5) The Player Card now passes goalie context (`baselineGsax`, `teamXga60`, `teamHdca60`, `baselineHdsvPct`) into `calcNAV` so a starter isn't valued on defaults. Note for next: the card is intentionally *floor-free* (honest current value — the user confirmed −64 is "correct"); Armchair adds the pedigree floor for trade realism, so the two still diverge for a legit star dip — by design, documented here. Touched `app/lib/xnav-engine.ts`, `app/lib/player-data.ts`, `app/api/evaluate/route.ts`, `app/components/PercentileCard.tsx`, `app/players/page.tsx`, `__tests__/xnav.test.ts`, `__tests__/feature-canaries.test.ts`.
- 2026-07-09: **STRAND clarity + tone-down pass.** Live-branch feedback: the redesigned strand was "too in your face" and the numbers didn't say what they were ("OPS is 77 — 77 what?"). Fixes: a **persistent key** under every strand ("Big number = 0–100 rating vs the NHL field (100 = elite). Small number = the actual stat. Blue = offense, red = defense."); **real units** on the raw line (TOI `21.5 min` not `21.5m`/metres, xG `28 xG/82`, NOIV `+12.2%`); **clearer labels** (`Usage`→`QoC` with the redundant raw dropped since the index *is* the QoC; `TOI+`→`TOI`; SUPP shown as positive suppression `+0.26 xGA`, higher = stingier, with a plain-English definition); the trait guide rewritten in plain language and relabelled "What does each trait mean?"; a **`maxWidth` cap** (460 on the players page) so a full-bleed container stops scaling the SVG up, plus slightly smaller node numbers. Bug: EDGE "OZ Time" printed the NHL feed's 0–1 percentile fraction raw (`0.898%ile`) and mis-colored off it — normalized to a whole-number percentile (`45% · 90%ile`, now correctly green). Touched `app/components/StrandDisplay.tsx`, `app/components/StrandView.tsx`, `app/components/EdgeStrip.tsx`, `app/players/page.tsx`, `__tests__/feature-canaries.test.ts`.
- 2026-07-09: **FMV — dynamic baseline blend so a contract year doesn't set the price.** Elite FMVs pinning near the CBA 20% max is right for proven stars (McDavid: pedigree + precedent), but a player merely playing over his head in a contract year shouldn't command that money. The engine already anchored offense to a multi-year baseline — but with a *flat* 40/60 blend that damped a real young breakout exactly as hard as a fluky veteran spike. New `currentSeasonWeight()` makes the blend dynamic on **breakout credibility**: when the current season spikes above baseline (`> baseline*1.05`), how much of the spike counts depends on whether it's corroborated — NOT riding hot high-danger finishing (EDGE `hdFinishingDelta`, the clearest luck tell), young age, draft pedigree, and a full sample. Corroborated breakouts earn a current weight up to 0.58 (the leap counts); uncorroborated spikes anchor toward baseline at 0.20. Proven stars sit at current ≈ baseline so the gate never fires — they're untouched. Feeds the deployment-context blend that drives `offTotal → FMV`. Touched `app/lib/xnav-engine.ts`, `__tests__/xnav.test.ts`.
- 2026-07-09: **Player Card — AA-accessible redesign + FMV surplus read.** The analytics Player Card (richest per-player data in the app) was a cramped 380px box of 8–9px text, `div`-grid "tables" screen readers can't parse, `title`-only tooltips, and several color-on-color combos failing WCAG AA. Rebuilt: percentiles now render as a semantic `<table>` (caption, `scope="col"/"row"`) with each bar carrying `role="img"` + an aria-label ("87th percentile — elite"); text bumped to 11–16px with darker ink/bar fills; tabular figures. FMV reframed for interpretation accuracy — shown as **Market AAV** beside the actual **Cap Hit** with an explicit **Surplus** cell (`+$7.9M BARGAIN` / `−$4.1M OVERPAY`), since a big FMV only means value relative to what he's paid; NAV breakdown moved to a `<dl>` and rounds its values (the goalie def path wasn't pre-rounded, printing a raw float). Wider responsive layout (max-width 620, two columns ≥540px, stacks on mobile). **`MetricTip` made keyboard + screen-reader accessible** (focusable trigger, opens on focus, closes on blur/Escape, full definition in `aria-label`, `role="tooltip"`) — lifts a11y everywhere the glossary tips appear. Touched `app/components/PercentileCard.tsx`, `app/components/MetricTip.tsx`, `__tests__/feature-canaries.test.ts`.
- 2026-07-09: **STRAND rebuilt — silhouette, one 0–100 read, EDGE band, 3×3 goalies.** The helix was doing two jobs and botching both: a shape-of-identity fighting a stat table stapled on the wave in four different number scales (OPS point-shares next to NOIV 0–100 next to OZ raw %). Applied everywhere the strand renders (players, trade machine, docket, armchair). Every node now reads a consistent **0–100 index** (`round(val*100)`) with the raw value on a faint sub-line; labels moved off the wave onto **fixed top (offense) / bottom (defense) rails** with per-trait column dividers, and the wave amplitude is clamped to the rail zone so peaks never climb into the text at any caller height. New **EDGE band** renders inside the strand card via a `footer` slot on every surface (skater: Top Speed / 20+ Bursts / HD Finish / OZ Time; goalie: HD Save% / GSAx); `EdgeStrip` now takes a structural `EdgeSignalSource` so both `Asset` and the players `Player` satisfy it. **Goalies** went from a lonely 2×2 (HDSV hardcoded to a greyed dash) to a shared **3×3 on real data** — GSAX / SV% / HDSV over Workload / Shot volume / GAA (GAA index inverted so a stingy goalie reads high), greying out honestly when a source is missing. Trait guide derived from the traits on screen; goalies get the GOALTENDER badge, not a mislabelled skater archetype. Verified against a throwaway preview route (local.db has no skaters to drive live pages). Touched `app/components/StrandDisplay.tsx`, `app/components/StrandView.tsx`, `app/components/EdgeStrip.tsx`, `app/players/page.tsx`, `app/components/QuickTradeMachine.tsx`, `app/docket/DocketClient.tsx`, `__tests__/feature-canaries.test.ts`.
- 2026-07-09: **Trade Machine (`/trade-machine`) — the flagship "who says no" surface.** Two upgrades to the shareable one-off trade page. (1) **Expandable EDGE scouting on every asset row**: collapsed shows a scannable stat chip (`pts/82 · TOI` for skaters, `SV% · GSAx` for goalies) plus the player's NAV; tapping expands into the `MeasuredProfile` percentile bands + a compact `StrandDisplay` (built from `buildAssetTraits`/`computeStrandType`). Picks stay flat. `navMap` threaded through all four `AssetList` call sites including the shared-link view (`SharedTradeView` now fetches NAV for the locked package so opened `/t/[code]` links render values + scouting). (2) **Live contention window + buyer/seller stance per team**: new `TeamWindowBadge` reads each team's live phase via `deriveTeamPhase` over the full-roster NAV (fetched once per pairing), falling back to the seed phase when the roster lacks enough valued players, and maps phase → stance (Contender/Bubble = Buyer, Rebuilding/Tanking = Seller, Retooling = Flexible) with a one-line rationale; when a deal is loaded it shows the post-trade window shift (▲/▼) from the effective rosters. The richer full-roster NAV also feeds `TeamStrandPreview`. Touched `app/components/QuickTradeMachine.tsx`, `__tests__/feature-canaries.test.ts`.
- 2026-07-08: **Measured Profile — the sim's drivers as percentiles, not a 0-99 rating.** New `app/lib/measured-profile.ts` (`computeMeasuredProfile`) + `app/components/MeasuredProfile.tsx` render the exact signals the engine reasons over — Production (pts/82), Ice Time (opportunity), Burst (EDGE explosiveness), Finishing Luck (EDGE high-danger finishing), Pedigree (draft slot / NHLe) — each as a percentile against a documented real-NHL reference band (~p10→p90 by position, the same bands the breakout/burst models use), with the raw measured value shown beside it ("45 bursts · 23.2 mph · 100%ile"). The honest discipline: EDGE-sourced dimensions are labeled, finishing is framed as luck (cold = bounce-back/green, hot = running-hot/red — not skill), and a dimension with no measured sample is rendered as a hatched bar with "no EDGE sample" instead of an invented number. Surfaced on the armchair AssetCard EDGE tab (broadened to all skaters; the shot map still no-ops without a numeric NHL id) above the shot map. Verified in-browser across explosive/no-sample/rush-D/prospect cards. Touched `app/lib/measured-profile.ts` (new), `app/components/MeasuredProfile.tsx` (new), `app/components/AssetCard.tsx`, `__tests__/measured-profile.test.ts` (new), `__tests__/feature-canaries.test.ts`.
- 2026-07-08: **Burst as a sim channel — explosiveness → rush offense + upside tail.** New `app/lib/burst-channel.ts` (`burstProfile`) reads the EDGE explosiveness signals (`edgeBurstsOver20` / `edgeSpeedMaxMph`) and returns a `rushLift` (a small, steady multiplicative scoring lift — explosive skaters create off the rush) and a `varianceKick` (extra upside added only to the ceiling of the season's pace variance — a boom year is more available to an explosive player). Wired into `projectSkaterOutcome` in `/api/simulate`: `paceVariance` gains `rand() * burst.varianceKick` (fattens the tail without lowering the floor) and `projectedPts` is multiplied by `burst.rushLift`. Tiers: elite (≥40 bursts or ≥22.5 mph) → +4% / +0.10; strong (≥25 or ≥21) → +2% / +0.05. Unlike the breakout model this is NOT age-gated (an explosive veteran still creates off the rush), and it is a strict no-op for any skater without an EDGE sample and for all goalies — explosiveness is never invented. Verified deterministically (same player id + seed across two runs isolates the channel: explosive > cruiser). Touched `app/lib/burst-channel.ts` (new), `app/api/simulate/route.ts`, `__tests__/burst-channel.test.ts` (new), `__tests__/simulate-and-claude-routes.test.ts`, `__tests__/feature-canaries.test.ts`.
- 2026-07-08: **League Numbers → real standings.** The League Numbers season page showed only award winners; the sim already returned full `standings` (all 32 teams with projectedPoints/division/divisionRank/leagueRank/madePlayoffs) and never rendered them. New `LeagueNumbers`/`DivisionStandings` components in `SeasonResultsPager` render a compact Award Race strip (President's Trophy, Cup, Conn Smythe, Art Ross/Richard/Hart/Norris/Vezina/Calder, Draft Lottery) plus real standings grouped Eastern (Atlantic, Metropolitan) / Western (Central, Pacific), each division sorted by rank with points and a playoff marker (x = clinched top-3, WC = wildcard, — = out). The user's home/partner teams are highlighted. Pure front-end over data the sim already produced — supports the year-3 Claude review having real league context. Touched `app/armchair-gm/SeasonResultsPager.tsx`, `__tests__/feature-canaries.test.ts`.
- 2026-07-08: **Breakout model overhaul — one data-driven model, not a coin flip.** New `app/lib/breakout-model.ts` (`computeBreakout`) replaces the finishing-delta-only breakout roll that both the single-season sim (`/api/simulate`) and the Cup Run rollover (`season-rollover`) used separately. It weights the signals that actually drive a breakout, so young players pop for reasons tied to their real-life profile: **opportunity** (real top-six/top-four TOI lifts odds; buried usage ×0.55 — you can't break out from the press box), **pedigree** (draft slot + NHLe prospect pace for young players), **finishing luck** (EDGE high-danger finishing vs league, xG-vs-goals fallback), and **burst** (EDGE 20+ mph bursts / top speed fatten a young player's upside tail — applied only where the EDGE sample exists, never invented). An aging-well guard keeps a productive, heavily-deployed veteran declining slower than a washed same-age player. Each signal is a bounded additive nudge on an age/role base (burst/pedigree modify, they aren't headline scalars), and the model reports a `driver` so breakout events are legible in the season recap / year-3 Claude review. `season-rollover.breakoutOdds` is now a thin adapter (its 4 existing tests preserved, incl. the exact scenery ×2); the sim route calls the model directly. Added optional signal fields to `SimPlayer`/`RolloverPlayer` (games, avgTOI, draftOverall, edgeBurstsOver20, edgeSpeedMaxMph, goalsPace). Touched `app/lib/breakout-model.ts` (new), `app/lib/season-rollover.ts`, `app/api/simulate/route.ts`, `__tests__/breakout-model.test.ts` (new), `__tests__/feature-canaries.test.ts`.
- 2026-07-08: **Live team timelines feed trade willingness.** The HOME/PARTNER timeline badge (and the server-side trade-willingness logic in `/api/evaluate`, which keys off `team.phase`) read a static last-season-standing phase seeded in `db.ts` that never moved when a roster was rebuilt or re-signed — so a gutted "Contender" still behaved like one. New pure `deriveTeamPhase(roster, navMap)` in `contention.ts` maps the roster's present-strength rating (the same contention math the quadrant uses) onto the phase vocabulary (Contender/Bubble/Retooling/Rebuilding/Tanking). A page effect recomputes every team's phase into `db.teams` whenever players/navMap change (trades, offseason signings, Cup Run rollover); because it depends only on players+navMap and no-ops when nothing changed, writing back to `db.teams` can't loop, and the store-synced badge + the willingness payload both track the roster in real time. Robustness: `deriveTeamPhase` returns null when a roster lacks ≥9 valued, established (games ≥10) players, and the effect then keeps the seed phase — so thin/partial data never collapses every team to "Tanking" (caught in browser QA where local rosters are sparse). The Cup Run offseason is exactly the "attract better players" case the user flagged: re-sign/sign your way up and the timeline climbs toward Contender, opening up who will deal with you. Touched `app/armchair-gm/contention.ts`, `app/armchair-gm/page.tsx`, `__tests__/team-phase.test.ts` (new), `__tests__/feature-canaries.test.ts`.
- 2026-07-08: **Sim UX batch — sim without a trade, AI best lines, split box score.** Three of the five requested sim improvements. (1) **Sim a season with zero trades**: the dispatch no longer requires an executed trade (`if (!homeTeam) return;`), the Sim tab is always present (badge only when trades exist), and the button is ungated — the header reads "Baseline (No Trades)". The baseline league is a valid scenario. (2) **AI teams field best lines by default**: `/api/simulate` builds a `defaultLineupOrder` (top 12 F, 6 D, best G by on-ice value) for any team the user hasn't hand-set, so every club gets a real starter/bench deployment in the projection instead of a flat, deployment-less season where depth plays like top-sixers. Verified with a route test (a 13th forward is benched with no lineup set). (3) **Season box score split by Forwards / Defense** with a Goaltending header on the Crease table, and League Numbers labels bumped 6.5px → 9px for accessibility. (4) **Prospect X-NAV clarification** (#5a, light): added a note that X-NAV is trade value — for a young prospect it is Upside/pedigree-weighted, so Offense/Defense components can read 0 even after a productive simulated season (Viggo Björck case). **Deferred with a plan**: recomputing team timelines/phase from the live roster and feeding it into trade willingness (the RETOOLING/CONTENDER badges are still static from load), and expanding League Numbers into full standings/division breakdowns (needs the sim to return richer standings data). Touched `app/api/simulate/route.ts`, `app/armchair-gm/useSimDispatch.ts`, `app/armchair-gm/GmAnalysisTabs.tsx`, `app/armchair-gm/SeasonResultsPager.tsx`, `__tests__/simulate-and-claude-routes.test.ts`, `__tests__/feature-canaries.test.ts`.
- 2026-07-07: **Cup Run sim fixes — cap escalation, draft year, FA signing, phantom NAV.** Four bugs surfaced running a multi-year Cup Run. (1) **Cap ceiling was flat** at $104M every year, so teams whose salaries grew showed as ~-5.5M illegally over the cap in Year 2. Added `CAP_BY_CUP_YEAR`/`capForCupYear` in season-config with the real announced numbers (104 → 113.5 → 123) and threaded the entering year's ceiling through `rollLeagueForward`'s AI cap-legality pass, `reconcileAiTeamCapSpaces`, and `db.capCeiling` in `useCupRunLifecycle`. (2) **Draft year off-by-one**: `draftYear = 2026 + nextYear` drafted the 2028 class when rolling into Year 2; fixed to `SEASON.draftYear + nextYear - 1` so Year 2 drafts 2027. (3) **AI never signed the FA pool**: a prior fix had fully exempted `FA_POOL` UFAs from AI signing, so Kane/Reinhart/etc. sat unsigned forever (and the over-cap bug blocked re-signings like Celebrini). Reverted to `addMarketCandidate` so contenders work the board again — cap room + positional need naturally leave plenty for the user, and the cap escalation is what makes teams solvent enough to sign. (4) **26yo-AHLer phantom value**: a rolled roster can carry an elite per-82 pace on a 1-game line ("1 GP · 1 PT"), and the franchise-cornerstone floor (`ptsPace >= 80`) fired with no sample gate → +140 NAVs. Gated the floor on `games >= 40`, and added a thin-sample credibility regression: below 15 games the value regresses toward a replacement anchor, but only when the player lacks draft pedigree AND accumulated Point Shares (ops+dps) AND a real sample — so injured stars (real Point Shares) and drafted rookies stay untouched while the phantom collapses from ~+142 to ~+57. Touched `app/lib/season-config.ts`, `app/lib/cup-run.ts`, `app/armchair-gm/useCupRunLifecycle.ts`, `app/lib/free-agency.ts`, `app/lib/xnav-engine.ts`, `__tests__/cup-run.test.ts`, `__tests__/free-agency.test.ts`, `__tests__/xnav-team-control.test.ts`, `__tests__/feature-canaries.test.ts`.
- 2026-07-07: **Docket cleanup — tabbed player detail, EDGE everywhere, mobile/AA.** The expanded Docket ruling dumped a full strand + development outlook inline for every player, so a two-team trade was a wall of data. Each player card is now tabbed (STATS / STRAND / OUTLOOK) with accessible tab semantics (`role="tablist"`/`role="tab"`/`aria-selected`) and 44px `tap-target` controls — one panel at a time. New `app/components/EdgeStrip.tsx` surfaces the real NHL EDGE snapshot fields as tiles: skaters get HD-finishing luck (color-coded bounce-back/cool-off), OZ-time share + percentile, top skating speed, and 20+ mph bursts; goalies get high-danger save % and GSAx (the danger-zone signals, de-duplicated from the conventional SV%/GS stat tiles). The EDGE strip rides under both the STATS and STRAND panels, so the strand finally carries EDGE for goalies too. The Development/Outlook panel is shored up with an `EDGE Finish Luck` line under the projection band (cold shooters read BOUNCE-BACK, hot read COOL-OFF) — the same signal that already nudges the sim/X-NAV, now visible where the projection lives. Mobile: the filter bar and both two-package grids collapse to a single column under 640px (new `.docket-pkg-grid`/`.docket-filters` classes) instead of forcing two columns and overflowing; small faint 9px labels bumped to 10-11px. Verified at 1180px and 390px against a mock ruling. Touched `app/docket/DocketClient.tsx`, `app/components/EdgeStrip.tsx` (new), `app/components/DevelopmentProfilePanel.tsx`, `app/globals.css`, `__tests__/feature-canaries.test.ts`.
- 2026-07-07: **Free agency fix — the top UFAs finally show.** Two bugs hid the marquee free agents behind a wall of zero-production depth. (1) The curated 2026 UFA/RFA class in `FREE_AGENT_SEED_2026`/`seedFreeAgentStatus` was dead code — never called — so FA-pool injection skipped every player whose bare seed row had no `expiryStatus` (Mantha, Kane, Tarasenko, Johansson, Nyquist, Klingberg, …). Injection now falls back to the curated class (`d.expiryStatus ?? seedFreeAgentStatus(d.name)`), and MoneyPuck's league-wide stat map fills in their real production so they price honestly. (2) `resolveLeagueOffseason` let AI teams auto-sign the pre-existing open FA pool and splice it out of the market before the user ever saw it — so the best names vanished on day one. The pre-existing pool (teamless / FA_POOL) now stays on the board for the user; AI teams still shop **walk-aways** (players leaving their own rosters), preserving cap-disciplined market churn for Cup Run. On the culling question: no hard 0G/0A cull — assist-only D-men (Grzelcyk 0-12-12, Petry 0-9-9) have value, and the market is sortable by ask/NAV/age so production floats to the top; genuine 0-year/$0 junk is still handled by the existing "Push Limbo Contracts to FA" tool. Touched `app/lib/roster-assembly.ts`, `app/lib/free-agency.ts`, `__tests__/free-agency.test.ts`, `__tests__/feature-canaries.test.ts`.
- 2026-07-07: **Armchair GM modal AA + free-agent pagination.** Re-sign and offer-sheet modals now expose dialog semantics, labeled/touch-sized controls, and paginate full UFA/RFA markets instead of truncating at 60 visible rows. Touched `app/components/ResignPhase.tsx`, `app/components/OfferSheetPhase.tsx`, `__tests__/feature-canaries.test.ts`.
- 2026-07-07: **Armchair GM rework Phase 5 complete — mobile + AA sweep.** Added shared 44px tap-target and focus-visible primitives, made the Armchair trade flow explicitly single-column on phones, labeled trade/sim/verdict controls, and made lineup slots keyboard-focusable/activatable with regression canaries. Touched `app/globals.css`, `app/armchair-gm/page.tsx`, `app/armchair-gm/GmAnalysisTabs.tsx`, `app/armchair-gm/VerdictSheet.tsx`, `app/components/TradePanel.tsx`, `app/components/LineupEditor.tsx`, `__tests__/feature-canaries.test.ts`, `docs/futures/ARMCHAIR_REWORK.md`.
- 2026-07-07: **Armchair GM lineup tab persistence.** Saved lineup orders now hydrate back into the lineup editor when the tab remounts, so switching to the Sim tab no longer resets hand-built lines; added a pure lineup-order helper and regression coverage. Touched `app/lib/lineup-order.ts`, `app/components/LineupEditor.tsx`, `app/armchair-gm/GmAnalysisTabs.tsx`, `app/armchair-gm/page.tsx`, `__tests__/lineup-editor.test.ts`, `__tests__/feature-canaries.test.ts`.
- 2026-07-07: **Armchair GM AI market cap reserve.** AI teams now keep one league-minimum cap cushion when signing open-market UFAs during offseason resolution, preventing the signing phase from draining every club to $0 cap space; added resolver regression coverage. Touched `app/lib/free-agency.ts`, `__tests__/free-agency.test.ts`.
- 2026-07-07: **Armchair GM rework Phase 3 complete — future draft pick + signing market.** Cup Run years 2-3 now surface an on-the-clock future-draft choice from the top curated NHLe-sorted prospects remaining, swapping displaced prospects instead of duplicating them; Re-Sign market rows now show NAV, EDGE HD, age-curve arrows, live cap impact bars, and Ask/NAV/Age sorting. Touched `app/lib/future-draft-choice.ts`, `app/armchair-gm/CupRunDraftSummaryModal.tsx`, `app/armchair-gm/useCupRunLifecycle.ts`, `app/armchair-gm/page.tsx`, `app/components/ResignPhase.tsx`, `__tests__/future-draft-choice.test.ts`, `__tests__/feature-canaries.test.ts`, `docs/futures/ARMCHAIR_REWORK.md`.
- 2026-07-07: **Armchair GM rework Phase 4 complete — Team DNA EDGE snapshot.** Latest NHL EDGE snapshots now thread OZ%, OZ percentile, top speed, and 20+ mph bursts onto roster assets; Team DNA aggregates them into post-trade home/partner EDGE tiles, backed by a pure team profile helper and regression coverage. Touched `app/lib/nhl-feed-capture.ts`, `app/lib/roster-assembly.ts`, `app/lib/trade-types.ts`, `app/lib/team-edge-profile.ts`, `app/armchair-gm/GmAnalysisTabs.tsx`, `app/armchair-gm/TeamEdgeTiles.tsx`, `__tests__/team-edge-profile.test.ts`, `__tests__/feature-canaries.test.ts`, `docs/futures/ARMCHAIR_REWORK.md`.
- 2026-07-07: **Armchair GM rework Phase 2 — main component decomposed.** `ArmchairGmPage` went 1,985 → 980 lines with zero behavior change. Three hooks own the lifecycle logic: `useCupRunLifecycle` (run state machine, localStorage + resume guard, year-advance rollover), `useOffseasonFlow` (once-per-season league FA resolution, draft/re-sign/offer-sheet routing, all offseason roster/cap mutations), `useTradeBench` (executed trades, hand-set lineups, executeTrade with the retention ledger, resetTrades). Five components own the big JSX: `TeamSelectModal`, `MemoModal`, `CupRunResumePrompt`, `VerdictSheet`, `MatchResultsPanel` (now the home of MATCH_FOLDERS). Hook-order cycles (sim controls, season-rolled resets) bridged by refs assigned each render and read only inside event handlers. Canaries retargeted via `readArmchairAll`. Roadmap: `docs/futures/ARMCHAIR_REWORK.md`. Touched `app/armchair-gm/*` (8 new files), `__tests__/feature-canaries.test.ts`.
- 2026-07-07: **Armchair GM rework Phase 4 slice — EDGE data into sim logic + trade UI.** (1) `/api/simulate` skaters now carry `hdFinishingDelta`; `projectSkaterOutcome` biases the single-season roll off it (≤−2% high-danger finishing vs league → +6% breakout odds; ≥+3% → +6% regression), so the same EDGE luck signal that adjusts X-NAV and Cup Run `breakoutOdds` also shapes each simulated season. (2) Armchair GM AssetCards gain an EDGE tab (skaters with a real NHL id only) rendering the existing `EdgeShotMap` — shot-location tiles, finishing deltas, zone time, speed — inside the trade panel. Roadmap: `docs/futures/ARMCHAIR_REWORK.md` Phase 4. Touched `app/api/simulate/route.ts`, `app/components/AssetCard.tsx`.
- 2026-07-06: **Push limbo contracts to FA.** `PATCH /api/admin/prune-stale` (button in the prune card: "Push Limbo Contracts to FA") moves 0-year/$0 rows into free agency instead of deleting them: age 27+ (or unknown-age non-draftees) get `expiryStatus: UFA` + current offseason expiry so the FA-pool injection carries them into the market; RFA-eligible players (17-26) and unsigned draftees are left alone (their rights belong to teams) and reported in the response. Skips retired/excluded/extension rows and anything already in the FA flow; clears roster+contract caches on success. Best run AFTER the FA age backfill so the age gate works with real ages. Touched `app/api/admin/prune-stale/route.ts`, `app/admin/health/page.tsx`.
- 2026-07-06: **FA identity backfill — kills the "age 27" bug at the source.** FA-class seed rows carry age 0 / position Unknown, and the read path's 27 fallback kept resurfacing (Nyquist this time). New `POST /api/admin/fa-backfill` (button: Admin → Health → "Backfill FA Ages") resolves each gap row against the NHL search API (`search.d3.nhle.com`, unique exact-name match only — same-name pairs like the Petterssons are reported, never guessed), pulls the landing profile, and writes the real birthdate-derived age + position into the players table, then clears roster/contract caches. Capped 60/call. Curated FA_KNOWN_FACTS stays as override; Nyquist (36, $3.185M) added meanwhile. New pure helpers `parsePlayerSearch`/`pickSearchMatch` with tests. Touched `app/lib/nhl-player-feed.ts`, `app/api/admin/fa-backfill/route.ts` (new), `app/admin/health/page.tsx`, `app/lib/free-agent-seed.ts`, `__tests__/nhl-player-feed.test.ts`.
- 2026-07-06: **Site-wide typography accessibility pass.** Two uniform levers matching the refined EDGE tab standard: (1) `--ledger-ink-faint` darkened #9a7d58 → #6e5a3d and `--ink-faint` #7a6a50 → #635340 — the secondary/agate ink used across the whole app now reads as dense newsprint instead of washed-out tan, one variable change with global reach; (2) a 9px font floor enforced by codemod — all `text-[6-8px]` Tailwind utilities and `fontSize: 6-8` style objects bumped to 9-10px (55 call sites across armchair GM, press box, admin pages, components). Verified visually at 375px. Touched `app/globals.css` + 15 tsx files.
- 2026-07-06: **EDGE shot map refinement — accessibility, layout, mobile.** Rebuilt the map as non-overlapping anatomical tiles (5 rows x 3 columns inside the rink outline), each carrying its shot count (12px) and a zone label so the map reads without hover on mobile; added a shot-volume legend under the rink. Location summary rows restructured to two lines (percentile chip + name + shots/league, then goals + shooting% + finishing delta) at 10-11px; zone-time tiles and the speed strip bumped to readable sizes with darker ink; desktop is a 46/54 split, mobile stacks with the map full-width. Visually verified at 375px and 1100px against the real captured McDavid payload seeded locally. Touched `app/components/EdgeShotMap.tsx`.
- 2026-07-06: **Codex review + leadership intangibles + EDGE shot-map tab.** Reviewed 58c6dde..15387d9 (cap-aware FA resolution with AI market signings, double AI cap pass, reconcileAiTeamCapSpaces, lineup-ranking extraction, draft-summary routing) — sound, all 421 tests green, no corrections needed. Added on top: (1) `app/data/leadership.ts` curated C/A letters (editable); `lineupContributionScore` adds +28/+14 for C/A (lineup only — never trade value) and the sim's `onIceValue` gets a +3/+1.5 steadier, so Lowry-types slot like captains. (2) NHL EDGE tab on the Player Analytics expanded card: new public `/api/player-edge/{nhlId}` serves the latest stored edge snapshot; `EdgeShotMap` renders the 15-zone offensive-half rink (fill = shot-volume percentile, counts inline), location summary with finishing vs league, zone-time splits, and the speed strip — the EDGE screenshot, in newsprint. Touched `app/data/leadership.ts` (new), `app/lib/lineup-ranking.ts`, `app/api/simulate/route.ts`, `app/api/player-edge/[playerId]/route.ts` (new), `app/components/EdgeShotMap.tsx` (new), `app/players/page.tsx`, `__tests__/lineup-ranking.test.ts`.
- 2026-07-06: **Best Lines lineup ranking uses contribution, not raw X-NAV.** Extracted a lineup contribution score that weights production, deployment trust, matchup role, and NHL tenure with X-NAV only as a light tiebreaker, so defensive leaders on negative-value contracts are not buried by Best Lines. Touched `app/lib/lineup-ranking.ts`, `app/components/LineupEditor.tsx`, `__tests__/lineup-ranking.test.ts`.
- 2026-07-06: **Cup Run AI cap discipline + market signings.** AI offseason resolution now checks whether re-signing cap swings fit, sends unaffordable UFAs/RFAs off the roster, signs open-market UFAs only to non-user AI teams with enough cap room, applies those market signings back into Armchair GM state, and reconciles non-user team cap space from rolled rosters after Cup Run year advancement. Touched `app/lib/free-agency.ts`, `app/lib/cup-run.ts`, `app/armchair-gm/page.tsx`, `__tests__/free-agency.test.ts`, `__tests__/cup-run.test.ts`, `docs/TASKS.md`.
- 2026-07-06: **NHL EDGE usage/presentation hardening.** Made EDGE high-danger luck visible as `EDGE HD` / `NHL EDGE HD`, threaded `hdFinishingDelta` into Players local Player Card and contract projection NAV calls, and added a source canary covering snapshot capture, roster join, xNAV/rollover use, and UI labels. Touched `app/players/page.tsx`, `app/components/PercentileCard.tsx`, `app/armchair-gm/page.tsx`, `__tests__/feature-canaries.test.ts`, `docs/TASKS.md`.
- 2026-07-06: **Cup Run later-year draft summary + re-sign handoff.** Year 2/3 Cup Run rollovers now expose the generated draft class, show a future-draft summary popup, and then open the Re-Sign phase; if the summary is unavailable, the offseason falls straight into re-signing so flagged 0-year contracts cannot remain stale on the user's roster. Touched `app/lib/cup-run.ts`, `app/armchair-gm/page.tsx`, `__tests__/cup-run.test.ts`, `__tests__/feature-canaries.test.ts`, `docs/TASKS.md`.
- 2026-07-06: **Full-roster sim projections, lineup-as-starters, pedigree Best Lines.** The sim now projects a season for every skater on each team (was top 18 with stats filters, capped): when a lineup is set, dressed players are starters (existing deployment floors/multipliers) and everyone outside it projects as press-box depth (≤18-42 GP, ×0.85 usage). Best Lines no longer ranks purely by NAV — on-ice rank blends 55% NAV with 45% coach-deployment trust (career TOI) plus a small tenure nudge, so a negative-NAV captain (Lowry-type: heavy contract, heavy minutes) still slots high while the trade machine keeps valuing him honestly. Touched `app/api/simulate/route.ts`, `app/components/LineupEditor.tsx`.
- 2026-07-06: **EDGE into X-NAV + analytics page; FA market display facts.** (1) `calcSkaterNAV` gains a bounded EDGE luck regression: `hdFinishingDelta` (high-danger finishing vs league from nhl_snapshots) adjusts offense −8…+10 NAV — hot finishers discounted, unlucky ones credited (pinned in xnav-team-control tests). (2) Player Analytics stat tiles gain "HD LCK" showing the delta as ±x.x%. (3) FA-class seed rows carried age 0/capHit 0 → Kane showed age 27 / "was $0.00M": new `FA_KNOWN_FACTS` curated map (ages + expiring AAVs for the notable teamless FAs) joins at injection, ResignPhase hides the "was" chip when unknown. Kane now prices as a 37-year-old (~$5M short-term), not a 27-year-old on a 7-year ask. Touched `app/lib/xnav-engine.ts`, `app/players/page.tsx`, `app/lib/free-agent-seed.ts`, `app/lib/roster-assembly.ts`, `app/components/ResignPhase.tsx`, `__tests__/xnav-team-control.test.ts`.
- 2026-07-06: **Prod cron feedback fixes: snapshot failures + FA-pool min-contract exploit.** First prod cron run stored 160 snapshots but flagged 17 "failures" — players with no current-season NHL stat line (rookies/no-GP); `parseLanding` now requires only identity (playerId/position/birthDate) and zeroes missing stats, while the health-check canary keeps the full strict path list for drift detection. Separately, FA-pool injected veterans (Tarasenko, Giroux, Klingberg…) priced at league-min because seed rows carry position "Unknown" (truthy — the `|| "C"` fallback never fired): the injection now infers position (goalie stats → G, ≥19 TOI with modest scoring → D, any skater stats → W), and `projectFreeAgentContract` gained a safety net pricing unknown-position skaters off their pace instead of capMin. Touched `app/lib/nhl-player-feed.ts`, `app/lib/roster-assembly.ts`, `app/lib/free-agency.ts`, `__tests__/nhl-player-feed.test.ts`.
- 2026-07-06: **NHL feed full integration — cron, health card, luck signal in the engine, UI.** (1) `vercel.json` cron hits new `/api/cron/nhl-feed` nightly (CRON_SECRET bearer auth or admin session); 4 teams per run on an 8-day rotation, shared `app/lib/nhl-feed-capture.ts` powers both cron and admin sync. (2) Admin → Health gains an "NHL FEED" card: probe both endpoints with drift-field readout, per-team capture button, snapshot count. (3) `latestEdgeLuckMap` joins the newest edge snapshot per player onto rosters in roster-assembly (`Asset.hdFinishingDelta`), and `breakoutOdds` prefers the EDGE high-danger finishing delta over the xG heuristic (≤−2% → +8% breakout; ≥+3% → +8% regression). (4) Ledger Line expanded rows show "HD Finish ±x.x% vs league" colored by luck direction. Verified locally: cron 401s without auth, authenticates with bearer, rotates ANA/BOS/BUF/CAR on cycle day 0, degrades gracefully when the NHL API is unreachable; snapshot write + latest-wins read E2E against local.db. Suite 413 green. Touched `app/lib/nhl-feed-capture.ts` (new), `app/api/cron/nhl-feed/route.ts` (new), `vercel.json` (new), `app/api/admin/nhl-feed/route.ts`, `app/admin/health/page.tsx`, `app/lib/roster-assembly.ts`, `app/lib/trade-types.ts`, `app/lib/season-rollover.ts`, `app/armchair-gm/page.tsx`, `__tests__/season-rollover.test.ts`.
- 2026-07-06: **First-party NHL player feed (landing + EDGE).** New pipeline against the endpoints Brady captured from NHL site traffic: `app/lib/nhl-player-feed.ts` fetches/parses `/v1/player/{id}/landing` (identity, season lines, career, draft) and `/v1/edge/skater-detail/{id}/{season}/2` (shot locations, zone time, speed), extracting the high-danger finishing delta vs league — the true luck signal for breakout bias. Snapshots persist to a new `nhl_snapshots` table (one row per player/season/source/day, key columns + full raw payload for future mining; self-provisioning via ensure-schema). `/api/admin/nhl-feed`: GET = source health check probing a canary player and reporting any missing required field (catches upstream v1→v2 drift); POST `{ team }` or `{ ids }` = capture up to 40 players per call with bounded concurrency. 3 parser/drift tests on real captured shapes. Touched `app/lib/nhl-player-feed.ts` (new), `app/api/admin/nhl-feed/route.ts` (new), `app/db/schema.ts`, `app/db/ensure-schema.ts`, `__tests__/nhl-player-feed.test.ts` (new).
- 2026-07-06: **FA projection market recalibration (post-Carlsson offer sheet).** `projectFreeAgentContract` now pays ascending stars for their prime: under-24 scoring pace projects forward 12%/yr (capped +45%) before pricing, so a Bedard-tier 21yo RFA lands in Kaprizov range (~$15-17M) on max term (RFAs ≥$10M lock 8 years) instead of ~$10M — no more bargain generational talents. Workload-only defensemen stop projecting like stars: `dToiPerMin` 0.60→0.42 (a 21-minute defensive D now asks ~$5.5M, not $7.5M) while elite minutes+points D still clear $11M+. 5 calibration pins in `__tests__/fa-projection-market.test.ts`. Touched `app/lib/free-agency.ts`.
- 2026-07-06: **Cup Run resume guard.** A saved ACTIVE run is never restored silently on page load anymore — the rolled league lives only in React state, so a reloaded mid-run session was a fresh 2026 league wearing Year-2 flags (offseason popups gated off, pending RFAs stuck at 0x0, confusing NAVs). A blocking modal now offers Resume (only for Year 1 pre-rollover, with the retention ledger reset since trades died with the tab) or Abandon (forced for Year 2+, with an explanation). Abandon paths clear the localStorage key explicitly. Touched `app/armchair-gm/page.tsx`.
- 2026-07-06: **Testing-feedback batch 2 — lineups, breakdowns, accessibility.** LineupEditor gains a Best Lines button (orders every unit by X-NAV, C-column pattern preserved) and hand-set lineups now lock through trades — roster changes merge (departed players drop, arrivals go to the bench) instead of resetting the sheet. The Ledger Line rows expand on click into a valuation breakdown: Offense/Defense/Age/Contract/Upside NAV component bars plus FMV vs cap hit, tier, and expected-vs-actual points. Season-results typography bumped for accessibility (tables 6.5-9px → 8-11px, StatCell labels 6→8px). Touched `app/components/LineupEditor.tsx`, `app/armchair-gm/page.tsx`.
- 2026-07-06: **Testing-feedback batch 1 — Cup Run correctness + same-name identity.** (1) Rollover flags all run-out contracts (incl. stale 0-year rows — the Nino case) so `resolveLeagueOffseason` moves them next offseason. (2) Cup Run years 2-3 no longer replay the 2026 Draft Night; rollover drafts from actual standings and `app/data/future-draft-classes.ts` accepts real 2027/2028 prospects (synthetic filler beyond). (3) Claude recap suppressed mid-run; fires once at run end with a `cupRunStory` schema+prompt section narrating the whole 3-season arc. (4) Elias Pettersson collision: seed inserts position-salted rows per `Name__POS` contract variant; `trade_block.position` column added (ensure-schema), admin sends it, block application prefers `Name__POS` keys. Touched `app/lib/cup-run.ts`, `app/lib/synthetic-draft.ts`, `app/data/future-draft-classes.ts` (new), `app/armchair-gm/page.tsx`, `app/armchair-gm/useSimDispatch.ts`, `app/api/claude/route.ts`, `app/db/schema.ts`, `app/db/ensure-schema.ts`, `app/db/seed.ts`, `app/api/admin/trade-block/route.ts`, `app/admin/trade-block/page.tsx`, `app/lib/roster-assembly.ts`, `__tests__/cup-run.test.ts`.
- 2026-07-06: **Season box score → "The Ledger Line" analytics table.** The full-roster season table now joins each player's sim line against the valuation engine via new `players`/`navMap` props threaded into `SeasonResultsPager`: per-player X-NAV, NOIV impact (±, colored), CAP± (fair-market AAV minus effective cap hit), and ΔXP (points vs preseason pace scaled to games played, with ▲/▼ season-arc markers folded in); player cells carry a POS · AGE · ARCHETYPE agate subline and a legend explains the columns. Crease table gains X-NAV and CAP± for the starter, and GSAX now renders rounded to two decimals (was a raw float like +5.539999999999992). Touched `app/armchair-gm/page.tsx`.
- 2026-07-06: **X-NAV: growth-adjusted surplus + team-control option value.** Fixed the structural undervaluation of pre-peak players on long deals (user report: Stankoven-type 23yo at 41pts on 6×8 ≈ Perfetti+Koepke for Stankoven+1st looked fair to the engine). Two bounded changes inside `calcSkaterNAV`: (1) the multi-year surplus loop now drifts trueMarketValue along the already-audited age curve — up to +9%/yr (gated by `youthProjectionSignal`) through the peak age, −3%/yr from peak+2, cumulative clamp [0.70, 1.35] — so a 23-year-old's years 2–6 price as prime seasons and an aging 8-year deal prices its decline; (2) a Team-Control Option Value term (youth signal × control years overlapping the growth window × 6 × capEstablishment, ≤ ~36 NAV) prices the asymmetry that a long deal caps downside at a known cap hit while capturing breakouts free. Probe case: 23yo 41pts 6×8 went 46→69 (cap −17→+9); his 1-yr rental 37→40; same profile at age 30 16→15. Suite green at 403 (5 new ordering pins in `__tests__/xnav-team-control.test.ts`), no existing valuations broke. Touched `app/lib/xnav-engine.ts`, `__tests__/xnav-team-control.test.ts` (new).
- 2026-07-06: **Franchise-style season box score in Team Numbers.** The Season Results team panel now expands (open by default) into a full-roster stat table — rank, player (with R rookie chip), pos, age, GP, G, A, PTS, and ▲/▼ breakout/regression markers — plus a Crease table for the starter (GS, GAA, SV%, GSAX). Pure presentation over the `projectedSkaters`/`goalie` data the sim response already carried. Touched `app/armchair-gm/page.tsx`.
- 2026-07-06: **Cup Run Challenge Phases 2–4 — playable 3-year mode.** `app/lib/cup-run.ts`: run state machine (start → record season → WON/FIRED), `rollLeagueForward` (scenery detection → advanceSeason → synthetic draft → AI cap-legality pass that never cuts goalies or breaks lineup minimums → roster repair), cross-season retention ledger (50% max, 3 slots occupied for the retained term, 15%-of-cap aggregate) enforced inside `executeTrade`, difficulty stars from phase/standing, share card. `app/lib/synthetic-draft.ts`: seeded 32-pick future classes (worst-first order, pedigree-shaped NHLe pace). `app/lib/lineup-context.ts`: slot multipliers + `computeChangeOfScenery`; `/api/simulate` applies slot weighting only when `lineupContext: true` so the classic sim is byte-identical. `CupRunPanel` HUD on Armchair GM with localStorage persistence; advancing a year resets trades/lineups/offseason and re-resolves FA against the rolled league. Verification: 17 new tests in `__tests__/cup-run.test.ts` incl. X-NAV consistency checks; suite 398 green; build clean; page smoke-tested. Touched `app/lib/cup-run.ts` (new), `app/lib/synthetic-draft.ts` (new), `app/lib/lineup-context.ts` (new), `app/components/CupRunPanel.tsx` (new), `app/armchair-gm/page.tsx`, `app/armchair-gm/useSimDispatch.ts`, `app/api/simulate/route.ts`, `app/lib/season-rollover.ts`, `docs/futures/CUP_RUN_CHALLENGE.md`.
- 2026-07-06: **Cup Run Challenge Phase 1 — season rollover engine.** New pure lib `app/lib/season-rollover.ts`: `advanceSeason()` ages the league one offseason (aging, contract decrement, seeded retirement ramping after 35 with a 2-year-later goalie clock, stat regeneration via `stablePts` × `ageDecay`, and breakout/regression rolls — 8%/10% base, age-biased, with an xG-vs-goals "unlucky finisher" bias from existing MoneyPuck `xGPace`/`goalsPace` and a doubled-odds change-of-scenery hook for Phase 3). Design doc at `docs/futures/CUP_RUN_CHALLENGE.md` maps all four phases onto existing machinery and notes NHL Edge shot-location endpoints (documented in coreyjs/nhl-api-py; called directly from TS) as a future luck-signal enrichment. Verification: 15 new tests in `__tests__/season-rollover.test.ts`, full suite green. Touched `app/lib/season-rollover.ts` (new), `__tests__/season-rollover.test.ts` (new), `docs/futures/CUP_RUN_CHALLENGE.md` (new).
- 2026-07-06: **Press Box "Back Issues" archive calendar.** NYT-crossword-style month grid below the game — one linked cell per hand since epoch; green+star = perfect, ink = finished, amber = in progress, outline = open, red ring = today; played/perfect tally in the header. Reads the same localStorage saves the game writes and refreshes when the current hand ends. Touched `app/press-box/Calendar.tsx` (new), `app/press-box/page.tsx`.
- 2026-07-06: **Press Box player headshots.** `/api/press-box/pool` now overlays NHL mugshot URLs by fetching the 32 live roster endpoints (best-effort, 1h cache); cards render a sepia circular mug with flag-emoji fallback on missing/failed images. Verified deal→score→retry flow at 375px. Touched `app/api/press-box/pool/route.ts`, `app/press-box/page.tsx`, `app/lib/press-box-engine.ts`.
- 2026-07-06: **Press Box live pool, peg-board feedback, playing-card redesign.** New `/api/press-box/pool` overlays team/age/draft year from the synced players table onto the curated pool (curated file keeps identity + serves as fallback; null DB team means "unknown", only a valid tricode overrides; retired/excluded rows drop). Replaced the exact "Target: N/15 (X pts away)" readout with a cribbage peg board (best/last pegs vs red target hole) and "X/4 cards in the perfect lineup" via new `findOptimalCombos`/`overlapWithOptimal` engine helpers. Cards rebuilt as 5:7 newspaper playing cards with mirrored corner indices and rubber-stamp overlays. Touched `app/api/press-box/pool/route.ts` (new), `app/lib/press-box-engine.ts`, `app/data/press-box-pool.ts`, `app/press-box/page.tsx`, `__tests__/press-box-engine.test.ts` (new).
- 2026-07-06: **Press Box pool freshness + Wordle-ification (recap of 07-03…07-05 work on this branch).** Pool updated for 2026 offseason moves (12 team changes, 4 removals, 4 additions, 2 name fixes); multi-attempt mechanic added — 5 attempts, call-up hidden on the first, Wordle-style share blocks, attempt history, v2 save format with v1 restore. Teamless FA-pool DB entries now inject into the armchair GM offseason market (`FA_POOL` virtual team). Touched `app/data/press-box-pool.ts`, `app/lib/press-box-engine.ts`, `app/press-box/page.tsx`, `app/lib/roster-assembly.ts`, `app/lib/free-agency.ts`, `__tests__/feature-canaries.test.ts`, `__tests__/derive-contract-status.test.ts`.
- 2026-07-02: Extracted Armchair GM simulation dispatch into `app/armchair-gm/useSimDispatch.ts`, added a source canary for the split, and marked CLAUDECONCERNS CC1 complete. Touched `app/armchair-gm/page.tsx`, `app/armchair-gm/useSimDispatch.ts`, `__tests__/feature-canaries.test.ts`, `docs/CLAUDECONCERNS.md`, `docs/DEVNOTES.md`.
- 2026-07-02: Added a source canary keeping the dead `ContractSyncer` component and obsolete `/api/contracts` route retired; marked CODEXAUDIT item 6 complete. Touched `__tests__/feature-canaries.test.ts`, `docs/CODEXAUDIT.md`, `docs/DEVNOTES.md`.
- 2026-07-02: Updated the admin dashboard canary to keep the retired `/admin/fa-overrides` path out while Contract Admin owns FA player-row facts; marked CODEXAUDIT item 5 complete. Touched `__tests__/feature-canaries.test.ts`, `docs/CODEXAUDIT.md`, `docs/DEVNOTES.md`.
- 2026-07-02: Confirmed the admin auth canary recursively discovers every `app/api/admin/**/route.ts` file and checks each exported handler for `requireAdmin(req)`; marked CODEXAUDIT item 4 complete. Touched `docs/CODEXAUDIT.md`, `docs/DEVNOTES.md`.
- 2026-07-02: Added route-level regression coverage for Contract Admin `EDITOR → SYNC`, proving editor rows reset to sync, curated FA/exclude flags clear, and roster/team caches invalidate; marked CODEXAUDIT item 3 complete. Touched `__tests__/contracts-source-reset.test.ts`, `docs/CODEXAUDIT.md`, `docs/DEVNOTES.md`.
- 2026-07-02: Added route-level regression coverage proving admin team overrides and broad cache flushes clear cap-specific `/api/league/teams` cache keys, including the active cap setting; marked CODEXAUDIT item 2 complete. Touched `__tests__/team-cache-routes.test.ts`, `docs/CODEXAUDIT.md`, `docs/DEVNOTES.md`.
- 2026-07-02: Added route-level regression coverage proving `/api/league/teams` surfaces draft-pick ownership overrides in the main trade UI payload; marked CODEXAUDIT item 1 complete. Touched `__tests__/league-teams-route.test.ts`, `docs/CODEXAUDIT.md`, `docs/DEVNOTES.md`.
- 2026-06-30: Increased Team Strand trade-delta chip typography to 11px for readability; touched `app/components/TeamStrand.tsx` and `__tests__/feature-canaries.test.ts`.
- 2026-06-30: Added per-side win/even/loss trade verdict reads and Team Strand pre/post trade delta chips, including the focused Trade Machine page, so forward-for-defense need trades can show both teams' gains; touched `app/api/evaluate/route.ts`, `app/lib/trade-types.ts`, `app/components/VerdictPanel.tsx`, `app/components/TeamStrand.tsx`, `app/components/QuickTradeMachine.tsx`, `app/armchair-gm/page.tsx`, `__tests__/evaluate-route.test.ts`, `__tests__/feature-canaries.test.ts`, and `docs/TASKS.md`.
- 2026-06-29: Updated the simulation path for the 2026-27 season contract: `/api/simulate` now returns `season: "2026-27"`, values drafted-rookie profiles through `prospectPtsPace`/`draftOverall`, and applies the user-edited lineup order to team-strength, skater deployment, and starter goalie selection. Armchair GM now sends full forward/defense/goalie lineup orders from `LineupEditor` into the sim request and merges newly added offseason players from live `db.players` into the original sim baseline so Draft Night rookies are included without double-applying executed trades. Added behavior tests for dressed McKenna/Stenberg-style rookies and lineup-sensitive standings, plus a source canary for the lineup payload. Verification: `npx tsc --noEmit` clean; `npm run test` 354 passed across 24 files. Touched `app/api/simulate/route.ts`, `app/lib/sim-engine.ts`, `app/components/LineupEditor.tsx`, `app/armchair-gm/page.tsx`, `__tests__/simulate-and-claude-routes.test.ts`, `__tests__/feature-canaries.test.ts`.
- 2026-06-29: Removed the front-facing `ADMIN ->` link from the Trade Block modal so public UI no longer links into `/admin/trade-block`; added a source canary to keep the link out. Touched `app/components/TradeBlockPanel.tsx`, `__tests__/feature-canaries.test.ts`.
- 2026-06-29: Implemented `docs/CODEXAUDIT.md` suggested fix order: shared draft-pick inventory now applies DB pick ownership overrides in `/api/league/teams`; shared team-cache clearing covers cap-specific cache keys across admin mutations; Contract Admin adds an `EDITOR → SYNC` bulk provenance reset; admin auth canary auto-discovers all admin API routes; deprecated FA override API now returns 410 and the dead `ContractSyncer` was removed. Added `__tests__/draft-pick-inventory.test.ts` and updated source canaries. Verification: `npx tsc --noEmit` clean; `npm run test` 350 passed across 24 files. Touched `app/lib/draft-pick-inventory.ts`, `app/lib/team-cache.ts`, league/admin routes, `app/admin/contracts/page.tsx`, `__tests__/*`, and `app/lib/free-agent-seed.ts`.

- 2026-06-29: **Enabled goalie STRAND tab on both Player Analytics and Armchair GM.** Goalies were excluded from the STRAND visualization tab by explicit position guards (`hasStrand = !isG` in `app/players/page.tsx`, `asset.position !== "G"` in three places in `app/components/AssetCard.tsx`). Removed all four guards so goalies now display their GSAX/SV%/HDSV/WRKLD strand alongside skaters. Also removed the goalie exclusion from AssetCard's compare dropdown so goalie-to-goalie STRAND comparison works. Touched `app/players/page.tsx`, `app/components/AssetCard.tsx`.
- 2026-06-29: **Updated 2026 NHL Draft with actual first-round results.** The draft was played on 2026-06-28; updated `app/data/draft-2026.json` with corrected pick ownership from draft-night trades (15 picks changed hands vs the pre-draft order — ANA got #15 via DET/STL, UTA got #17 via LAK, SJS got #21 via PHI, DET got #23 via BOS/UTA, MTL got #26 via VGK/NYR/DAL/CAR, PHI got #27 via SJS/BUF, etc.). Updated `app/lib/draft-2026.ts` DRAFT_2026_PROSPECTS to match actual selections in draft order — reordered existing prospects and added 9 new players not in the pre-draft board (Wyatt Cullen, Alexander Command, Maddox Dagenais, Liam Ruck, Jonas Lagerberg Hoen, Gleb Pugachyov, Maksim Sokolovskii, Tommy Bleyl, Jaxon Cover); removed 9 prospects who were not taken in the first round. Stats for new players are estimated from draft-year league context. Changed `pickState` from `"fut"` to `"complete"` and `source` to `"actual"`. Touched `app/data/draft-2026.json`, `app/lib/draft-2026.ts`.
- 2026-06-29: **Added goalie STRAND trait builders.** The `FullStrand` component in `app/players/page.tsx` and `buildGoalieTraits` in `app/components/StrandView.tsx` now detect `position === "G"` and return goalie-specific traits (GSAX normalized -15–25, SV% normalized 0.890–0.935, HDSV from `baselineHdsvPct` normalized 0.780–0.880, WRKLD from games started normalized 10–65) instead of meaningless skater metrics. HDSV is marked `unavailable` on Player Analytics since `baselineHdsvPct` isn't exposed there. Strand type for goalies is set to `"GOALTENDER"`. Also fixed null `xGPace` displaying as "0" by checking `xGPace != null` and marking unavailable when null. Touched `app/players/page.tsx`, `app/components/StrandView.tsx`.
- 2026-06-29: **Added onboarding for new testers: welcome modal, inline tooltips, methodology page.** (1) `app/components/WelcomeModal.tsx` — first-visit welcome overlay using localStorage `"hockey-ledger-welcomed"` key; introduces X-NAV, STRAND, GM Audit, FMV with brief definitions; "Enter the Ledger" dismiss button; click-outside-to-dismiss. Rendered in `app/layout.tsx`. (2) `app/components/MetricTip.tsx` — hover/touch tooltip component with 25+ term glossary (GLOSSARY record); dotted underline on terms; dark tooltip with cream text; 150ms leave delay. Applied to X-NAV and FMV labels in `app/components/PercentileCard.tsx`. (3) `app/methodology/page.tsx` — dedicated methodology reference page importing `iconKey` and `methodologySections` from Footer; all content expanded (no collapsible sections); table of contents with anchor links; newspaper aesthetic. (4) Updated `app/components/Footer.tsx` to export `iconKey` and `methodologySections` arrays and linked "Methodology"/"Glossary" text to `/methodology` and `/methodology#strand-glossary`. Touched `app/components/WelcomeModal.tsx` (new), `app/components/MetricTip.tsx` (new), `app/methodology/page.tsx` (new), `app/layout.tsx`, `app/components/Footer.tsx`, `app/components/PercentileCard.tsx`.
- 2026-06-28: **Fixed Re-Sign Phase layout breaking on mobile.** Flexbox layout in the Re-Sign modal was overflowing on narrow screens. Fixed spacing, wrapping, and button sizing so the UFA/RFA re-sign controls work properly on mobile. Touched `app/components/ResignPhase.tsx`.
- 2026-06-28: **Added games-based confidence scaling to DEF display xGA weight.** Defensive display value for low-sample players now scales xGA suppression weight by games played, so a 10-game sample doesn't get full weight. Touched `app/components/PercentileCard.tsx`.
- 2026-06-28: **Replaced hardcoded player awards with CSV-derived data.** Moved historical award data (Hart, Norris, Vezina, Selke, Calder, Byng, Art Ross, Rocket Richard, Conn Smythe, etc.) from a hardcoded map to a CSV-sourced build, covering all winners through the 2025-26 season. Touched `app/lib/player-data.ts`, `app/data/byng_winners.csv` (new), `scripts/build-awards.ts`.
- 2026-06-28: **Added team card section with advanced analytics breakdown and historical awards.** New bar-graph breakdown of player advanced analytics per team card, plus historical award badges surfaced from the CSV data. Significant refactor across 17 files. Touched `app/players/page.tsx`, `app/lib/player-data.ts`, and 15 other files.
- 2026-06-28: **Fixed FMV display showing $0.0M.** `fmvAav` was already in millions but was being divided by 1M again, producing $0.0M. Fixed the unit mismatch. Touched `app/components/PercentileCard.tsx`.
- 2026-06-28: **Added plum-styled analysis tabs to Armchair GM asset cards.** Unified the STATS/STRAND/TIMELINE/DEV tab styling on the Armchair GM page with the same plum accent treatment used on Player Analytics. Touched `app/armchair-gm/page.tsx`.
- 2026-06-28: **Fixed DEF display for defensemen by blending DPS into defensive display value.** Defensemen with DPS data now show a blended defensive value (DPS×15 weighted with xGA suppression) instead of raw xGA only. Touched `app/components/PercentileCard.tsx`.
- 2026-06-28: **Added position median reference lines to percentile card bars.** Each percentile bar now shows the median value for that position group as a reference marker, making it clearer whether a player is above or below average. Touched `app/components/PercentileCard.tsx`.
- 2026-06-28: **Refactored expanded player panel into tabbed navigation with plum accent.** Replaced the flat expanded-row layout with a tabbed interface (Stats, Strand, Player Card, Contract, Outlook) using the PLUM accent color for active tabs and borders. Touched `app/players/page.tsx`.
- 2026-06-28: **Fixed defensive D valuation, injured player blending, and added percentile player cards.** Increased shutdownDAdj ceiling from 18→28 so elite shutdown D-men get proper credit. Added TOI-based defensive floor (+1.8/min above 20 TOI, max +12) for heavy-minutes D. Dynamic baseline blending: 80% baseline when games < 30 (injury) vs 60% normally. New `PercentileCard` component: JFresh-style position-relative percentile bars with X-NAV breakdown. Touched `app/components/PercentileCard.tsx` (new), `app/lib/xnav-engine.ts`, `app/players/page.tsx`.
- 2026-06-28: **Removed standalone offer-sheets page and fixed UFA re-sign skip bug.** The offer sheets page was merged into the Armchair GM offseason flow instead of being a separate route. Fixed a bug where skipping a UFA re-sign would break the flow. Touched `app/offer-sheets/page.tsx` (removed), `app/components/ResignPhase.tsx`, and 4 other files.
- 2026-06-28: **Added JS-based scroll snap for newspaper-to-viewport alignment.** The newspaper-on-desk home page now snaps cleanly to viewport edges during scroll, giving a polished page-turning feel. Touched `app/page.tsx`, `app/globals.css`.
- 2026-06-28: **Achieved 60 FPS scroll performance on home page (was 16 FPS).** Moved heavy CSS effects (box shadows, transforms) off the compositor path, replaced scroll-triggered repaints with GPU-composited layers, and reduced paint complexity. Touched `app/page.tsx`, `app/globals.css`.
- 2026-06-28: **Capped trade panel asset list height to prevent excessive page scrolling.** Long asset lists in trade panels now have a max-height with internal scroll, keeping the overall page layout manageable. Touched `app/components/TradePanel.tsx`.
- 2026-06-28: **Mobile styling adjustments.** Various mobile-specific layout fixes. Touched `app/globals.css`.
- 2026-06-26: **Added RFA offer sheet phase to Armchair GM offseason flow.** After the Re-Sign phase, teams now enter an RFA offer sheet phase where they can match or let walk restricted free agents, with CBA Article 10.3 compensation pick tiers displayed. Extended draft pick generation to include 2029-2030 rounds for offer sheet compensation. Touched `app/components/OfferSheetPhase.tsx` (new), `app/components/ResignPhase.tsx`, `app/armchair-gm/page.tsx`, `app/api/league/route.ts`, `app/lib/free-agency.ts`.
- 2026-06-26: **Added RFA offer sheet page and extended draft picks to 2029-2030.** Standalone offer sheet page for RFA scenarios. Extended the generated draft pick inventory from 2027-2029 to 2027-2030 so offer sheet compensation picks in the 4th and 5th years are available. Touched `app/offer-sheets/page.tsx` (new), `app/api/league/route.ts`, and 4 other files.
- 2026-06-26: **Reduced scroll animation jank by moving shadow off the compositor.** The newspaper-stack shadow effect was causing repaint storms on scroll. Moved it to a GPU-composited pseudo-element. Touched `app/globals.css`.
- 2026-06-26: **Exaggerated newspaper stack with 7 visible page layers and loose sheets.** The home page desk effect now shows 7 stacked newspaper pages with slight rotation/offset for a more dramatic broadsheet effect. Touched `app/page.tsx`, `app/globals.css`.
- 2026-06-26: **Restored scroll-driven newspaper weight, removed floating cards and glass.** Reverted the hero entrance from framer-motion floating cards and glass accents back to the pure-CSS newspaper-on-desk scroll reveal that users preferred. Touched `app/page.tsx`, `app/globals.css`.
- 2026-06-26: **Fixed LedgerToaster and motion hydration mismatches.** Deferred portal rendering and motion component mounting to after hydration to prevent React hydration warnings from server/client HTML mismatches. Touched `app/components/LedgerToaster.tsx`, `app/page.tsx`.
- 2026-06-26: **Added premium hero entrance animations with staggered fade-up and floating glass accents.** Initial framer-motion hero animation attempt with staggered reveals and glass-morphism cards. Later reverted in favor of pure-CSS newspaper approach. Touched `app/page.tsx`, `app/globals.css`, `package.json`, `package-lock.json`.
- 2026-06-25: **Admin: repainted Contract Admin to the light-paper theme.** The admin contracts page now uses the newspaper/ledger aesthetic instead of the old dark-mode styling. Touched `app/admin/contracts/page.tsx`.
- 2026-06-25: **Draft Night: sign selections to a 3-year ELC so rookies join rosters.** After the draft simulation, each selected prospect is automatically signed to a 3-year entry-level contract and added to their drafting team's roster as a tradeable asset. Added `app/lib/draft-rookies.ts` (new) for `draftedRookieAssets()` conversion. Touched `app/lib/draft-rookies.ts`, `app/components/DraftNight.tsx`, `app/armchair-gm/page.tsx`.
- 2026-06-25: **Draft Night: source first-round pick order from official NHL data.** Added `scripts/build-draft-board.ts` to fetch the 2026 first-round pick order from the NHL API. Added `app/data/draft-2026.json` (32 picks with team/originalTeam), `app/lib/draft-2026.ts` (DRAFT_2026_ORDER + hand-curated DRAFT_2026_PROSPECTS board of 32 ranked prospects with stats), and `app/components/DraftNight.tsx` (interactive draft simulation with PICK MODE + LOG MODE). Touched `scripts/build-draft-board.ts` (new), `app/data/draft-2026.json` (new), `app/lib/draft-2026.ts` (new), `app/components/DraftNight.tsx` (new).
- 2026-06-25: **Home page CSS styling iterations.** Multiple commits adjusting the newspaper-on-desk home page feel — changed shadows, perspectives, and layer positioning to feel more 2D/flat paper. Touched `app/page.tsx`, `app/globals.css`.
- 2026-06-29: **Redesigned STRAND helix to pure z-order 3D crossover with no breaks.** Went through 5 iterations based on user feedback: (1) replaced crossover gap effect with section-based 3D helix depth rendering in `StrandDisplay.tsx` and `TeamStrand.tsx`; (2) removed node dots/circles — switched from individual `<line>` segments to continuous `<path>` elements to eliminate visible junction dots; (3) tried knockout border technique for depth separation, but user's GF didn't like the visible breaks; (4) removed knockout layer entirely — final approach uses pure z-order layering (back sections render first at opacity 0.9/strokeWidth 2.5, front sections render on top at same weight), no breaks, no opacity dimming. Crossing positions calculated as `W * (1 + 2k) / (4 * sineMultiplier)`. `cos(θ)` at section midpoint determines which strand is in front. (5) Increased label offsets from strand lines to prevent overlap (StrandDisplay: 10→18px above, 14→20px below; TeamStrand: 8→14px above, 14→18px below). Touched `app/components/StrandDisplay.tsx`, `app/components/TeamStrand.tsx`.
- 2026-06-25: **Orthogonal backend — the `players` table is the single source of truth for contract + FA facts.** Replaced the five-layer read-time merge (NHL API → CapWages scrape → `contracts.bundled.json` → `free-agent-seed.ts` → DB `fa_overrides`, resolved on every roster assembly) with a clean join: `rostered players ⨝ DB contract row ⨝ live stats`. Reads no longer scrape — so test and live environments resolve identical data. Ingestion is now write-time only. **Schema:** `players` gains `expiry_status`, `expiry_year`, `exclude_from_roster`, `source` (`seed`|`sync`|`editor`). **Seed:** committed `app/data/league-seed.json` (build with `npm run build:seed` from bundled.json + the 2026 FA seed); `seedPlayersTable()` loads it idempotently, auto-seeds an empty table, reseeds on admin reset, and never clobbers `source='editor'` rows. **Read:** `loadContracts()` → `loadContractsFromDB()` in `roster-assembly.ts` (DB-only; bundled.json kept only as a hard DB-error fallback); FA status derives from stored facts via the new pure `deriveContractStatus()`; `excludeFromRoster` removes a player; `contractMissing` flags placeholder deals. **Write:** Sync (`PUT /api/admin/contracts`) stamps `source='sync'`, skips editor rows, and only fills a NULL expiry (curated FA marks survive a refresh); the editor (`POST`) stamps `source='editor'`; `POST /api/admin/seed` = Load Baseline; `POST /api/admin/fa-bulk` = bulk FA class onto player rows. **Admin:** Contract Admin shows provenance + FA, edits FA/expiry/exclude inline, and adds Load Baseline / Sync Live / Needs-data filter / Bulk Free Agents; the old `fa-overrides` page is a signpost (the `fa_overrides` table is left in place but unused by reads). The Nyquist 2026-UFA fix now lives in the seed as data. Tests: `__tests__/league-seed.test.ts`, `__tests__/derive-contract-status.test.ts`, updated `league-players-route` (table-aware DB mock) and source canaries. Touched `app/db/schema.ts`, `app/db/ensure-schema.ts`, `app/lib/roster-assembly.ts`, `app/lib/league-seed.ts`, `app/lib/trade-types.ts`, `scripts/build-league-seed.ts`, `app/api/admin/{contracts,seed,fa-bulk,reset}/route.ts`, `app/admin/contracts/page.tsx`, `app/admin/fa-overrides/page.tsx`. NOTE: contracts are Redis-cached 23h — clear the contracts/league cache from admin after a sync/seed/edit for changes to take effect on a warm cache.
- 2026-06-24: Fixed the "league-minimum 1-year" phantom contract (WPG Gustav Nyquist showed $0.93M/1yr instead of his real $3.25M/1yr). Root cause: `loadFromDB()` only falls back to `contracts.bundled.json` when the entire DB read throws — bundled was never a *per-player* fallback. So a player missing from both the live CapWages scrape (which 403s datacenter IPs and drops players in the off-season) and the synced `players` table had no contract match (`fin` null) and the build loop applied its `fin?.capHit ?? 0.925` / `?? 1` placeholder, ignoring his known bundled deal. Fix: fold `contracts.bundled.json` into `loadContracts()` as a lowest-priority per-player fallback (below DB and scrape, keyed by base name so the loop's `CONTRACTS[p.name]` lookup hits it) — players absent from scrape and DB now get their last-known curated contract instead of the placeholder. Verified Nyquist resolves to 3.25/1 against the real bundled file; all 332 tests green. Touched `app/lib/roster-assembly.ts`. NOTE: contracts are Redis-cached 23h — clear the contracts/league cache from admin Settings for the fix to take effect on an already-warm cache.
- 2026-06-24: Fixed the empty off-season free-agent list with a curated 2026 FA seed. Root cause: in the off-season the live CapWages scrape (`/players/active`) does not reliably surface 2026-expiring contracts — once free agency rolls over, active rows show the next deal (2027+ expiry) or drop the player entirely, and CapWages also 403s datacenter IPs — so no contract reaches roster assembly with `expiryYear <= 2026` and auto-detection finds zero pending FAs. An admin DB reset + re-sync makes it worse: the `loadFromDB` backfill that marked dropped players as 2026 UFAs loses its rows, and the re-sync only re-adds players the scrape still lists. Added `app/lib/free-agent-seed.ts` (the 2026 UFA/RFA class from the PuckPedia list in TASKS.md — 30 UFA + 25 RFA, matched case/accent-insensitively) and wired it into `assembleCanonicalRoster` as the lowest-precedence FA marker: DB `fa_overrides` win, then live scrape, then the seed fills the rest. Seed-marked players get `expiresThisOffseason=true`, `capHit=0`, status UFA/RFA so the Re-Sign phase populates out of the box and survives resets. Added `__tests__/free-agent-seed.test.ts`. Touched `app/lib/free-agent-seed.ts`, `app/lib/roster-assembly.ts`, `__tests__/free-agent-seed.test.ts`.
- 2026-06-24: Fixed FA overrides so any player can be forced into the off-season free-agent pool — previously the `/admin/fa-overrides` form only let you select from a dropdown of existing DB `players` rows, and the POST route 400'd (`"Selected player was not found in the DB"`) for anyone not already curated in that table. Live-scraped free agents (e.g. Alex Tuch) are never written to the `players` table, so they could not be forced into the pool — the exact case S1.5 was built for. Now the POST enriches from the DB players table when the id matches but no longer requires it (override is matched by id OR name during roster assembly), and the admin form takes a free-text PLAYER NAME input with the DB dropdown demoted to an optional name pre-fill. Note: the related "Alex Tuch stuck at 1yr/$0.93M" symptom is a polluted DB row — `app/api/admin/contracts` POST inserts a placeholder with `capHit ?? 0.925` (displays $0.93M) and `yearsRemaining ?? 1` for brand-new players; forcing Tuch UFA via the fixed override zeroes that cap and flags him expiring, so he surfaces correctly in the FA list (or delete the stray row via the contracts page Clear action). Touched `app/api/admin/fa-overrides/route.ts`, `app/admin/fa-overrides/page.tsx`.

<!-- ============================================================ -->
<!-- HANDOFF — 2026-06-24 — DB 500 investigation (for Codex)       -->
<!-- ============================================================ -->

## 2026-06-24 — DB 500 handoff

Today's work added two new DB tables and several routes that read/write them.
There is a reported **500 error involving the database**. This section documents
everything changed today and the most likely culprits so it can be triaged
without re-deriving context.

### Most likely root cause (start here)
The two new tables — **`draft_pick_overrides`** and **`fa_overrides`** — were added
to `app/db/schema.ts` and are provisioned **only** by the runtime DDL helper
`ensureNewTables()` in `app/db/ensure-schema.ts` (which runs
`CREATE TABLE IF NOT EXISTS …`). **Unlike every prior table, no committed
`drizzle/*.sql` migration file was generated for them.** Compare:
- `trades`            → `drizzle/0002_add_trades.sql` ✅
- player retirement   → `drizzle/0001_add_player_retirement.sql` ✅
- `roster_mutating`   → `drizzle/0003_add_trade_roster_mutating.sql` ✅
- `draft_pick_overrides` / `fa_overrides` → **no migration file** ❌ (runtime DDL only)

So if production Turso is provisioned via `drizzle-kit push` / applied migrations
at deploy (not via the runtime `ensure*` safety net), the two new tables will be
**missing**, and any query against them throws `no such table: draft_pick_overrides`
(or `fa_overrides`) → 500.

**Suspected fix:** generate the missing migration so the tables exist in Turso the
same way the others do:
```
npm run db:generate   # drizzle-kit generate → emits drizzle/0004_*.sql
npm run db:push        # or apply via the deploy migration step
```
Then confirm both tables exist in the Turso instance. The runtime
`ensureNewTables()` should still serve as the local/dev safety net.

### Why some paths 500 and others don't
`ensureNewTables()` is **memoized per DB instance via a WeakMap**, and on failure it
`cache.delete()`s so a later call can retry. The reads are wrapped defensively in
different ways — this asymmetry explains why only *some* surfaces 500:

| Caller | File | On table-missing |
|---|---|---|
| League route pick merge | `app/api/league/route.ts` (~250) | `try/catch` → falls back to default picks (no 500) |
| Roster assembly FA overrides | `app/lib/roster-assembly.ts` (~987) | `try/catch` → skips overrides (no 500) |
| Admin draft-picks GET/PUT/DELETE | `app/api/admin/draft-picks/route.ts` | `catch` returns **500** with `{error}` |
| Admin fa-overrides GET/POST/DELETE | `app/api/admin/fa-overrides/route.ts` | `catch` returns **500** with `{error}` |

So the **admin pages** (`/admin/draft-picks`, `/admin/fa-overrides`) are the most
likely place a user sees a 500; the public league/roster paths degrade silently.
Check the server log for the exact thrown message — the routes `console.error`
with a `[admin/draft-picks …]` / `[admin/fa-overrides …]` prefix.

### Secondary things to verify
1. **Multi-line `CREATE TABLE` via `db.run(sql.raw(...))`** — `NEW_TABLE_STATEMENTS`
   in `ensure-schema.ts` are multi-line strings. libSQL accepts these, but confirm
   the Turso driver doesn't choke on the formatting vs. the single-line ALTERs.
2. **`onConflictDoUpdate`** is used in both admin write routes. Verify the libSQL
   dialect supports the emitted `ON CONFLICT … DO UPDATE` for these tables.
3. **WeakMap memoization of a rejected promise** — if the very first
   `ensureNewTables()` call throws for a transient reason, the catch deletes the
   cache entry, so this should self-heal; but if the table-create silently
   "succeeds" against the wrong DB file (dev `file:local.db` vs Turso), the memo
   masks the real target. Confirm `DATABASE_URL` / `DATABASE_AUTH_TOKEN` are set in
   the failing environment (`app/db/client.ts` falls back to `file:local.db`).

### Full inventory of today's changes (2026-06-24)
**S1 (committed earlier today/prior, context):** FA detection keys on `expiryYear`
(authoritative) instead of floored `yearsRemaining` — `app/services/scraper.ts`
surfaces `expiryYear` from CapWages `row[29]`; `app/lib/roster-assembly.ts` derives
`expiresThisOffseason` / `contractStatus` from it.

**S1.75 — draft pick DB ownership** (commit `ad1207b`):
- `app/db/schema.ts`: new `draftPickOverrides` table (`id` = `pick-{origOwner}-{year}-{round}`,
  `currentOwnerId`, `originalOwnerId`, `round`, `year`, `isProtected`, `conditions`, `updatedAt`).
  Stores **only moved picks** (exceptions); the natural 480-pick set is still generated at runtime.
- `app/db/ensure-schema.ts`: `ensureNewTables()` + `NEW_TABLE_STATEMENTS` `CREATE TABLE IF NOT EXISTS`.
- `app/api/admin/draft-picks/route.ts`: GET (merged default+override list), PUT (upsert/auto-reset
  when owner == original & no flags), DELETE (reset to default). All `requireAdmin`-gated.
- `app/admin/draft-picks/page.tsx`: full pick board, team/year/round filters, "moved only" toggle,
  inline transfer modal.
- `app/api/league/route.ts`: generates all 480 picks by original owner, applies override
  `currentOwnerId`; pick valuation still uses the **original** team's standing (the real draft slot);
  label shows "via {origTeam}" when moved.

**S1.5 — FA override admin** (commit `ad1207b`):
- `app/db/schema.ts`: new `faOverrides` table (`id` = name slug, `playerName`, `teamSlug`,
  `forceStatus` ∈ {UFA,RFA,SIGNED,EXCLUDE}, `season`, `notes`, `updatedAt`).
- `app/api/admin/fa-overrides/route.ts`: GET / POST (upsert, validates `forceStatus`) / DELETE.
- `app/admin/fa-overrides/page.tsx`: add/remove overrides by name; status badge; notes.
- `app/lib/roster-assembly.ts`: loads overrides **after** the trade-block `db.select()` (ordering
  matters — the `league-players-route.test.ts` mock counts `selectCall`), applies them before
  dedup; `EXCLUDE` filters the player out of all rosters entirely.

**S1.25 — RFA offer-sheet compensation** (commit `ad1207b`):
- `app/lib/free-agency.ts`: `getOfferSheetCompensation(aavMillions)` → CBA Art. 10.3 pick tiers.
- `app/components/ResignPhase.tsx`: market RFAs show amber "Offer sheet · picks owed" + "Offer Sheet" button.

**Nav/docs:** `app/admin/layout.tsx` (+PICKS, +FREE AGENTS links); `docs/TASKS.md`
(S1.25/S1.5/S1.75 checked off). Gates at commit time: 321 tests pass, `tsc --noEmit` clean.

<!-- ============================================================ -->

- 2026-06-24: Fixed expired CapWages contracts so UFA/RFA players with `expiryYear` at the projected season start are surfaced as pending free agents with zero active cap charge/years instead of a fake one-year league-minimum deal; touched `app/lib/roster-assembly.ts`, `__tests__/league-players-route.test.ts`, `__tests__/feature-canaries.test.ts`, `docs/DEVNOTES.md`.
- 2026-06-24: Fixed post-reset CapWages sync by creating/seeding canonical `teams` rows before contract sync and changing admin reset to clear only team override columns instead of deleting team identities referenced by `players.team_id`; touched `app/db/ensure-schema.ts`, `app/api/admin/contracts/route.ts`, `app/api/admin/reset/route.ts`, `__tests__/feature-canaries.test.ts`, `docs/DEVNOTES.md`.
- 2026-06-24: Fixed post-reset `/api/admin/contracts` recovery by adding a runtime `players` base-table safety net before contract admin reads/writes, so an empty local/reset DB returns an empty contracts list and can sync CapWages back in; touched `app/db/ensure-schema.ts`, `app/api/admin/contracts/route.ts`, `__tests__/feature-canaries.test.ts`, `docs/DEVNOTES.md`.
- 2026-06-24: Standardized admin operations by adding a guarded Settings hard reset that clears mutable admin DB state back to CapWages/NHL roster scrape defaults, optionally deletes saved Docket trades, clears all live caches, and updates the dashboard to include Picks and Free Agents; touched `app/api/admin/reset/route.ts`, `app/admin/settings/page.tsx`, `app/admin/page.tsx`, `__tests__/admin-auth.test.ts`, `__tests__/feature-canaries.test.ts`, `docs/DEVNOTES.md`.
- 2026-06-24: Prevented manual contract DB edits from persisting new players with `position: "Unknown"` by requiring position on manual inserts, adding position controls to the admin add/edit flows, and adding a canary for the position payload; touched `app/api/admin/contracts/route.ts`, `app/admin/contracts/page.tsx`, `__tests__/feature-canaries.test.ts`, `docs/DEVNOTES.md`.
- 2026-06-24: Changed FA overrides from name-only manual entry to DB-player selection by storing `fa_overrides.player_id`, returning selectable DB players from the admin route, applying overrides by player id before legacy name fallback, and covering Alex Tuch-style id-backed UFA forcing; touched `app/db/schema.ts`, `app/db/ensure-schema.ts`, `drizzle/0005_add_fa_override_player_id.sql`, `app/api/admin/fa-overrides/route.ts`, `app/admin/fa-overrides/page.tsx`, `app/lib/roster-assembly.ts`, `__tests__/league-players-route.test.ts`, `docs/DEVNOTES.md`.
- 2026-06-24: Added the missing Drizzle migration for `draft_pick_overrides` and `fa_overrides` so deployed DBs provision the admin draft-pick and FA override tables instead of relying only on runtime DDL; also fixed the new admin routes to await the shared admin gate and keep helper exports out of Next route modules; touched `drizzle/0004_add_pick_and_fa_overrides.sql`, `app/api/admin/draft-picks/route.ts`, `app/api/admin/fa-overrides/route.ts`, `docs/DEVNOTES.md`.
- 2026-06-24: Completed S1.75 (draft pick DB ownership), S1.5 (FA override admin), and S1.25 (RFA offer-sheet compensation). S1.75 adds a `draft_pick_overrides` table and admin board at `/admin/draft-picks` — all 480 picks (32 teams × 3 years × 5 rounds) are shown with their original/current owner; transfers persist to DB and the league route merges overrides at runtime so the trade machine reflects real-life pick moves without touching the runtime generation logic. S1.5 adds a `fa_overrides` table and admin panel at `/admin/fa-overrides` — admins can force any player's free-agent status (UFA/RFA/SIGNED/EXCLUDE) by name to fix CapWages scraper misdetections (e.g. Alex Tuch); EXCLUDE removes a player from rosters entirely. S1.25 adds `getOfferSheetCompensation(aav)` to `free-agency.ts` implementing the seven CBA Article 10.3 tiers (none → 4×1st); the ResignPhase market panel now shows an amber "Offer sheet · picks owed" warning and re-labels the Sign button to "Offer Sheet" for RFAs. Admin nav extended with PICKS and FREE AGENTS links. FA override loading is sequenced after the trade block DB select to preserve the test mock's selectCall ordering. All 321 tests pass; `tsc --noEmit` clean. Touched `app/db/schema.ts`, `app/db/ensure-schema.ts`, `app/api/admin/draft-picks/route.ts`, `app/api/admin/fa-overrides/route.ts`, `app/admin/draft-picks/page.tsx`, `app/admin/fa-overrides/page.tsx`, `app/admin/layout.tsx`, `app/api/league/route.ts`, `app/lib/roster-assembly.ts`, `app/lib/free-agency.ts`, `app/components/ResignPhase.tsx`.
- 2026-06-23: Added a roster-overlay confirmation modal before mutating Docket publishes and cleared league/team caches after Docket trade save, publish, or delete so UI-only corrections and deletions take effect cleanly; touched `app/admin/trades/page.tsx`, `app/api/admin/trades/route.ts`, `__tests__/feature-canaries.test.ts`, `docs/DEVNOTES.md`.
- 2026-06-23: Added admin Docket saved-trade deletion with a guarded API route, data-layer delete helper, saved-list delete button, and regression coverage; touched `app/lib/trades.ts`, `app/api/admin/trades/route.ts`, `app/admin/trades/page.tsx`, `__tests__/trades-data.test.ts`, `__tests__/feature-canaries.test.ts`, `docs/DEVNOTES.md`.
- 2026-06-23: Updated admin Docket ingestion so current asset-source teams are independent from historical traded-from teams, allowing already-updated DB trades to be captured accurately; touched `app/admin/trades/page.tsx`, `app/components/TradePanel.tsx`, `__tests__/feature-canaries.test.ts`, `docs/DEVNOTES.md`.
- 2026-06-23: Completed The Docket D2.5/D3 by adding a homepage Docket card and server-side today re-grades that enrich public Docket entries with current NAV/verdicts while preserving frozen at-trade snapshots; touched `app/page.tsx`, `app/docket/page.tsx`, `app/docket/DocketClient.tsx`, `app/lib/docket-view.ts`, `app/lib/docket-today.ts`, `__tests__/feature-canaries.test.ts`, `docs/futures/FUTURECONCEPTS.md`, `docs/DEVNOTES.md`.
- 2026-06-23: Completed The Docket D2 by adding expanded public Docket entries with the frozen verdict panel, per-asset at-trade NAV/impact detail, STRAND rendering, development outlooks, pick-curve NAV notes, and trade conditions; touched `app/docket/DocketClient.tsx`, `app/lib/docket-view.ts`, `__tests__/docket-view.test.ts`, `__tests__/feature-canaries.test.ts`, `docs/futures/FUTURECONCEPTS.md`, `docs/DEVNOTES.md`.
- 2026-06-23: Completed The Docket D1 by adding a public `/docket` page with published-trade list/filter/sort controls, NAV-margin chips, package summaries, and Docket view-model tests while leaving live today re-grades for D3; touched `app/docket/page.tsx`, `app/docket/DocketClient.tsx`, `app/lib/docket-view.ts`, `app/components/Header.tsx`, `__tests__/docket-view.test.ts`, `__tests__/feature-canaries.test.ts`, `docs/futures/FUTURECONCEPTS.md`, `docs/DEVNOTES.md`.
- 2026-06-23: Completed The Docket C4 by adding a persisted UI-only trade mode that lets published Docket entries skip roster and cap overlays while remaining visible in admin trade management; touched `app/db/schema.ts`, `drizzle/0003_add_trade_roster_mutating.sql`, `app/lib/trades.ts`, `app/api/admin/trades/route.ts`, `app/admin/trades/page.tsx`, `app/lib/roster-assembly.ts`, `__tests__/trades-data.test.ts`, `__tests__/trade-overlay.test.ts`, `__tests__/feature-canaries.test.ts`, `docs/futures/FUTURECONCEPTS.md`, `docs/DEVNOTES.md`.
- 2026-06-23: Completed The Docket C3 by adding admin trade list/edit/publish/unpublish controls and making published overlays skip already-reconciled scrape moves for cap purposes; touched `app/api/admin/trades/route.ts`, `app/admin/trades/page.tsx`, `app/lib/trades.ts`, `app/lib/roster-assembly.ts`, `__tests__/trades-data.test.ts`, `__tests__/trade-overlay.test.ts`, `__tests__/feature-canaries.test.ts`, `docs/futures/FUTURECONCEPTS.md`, `docs/DEVNOTES.md`.
- 2026-06-23: Completed The Docket C2 by deriving retained-salary cap moves from published trade snapshots and merging them into canonical roster team cap-space assembly while leaving untraded teams unchanged; touched `app/lib/roster-assembly.ts`, `__tests__/trade-overlay.test.ts`, `__tests__/league-players-route.test.ts`, `__tests__/feature-canaries.test.ts`, `docs/futures/FUTURECONCEPTS.md`, `docs/DEVNOTES.md`.
- 2026-06-23: Completed The Docket C1 by applying ordered published trade overlays inside canonical roster assembly so moved players read on their new teams app-wide without mutating source rows; touched `app/lib/trades.ts`, `app/lib/roster-assembly.ts`, `__tests__/trades-data.test.ts`, `__tests__/trade-overlay.test.ts`, `__tests__/feature-canaries.test.ts`, `docs/futures/FUTURECONCEPTS.md`, `docs/DEVNOTES.md`.
- 2026-06-23: Completed The Docket B3 by adding a gated admin trade-ingestion page that reuses the trade-machine asset pickers for teams, players, picks, and retention, previews grades through the evaluate API, and saves unpublished frozen trade drafts through a new admin route; touched `app/admin/trades/page.tsx`, `app/api/admin/trades/route.ts`, `app/api/evaluate/route.ts`, `app/admin/page.tsx`, `__tests__/feature-canaries.test.ts`, `docs/futures/FUTURECONCEPTS.md`, `docs/DEVNOTES.md`.
- 2026-06-23: Completed The Docket A3b by exposing cap-delta-adjusted teams from canonical roster assembly and routing Armchair GM preview/executed trade cap math through the shared helper; touched `app/lib/cap-delta.ts`, `app/lib/roster-assembly.ts`, `app/api/league/route.ts`, `app/armchair-gm/page.tsx`, `__tests__/cap-delta.test.ts`, `__tests__/feature-canaries.test.ts`, `docs/futures/FUTURECONCEPTS.md`, `docs/DEVNOTES.md`.
- 2026-06-23: Completed The Docket A2A by extracting canonical roster assembly into `app/lib/roster-assembly.ts`, thinning both league routes to response shaping, and repointing source canaries to the module; touched `app/lib/roster-assembly.ts`, `app/api/league/route.ts`, `app/api/league/players/route.ts`, `__tests__/feature-canaries.test.ts`, `docs/futures/FUTURECONCEPTS.md`, `docs/TASKS.md`, `docs/DEVNOTES.md`.
- 2026-06-23: Completed The Docket B2 by adding save-time trade freezing for locked verdicts, per-asset `navAtTrade`, input snapshots, and at-trade grades with an immutability regression test; touched `app/lib/trades.ts`, `__tests__/trades-data.test.ts`, `docs/futures/FUTURECONCEPTS.md`, `docs/TASKS.md`, `docs/DEVNOTES.md`.
- 2026-06-23: Completed The Docket B1 by adding the `trades` table schema/migration, a typed trade persistence helper, and a round-trip test for frozen asset input snapshots; touched `app/db/schema.ts`, `drizzle/0002_add_trades.sql`, `app/lib/trades.ts`, `__tests__/trades-data.test.ts`, `docs/futures/FUTURECONCEPTS.md`, `docs/TASKS.md`, `docs/DEVNOTES.md`.
- 2026-06-23: Synced The Docket future plan checklist with the already-completed A3a cap-delta helper; touched `docs/futures/FUTURECONCEPTS.md`, `docs/DEVNOTES.md`.
- 2026-06-22: Fixed the real Colton Parayko reading NAV -34 — the R3 shutdown-D floor only engaged when shutdownDSignal>0, but his live inputs (QoC 76 vs the 78 gate, current DPS 3.4 blended under 3.3 by a lower multi-season baseline, no pairDriver/xgaRel signal) fell through every gate so no floor applied. Lowered the QoC gate 78->74 so a genuine 22+ TOI top-pair shutdown D clears it (~+130), and added a regression test built from his actual card profile (blended DPS <3.3 + QoC 76) that the old code would have failed; touched `app/lib/xnav-engine.ts`, `__tests__/xnav.test.ts`, `docs/DEVNOTES.md`.
- 2026-06-22: Completed 1c by adding mocked `/api/league/players` roster-assembly tests for live duplicate dedupe, DB-row augmentation without duplication, and stat attachment; touched `__tests__/league-players-route.test.ts`, `docs/TASKS.md`.
- 2026-06-22: Completed A3a by adding a pure `applyCapDelta` helper and characterization tests for straight swaps, retained-salary moves, and pick-only moves; touched `app/lib/cap-delta.ts`, `__tests__/cap-delta.test.ts`, `docs/TASKS.md`.
- 2026-06-22: Completed A4 by adding reversible player retirement flags, a contracts-admin Retire/Restore action, cache invalidation, and retired-player filtering in both league roster routes; touched `app/db/schema.ts`, `drizzle/0001_add_player_retirement.sql`, `app/api/admin/contracts/route.ts`, `app/admin/contracts/page.tsx`, `app/api/league/route.ts`, `app/api/league/players/route.ts`, `__tests__/feature-canaries.test.ts`, `docs/TASKS.md`.
- 2026-06-22: Completed A3 admin endpoint cleanup by dropping unsupported trade-block `blocked` status, adding direct contract POST cap/term bounds, and documenting guarded curl-only admin endpoints; touched `app/api/admin/trade-block/route.ts`, `app/api/admin/contracts/route.ts`, `docs/admin-endpoints.md`, `__tests__/feature-canaries.test.ts`, `docs/TASKS.md`.
- 2026-06-22: Removed dead superseded admin team editor files; touched `app/admin/AdminTeamRow.tsx`, `app/admin/actions.ts`, `docs/TASKS.md`.
- 2026-06-22: Fixed admin mutation error handling so failed writes show server errors and skip optimistic success/reloads; touched `app/admin/admin-response.ts`, `app/admin/settings/page.tsx`, `app/admin/contracts/page.tsx`, `app/admin/teams/page.tsx`, `app/admin/trade-block/page.tsx`, `docs/TASKS.md`.
- 2026-06-22: Closed admin auth with signed httpOnly session login, fail-closed admin API gates, and `/admin/*` middleware; touched `app/lib/admin-auth.ts`, `app/admin/login/page.tsx`, `middleware.ts`, `app/api/admin/*/route.ts`, `__tests__/admin-auth.test.ts`, `docs/TASKS.md`.
- 2026-06-22: Updated admin login for GitHub Codespaces forwarded URLs by replacing the Server Action form with `/admin/login/submit`, avoiding the localhost vs `*.app.github.dev` origin mismatch; touched `app/admin/login/page.tsx`, `app/admin/login/submit/route.ts`, `middleware.ts`.
- 2026-06-22: Reframed the sim from a "2025-26 season-start replay" to a forward "2026-27 season projection" (the engine was already a forward projection, not a schedule replay — only framing changed). Updated `simulationMode`/`rosterMoveWindow` in season-config; rewrote the Claude prompt to recap the PROJECTED 2026-27 season while keeping 2025-26 as the completed reference (CAR champion); kept the 2025-26 data baseline (MoneyPuck `2025` stats + `20252026` rosters) since 2026-27 hasn't been played. Also fixed two latent bugs surfaced en route: `CapProjection` hardcoded a 95.5 cap (now `SEASON.capCeiling`) and `QuickTradeMachine` hardcoded `season:"2025-26"` (now `SEASON.label`); parameterized the four hardcoded MoneyPuck CSV URLs to `SEASON.mpSeason`. Touched `app/lib/season-config.ts`, `app/api/claude/route.ts`, `app/api/simulate/route.ts`, `app/components/CapProjection.tsx`, `app/components/QuickTradeMachine.tsx`, `app/api/league/route.ts`, `app/api/league/players/route.ts`, and `docs/DEVNOTES.md`.

- 2026-06-22: Fixed Armchair GM depth-chart cells overlapping after the 8px→11px font bump by keeping player names at 11px while dropping the secondary metrics (NAV, position badge, P82/TOI meta) to 9px and adding overflow guards (minWidth:0, flexShrink:0 on fixed labels, ellipsis on meta, overflow:hidden on the cell); applied the matching name-11px/position-9px hierarchy to the read-only LineupCard; touched `app/components/LineupEditor.tsx`, `app/components/LineupCard.tsx`, and `docs/DEVNOTES.md`.
- 2026-06-22: Corrected league team cap space that summed every DB contract row (no active-roster bound / LTIR / burial accounting), which overstated used cap and pushed teams negative (Jets -7M) and reversed Decision A; reverted to the curated static TEAMS_DB room shifted by the live ceiling delta (capCeiling − 95.5) so the 2026-27 ceiling raises every team's room (Jets → 13.5M) without a naive recompute, leaving `buildTeamCapSpaceMap` in `cap-settings.ts` for the future A3a delta work; touched `app/api/league/teams/route.ts`, `__tests__/feature-canaries.test.ts`, and `docs/DEVNOTES.md`.
- 2026-06-22: Fixed admin cap settings falling back to stale 95.5/65 stored defaults by centralizing cap override parsing, using 104 as the settings loading placeholder, and surfacing failed settings saves; touched `app/lib/cap-settings.ts`, `app/api/admin/settings/route.ts`, `app/api/league/teams/route.ts`, `app/api/evaluate/route.ts`, `app/admin/settings/page.tsx`, `__tests__/cap-settings.test.ts`, and `__tests__/feature-canaries.test.ts`.
- 2026-06-22: Clarified Development Outlook score meanings and renamed the visible Pedigree input to Draft Sig/Draft weight to avoid confusing it with career reputation; touched `app/components/DevelopmentProfilePanel.tsx` and `__tests__/feature-canaries.test.ts`.
- 2026-06-22: Completed D4 by adding a collapsed-by-default Outlook key to the Development Outlook panel defining metrics, inputs, projection, phase, trend, and sample confidence; touched `app/components/DevelopmentProfilePanel.tsx`, `__tests__/feature-canaries.test.ts`, and `docs/TASKS.md`.
- 2026-06-22: Completed D3 by adding established-veteran peak-years-left framing to Development Outlook and swapping the panel Breakout tile to Peak Left for vets; touched `app/lib/development-profile.ts`, `app/components/DevelopmentProfilePanel.tsx`, `__tests__/development-profile.test.ts`, `__tests__/feature-canaries.test.ts`, and `docs/TASKS.md`.
- 2026-06-22: Completed D2 by adding Development Outlook durability scoring from NHL season games, threading it into risk/confidence/bust outputs, and surfacing it in the panel Inputs; touched `app/lib/development-profile.ts`, `app/components/DevelopmentProfilePanel.tsx`, `__tests__/development-profile.test.ts`, `__tests__/feature-canaries.test.ts`, `__tests__/trade-logic-development.test.ts`, and `docs/TASKS.md`.
- 2026-06-22: Completed D1 by raising Development Outlook production references and projection clamps so elite scorers separate at the top end; touched `app/lib/development-profile.ts`, `__tests__/development-profile.test.ts`, and `docs/TASKS.md`.
- 2026-06-22: Removed sticky positioning from the players filter/search bar so the sticky icon key remains unobstructed; touched `app/globals.css`.
- 2026-06-22: Made the players icon key sticky, exposed each icon definition directly in the key, and removed the Dump entry/badge; touched `app/players/page.tsx`.
- 2026-06-22: Completed UI5 by adding a visible top-of-page players icon key for row badges so users do not need the footer glossary; touched `app/players/page.tsx`, `__tests__/feature-canaries.test.ts`, and `docs/TASKS.md`.
- 2026-06-22: Completed UI4 by adding Armchair GM-style players-page row icon badges for Megalodon, Franchise, Surplus, prospect/pedigree, awards, injury risk, salary dump, and shutdown pedigree; touched `app/players/page.tsx`, `__tests__/feature-canaries.test.ts`, and `docs/TASKS.md`.
- 2026-06-22: Completed UI3 by giving players-page forwards, defence, and goalies their own sortable section headers and row stats, including D suppression and goalie SV%/GAA/GP; touched `app/players/page.tsx`, `__tests__/feature-canaries.test.ts`, and `docs/TASKS.md`.
- 2026-06-22: Completed UI2 by letting players-page names wrap without ellipsis and replacing archetype text pills with compact icon badges/tooltips; touched `app/players/page.tsx` and `docs/TASKS.md`.
- 2026-06-22: Completed UI1 by raising players-page filter, pager, and sortable column button text to a readable 11px with tighter letter spacing; touched `app/players/page.tsx` and `docs/TASKS.md`.
- 2026-06-19: Followed up R3 by lowering the shutdown-D DPS signal threshold and adding a live Parayko-shaped regression test so 22+ TOI / 3.4 DPS defenders clear the market floor; touched `app/lib/xnav-engine.ts` and `__tests__/xnav.test.ts`.
- 2026-06-19: Completed V2-5 by threading LineupEditor starting-goalie selections into `/api/simulate` and honoring them in goalie/team projections; touched `app/components/LineupEditor.tsx`, `app/armchair-gm/page.tsx`, `app/api/simulate/route.ts`, `__tests__/simulate-and-claude-routes.test.ts`, and `docs/TASKS.md`.
- 2026-06-19: Completed R3 by adding a guarded shutdown top-pair D defensive adjustment/floor for Parayko-type valuations while keeping weak top-minute D and offensive-D guards covered; touched `app/lib/xnav-engine.ts`, `__tests__/xnav.test.ts`, and `docs/TASKS.md`.
- 2026-06-19: Completed R1 by verifying expanded-card STRAND de-dup, adding sortable PTS/Term players-table headers, and compacting short-term contract projections; touched `app/players/page.tsx`, `app/components/PlayerTimeline.tsx`, `__tests__/feature-canaries.test.ts`, and `docs/TASKS.md`.
- 2026-06-19: Completed R3 by verifying the guarded shutdown top-pair D floor/adjustment and Parayko/weak-D/Makar characterization tests already in `app/lib/xnav-engine.ts` and `__tests__/xnav.test.ts`; touched `docs/TASKS.md`.
- 2026-06-19: Completed V2-4.5 by reordering development phase classification so young EMERGING and older REGRESSION_RISK branches beat the elite PEAK_WINDOW fallback; touched `app/lib/development-profile.ts`, `docs/TASKS.md`, and `docs/DEVNOTES.md`.
- 2026-06-19: Completed V2-4 by adding extra goalies beyond starter/backup to the editable Lineup bench so they can be swapped into goalie slots; touched `app/components/LineupEditor.tsx`, `__tests__/feature-canaries.test.ts`, and `docs/TASKS.md`.
- 2026-06-19: Completed V2-3 by requiring a 20-game live NHL sample before D-corps depletion flags can treat traded defensemen as established top-pair losses; touched `app/api/evaluate/route.ts`, `__tests__/evaluate-route.test.ts`, and `docs/TASKS.md`.
- 2026-06-19: Completed V2-2 by making `/api/league/teams` return the live `cap_ceiling` from `siteSettings` with validation/fallback while preserving the existing split team cache bust keys; touched `app/api/league/teams/route.ts`, `__tests__/feature-canaries.test.ts`, and `docs/TASKS.md`.
- 2026-06-19: Completed Task 0.3 by tightening draft-class import overwrites so only existing rows with prospect metadata and no real contract/clauses receive ELC defaults; touched `app/api/admin/import-draft-class/route.ts`, `__tests__/feature-canaries.test.ts`, and `docs/TASKS.md`.
- 2026-06-19: Completed V2-1 by removing Armchair GM's client-side league re-rank/phase remap after executed trades, preserving non-involved teams' standings/phases while only applying trader cap deltas; touched `app/armchair-gm/page.tsx`, `__tests__/feature-canaries.test.ts`, and `docs/TASKS.md`.

---

## 2026-06 — Docs restructure
- Moved planning docs under `docs/`: `AUDIT.md` → `docs/TASKS.md` (trimmed to remaining
  work), `FUTURECONCEPTS.md` → `docs/futures/`, `DEVNOTES.md` → `docs/`.
- Added `docs/bugs/CONFIRMEDFIXES.md` (shipped items, verified vs f743338) and
  `docs/bugs/KNOWNBUGS.md` (triage inbox).
- Moved the task preamble into `AGENTS.md` → "Task Discipline" (single source; auto-loaded).
- Added Audit V2 bugs (V2-1…V2-5) and the Ledger Trade Tracker spec.

## 2026-06-19 Audit Refinements — R0-R2

### Completed Today

- Completed trimmed `AUDIT.md` refinements R0, R1, and R2.
- R0: Replaced the old hard replacement-callup clamp with establishment-based dampening of positive cap surplus. Cheap-contract surplus now scales by current games and multi-year baseline, while negative cap value is not softened.
- R0: Added regression coverage for a Heinola-class low-sample depth defender and an established-but-injured star so the depth case stays below premium NAV while strong-baseline players keep cap surplus.
- R1: Decluttered expanded player cards by removing the standalone Season Points card, dropping duplicate OPS/DPS pills while keeping PS, and rebalancing the expanded panel around stats, STRAND, timeline, and development content.
- R1: Removed duplicate STRAND offense/defense bar blocks from the shared renderer and collapsed the repeated trait guide behind a small `?` details control.
- R2: Added `fmvAav` to `XNAVResult` in the engine and shared trade types, populated it for skaters and goalies from the current-cap fair-market AAV calculation, and blended it through prospect transition results.
- R2: Added a small `estimateNextContractTerm` heuristic and surfaced `Projected next: $X.XM × Yyr (RFA/UFA)` in the shared contract timeline block with a tooltip clarifying fair-market midpoint AAV.
- Added source canaries and xNAV characterization tests for R0-R2.

### Verification

- `npx tsc --noEmit`
- Result: no TypeScript errors.
- `npm run test`
- Result: `263` tests passing across `11` test files.
- Dev server was not started in Codespace, per project instructions.

## 2026-06-19 UX and UI Polish — Task U2 Revisited

### Completed Today

- Fixed the page-freeze regression described in `AUDIT.md` Task U2 Revisited.
- Removed `verdictOpen` from the Armchair GM body scroll-lock condition, so expanding or auto-opening the verdict bottom sheet no longer freezes page scrolling.
- Limited Armchair GM scroll locking to true blocking overlays: team select, trade block, and active trade-request modal.
- Converted `useBodyScrollLock` into a module-level reference-counted hook so overlapping modals increment/decrement one shared lock instead of clobbering each other's body overflow restore state.
- Confirmed direct `document.body.style.overflow` / `document.documentElement.style.overflow` writes now live only in `app/lib/use-body-scroll-lock.ts`.
- Updated feature canaries to require the reference counter and to prevent `verdictOpen` from returning to the scroll-lock condition.

### Verification

- `npx tsc --noEmit`
- Result: no TypeScript errors.
- `npm run test`
- Result: `257` tests passing across `11` test files.
- Dev server was not started in Codespace, per project instructions.

## 2026-06-19 UX and UI Polish — Tasks U1-U7

### Completed Today

- Completed the `AUDIT.md` UX and UI Polish section from Task U1 through Task U7.
- Kept MoneyPuck goalie GSAx authoritative on both league roster routes by merging NHL goalie fallback stats with MoneyPuck `goalieMap` stats and preserving `mpG.gsax` when available.
- Added shared `useBodyScrollLock(isOpen)` in `app/lib/use-body-scroll-lock.ts` and applied it to the trade proposal, ledger dropdown, trade block panel, asset dropdown, trade history save modal, and Armchair GM modal overlays.
- Reworked Player Analytics to fetch canonical teams and players routes, use a denser desktop stat table, label/sort stat columns, and page sections at 25 forwards, 10 defencemen, and 5 goalies with Prev/Page/Next controls.
- Added visible `FLOOR` residual rows to NAV breakdowns so floored assets reconcile their visible components to headline NAV.
- Strengthened the active header tab with red active text and an underline while keeping the filled diamond.
- Added point-of-use `NAV` tooltips on asset cards and Armchair GM card/table labels.
- Raised sub-11px data labels in the dense AssetCard and lineup bench/scratch zones to the 11px `text-2xs`/equivalent floor.
- Confirmed Task U6 completion: NAV labels now expose point-of-use tooltips defining Net Asset Value.
- Enlarged the shared Team Strands and Lineups headers.
- Moved Lineups below the main trade grid and removed the old Armchair GM `CapProjection` render; the stale `Post-Trade Roster Projection` heading was removed from the component source.
- Removed the duplicate `players-mobile-sort-strip` and made `players-column-header` the single sortable player table header, with horizontal scrolling on narrow screens.
- Replaced the duplicate desktop PPG display slot with total season points and added season points to the expanded player panel.
- Rebalanced the expanded player panel into responsive stats, STRAND, and timeline/development zones to reduce desktop dead space.
- Updated feature canaries for the goalie GSAx merge, paged Player Analytics table, scroll lock hook, NAV floor residuals/tooltips, active navigation styling, Lineups placement, removed projection panel, and player table header cleanup.

### Verification

- `npm run test`
- Result: `257` tests passing across `11` test files.
- `npx tsc --noEmit`
- Result: no TypeScript errors.
- Dev server was not started in Codespace, per project instructions.

## 2026-06-19 Batch Audit the Batch Audit — Task 0

### Completed Today

- Started at the `AUDIT.md` `# Batch Audit the Batch Audit` section and completed Task 0 only, per the standing preamble.
- Ran the baseline test suite before edits: `npm run test` passed with `253` tests across `11` files.
- Verified the following Task 0 items were already present in the current tree:
  - BreakdownTable optional metric guards in `app/armchair-gm/page.tsx`.
  - Draft-class import protection for existing NHL contract rows in `app/api/admin/import-draft-class/route.ts`.
  - Expanded admin cache clearing keys in `app/api/admin/clear-cache/route.ts`.
  - Trade-block name-derived keying and status enum validation in `app/api/admin/trade-block/route.ts`.
  - Cross-team duplicate player dedupe in `app/api/league/players/route.ts`.
- Completed the remaining Task 0 gap: cap-ceiling validation now rejects absurd values above `120` before admin persistence and ignores invalid request/DB cap ceilings in `app/api/evaluate/route.ts`.
- Updated the existing Batch 6 source canary in `__tests__/feature-canaries.test.ts` to cover absurd cap-ceiling validation on both admin settings and evaluate route reads.

### Verification

- `npm run test`
- Result: `253` tests passing across `11` test files.
- `npx tsc --noEmit`
- Result: no TypeScript errors.
- Dev server was not started in Codespace, per project instructions.

## 2026-06-19 Batch Audit Follow-up

### Completed Today

- Worked through the `AUDIT.md` "Batch Audit" notes against the current codebase and verified that most previously listed Batch 1-6 discrepancies were already addressed in the tree.
- Completed the remaining concrete discrepancy found in `app/components/CapProjection.tsx`: current roster cap usage now sums effective retained cap hits, matching the incoming/outgoing retained-cap math already used for post-trade deltas.
- Completed the remaining young-player contract collision discrepancy in both league roster builders:
  - `app/api/league/players/route.ts`
  - `app/api/league/route.ts`
- The contract collision guard now tracks whether a contract matched by position, team, or generic name. It only applies the ELC fallback for young players when the risky match was generic name-only, so legitimate position/team-specific contracts are preserved even when roster position metadata differs.
- Added source canaries in `__tests__/feature-canaries.test.ts` for retained-cap current roster usage and for preserving young-player contracts when only position metadata disagrees.

### Verification

- `npm run test`
- Result: `253` tests passing across `11` test files.
- Dev server was not started in Codespace, per project instructions.

## 2026-06-18 Batch 6 Admin / Players Ledger Audit

### Completed Today

- Completed Batch 6 edits from `AUDIT.md` across admin import/settings/cache routes, trade-block writes, roster patch reporting, STRAND rendering, cap projection, player comparison, and the players ledger.
- Guarded draft-class imports so existing NHL contract rows are not overwritten with ELC cap/term/clauses when a normalized prospect id collides with a real player.
- Added cap ceiling/floor validation in admin settings so zero, negative, non-finite, and inverted cap values are rejected before persistence.
- Hardened STRAND rendering and strand-type classification against empty trait arrays to avoid NaN SVG paths and divide-by-zero classifications.
- Expanded admin cache clearing to flush point shares, MoneyPuck skater/goalie CSV caches, NHL goalie summary stats, and versioned prospect enrichment caches.
- Changed patch-team-ids roster fetch failures to use a negative sentinel so failed NHL API fetches appear in `failedTeams`.
- Normalized trade-block row ids from player names on the server and validated statuses against `requested | available | blocked | untouchable`.
- Fixed post-trade cap projection to use effective retained cap hits and to strike through only players that belong to the displayed roster, with count labels matching rendered rows.
- Fixed lower-is-better comparison bars so cheaper and younger sides render as the longer winning bar.
- Added the development outlook panel to expanded skater rows on the players page.
- Split the players ledger into capped Forwards, Defence, and flat GSAx Goalies sections with show-all toggles and populated desktop sticky column labels.
- Added Batch 6 canaries covering each fixed audit path.

### Deferred

- Batch 6 lower-severity UX items not directly covered by the implementation task remain open: shared verdict color/status copy, QuickTradeMachine copied feedback, modal/row a11y improvements, disabled CTA affordance cleanup, AssetPicker untouchable flags, and AssetCard headshot fallback polish.

* Revision: Higher-impact: severity-tier legend (HARD/SOFT/WARN/INFO); plain-language + colorblind-safe verdict/net-gain labels; mobile audit/share controls hidden after a verdict; dead-end error messaging (friendly copy + Retry, no leaked endpoint paths).
Medium: shared verdict color/status config; QuickTradeMachine copied feedback; modal/row a11y; disabled-CTA affordance; AssetPicker untouchable flags; AssetCard headshot fallback.
Polish: sub-11px typography; ink-faint/rule contrast (WCAG); emoji copy icon.

### Verification

- `npx tsc --noEmit`
- Result: no TypeScript errors.
- `npm run test -- __tests__/feature-canaries.test.ts`
- Result: `112` tests passing.
- `npm run test`
- Result: `250` tests passing.
- Dev server was not started in Codespace, per project instructions.

## 2026-06-18 Batch 5 UI State / Async Robustness Audit

### Completed Today

- Completed Batch 5 edits from `AUDIT.md` for Armchair GM, the players page, trade state, focused trade machine summaries, and saved scenarios.
- Added composite trade asset identity via `tradeAssetKey(id + teamId)` and applied it to trade-store add/remove/retention, AssetCard updates, trade execution, and post-trade lineup previews so duplicate-id rows can be selected and moved independently.
- Fixed Armchair GM retention NAV fetch cleanup so debounced requests are actually aborted and cannot re-inject stale retained NAV.
- Hardened Armchair GM league boot loading with `response.ok` checks, `Promise.allSettled`, empty payload validation, and clearer error reporting.
- Added abort/request-token guards to `findMatches` so stale "Who Wants This Package?" results cannot overwrite a newer package or clear the newer spinner.
- Guarded BreakdownTable optional skater metrics before `.toFixed()` calls so stats-less skaters cannot crash the table.
- Fixed players-page fetch error handling, deterministic sort tie-breaks, null-last OPS/DPS sorting, low-game PPG handling, deferred search filtering, duplicate-safe row keys, and continuous goalie ranks across goalie subsections.
- Expanded saved scenario snapshots to include asset id, teamId, retainedPct, round, and year; replaced content-hash ids with unique ids; and added guarded localStorage hydration for corrupt or oversized persisted JSON.
- Aligned QuickTradeMachine package summaries with Armchair GM's compression-aware package NAV and updated labels away from "Linear NAV".
- Added Batch 5 source canaries covering duplicate-id state operations, async abort guards, guarded metrics, players-page load/sort behavior, scenario persistence, and package NAV display.

### Deferred

- The full shared `useLeagueData` / `useNavMap` / `useTradeVerdict` hook extraction remains open. Batch 5 aligned the concrete package-value drift and hardened duplicated lifecycles, but a broad hook extraction would be a larger structural refactor.

### Verification

- `npx tsc --noEmit`
- Result: no TypeScript errors.
- `npm run test -- __tests__/feature-canaries.test.ts`
- Result: `103` tests passing.
- `npm run test -- __tests__/trade-share.test.ts`
- Result: `5` tests passing.
- `npm run test -- __tests__/evaluate-route.test.ts`
- Result: `5` tests passing.
- `npm run test`
- Result: `241` tests passing.
- Dev server was not started in Codespace, per project instructions.

## 2026-06-18 Batch 3 Development / Static Pedigree Audit

### Completed Today

- Completed Batch 3 edits from `AUDIT.md` for the development-profile and static pedigree data paths.
- Added normalized static player-data lookups so accent/spelling variants such as `Tim Stutzle` resolve the same pedigree, prospect-tier, injury-risk, and shutdown-D records as their canonical keyed names.
- Updated evaluate logic, asset badges, and asset cards to use the normalized static lookup helpers instead of exact-name map indexing.
- Changed historical NAV floors to decay with age, availability, and current production when asset context is available, so declined or injured veterans are no longer re-inflated to peak value by a static floor.
- Wired development profile context inputs through league routes, including inferred international score, team context, and linemate/usage context where route data supports it.
- Fixed development profile classifier edge cases so ordinary 26-31 year-old NHLers do not render as `UNKNOWN`, low-volatility low-confidence profiles can be `STABLE`, and one-snapshot TOI changes cannot saturate role growth.
- Fixed rookie route-payload development inputs so players under 40 NHL games keep prospect NHLe as the headline pace while retaining the live NHL sample in the timeline.
- Versioned the prospect-enrichment Redis key by draft-year window and made slug merging first-write-wins to avoid silent same-slug overwrites.
- Added regression coverage for decayed historical floors, mid-career phase classification, boom/bust labeling, role-growth damping, and rookie small-sample pace handling.

### Verification

- `npx tsc --noEmit`
- Result: no TypeScript errors.
- `npm run test -- __tests__/development-profile.test.ts`
- Result: `13` tests passing.
- `npm run test -- __tests__/development-sources.test.ts`
- Result: `21` tests passing.
- `npm run test -- __tests__/xnav.test.ts`
- Result: `67` tests passing.
- `npm run test -- __tests__/feature-canaries.test.ts`
- Result: `97` tests passing.
- `npm run test`
- Result: `235` tests passing.
- Dev server was not started in Codespace, per project instructions.

## 2026-06-18 Batch 4 League Roster / Simulation Audit

### Completed Today

- Completed the concrete Batch 4 edits from `AUDIT.md` across league roster assembly, cache isolation, and playoff simulation.
- Added shared player identity helpers in `app/lib/player-identity.ts` for canonical name keys, safe NHL roster row parsing, DB-authoritative roster removal, and final player dedupe.
- Applied DB-authoritative player dedupe in both `app/api/league/players/route.ts` and `app/api/league/route.ts`, so admin-assigned players such as a moved Joseph Woll cannot be emitted for both old and new teams.
- Made live roster ingestion skip malformed NHL rows instead of aborting the rest of the team loop.
- Fixed young-player contract collision handling so position-only overrides, such as Quinton Byfield's center override, do not strip real contracts down to ELC terms.
- Split the Redis team caches into `cache:league:teams:v1` and `cache:trade:teams:v1`, guarded standings sorts against missing points, and updated admin cache invalidation to clear both keys.
- Removed surname-only goalie stat fallbacks from league roster routes to avoid same-surname goalie stat collisions.
- Hardened playoff simulation so conference seeds are not padded with duplicate teams, winner lookups fail visibly instead of advancing the last seed, and later rounds sort series sides by projected strength before calculating win probability.
- Guarded `stablePts` against missing or non-finite scoring pace values so simulation standings cannot become `NaN`.
- Added canaries for roster dedupe, isolated team cache keys, goalie fallback behavior, playoff bracket safety, and simulation numeric guards.

### Deferred

- Batch 4's traded-pick origin/ownership item remains open. The current app only has synthetic pick generation and no local traded-pick ownership source or schema, so fixing that correctly requires a real ownership data model/feed rather than guessing pick origins.

### Verification

- `npx tsc --noEmit`
- Result: no TypeScript errors.
- `npm run test -- __tests__/feature-canaries.test.ts`
- Result: `97` tests passing.
- `npm run test -- __tests__/simulate-and-claude-routes.test.ts`
- Result: `11` tests passing.
- `npm run test`
- Result: `230` tests passing.
- Dev server was not started in Codespace, per project instructions.

## 2026-06-18 Batch 2 Trade UI / Share Fidelity Audit

### Completed Today

- Completed Batch 2 edits from `AUDIT.md` for the trade UI, proposal, evaluation-client, verdict, and share surfaces.
- Added abort/run-id guards to focused Trade Machine NAV loading and GM Audit runs so stale async responses cannot restore old verdicts or clobber newer NAV maps.
- Reworked client NAV cache keys to serialize the full asset valuation payload plus cap ceiling instead of relying on a hand-maintained allow-list.
- Passed live cap ceiling through Trade Machine and Armchair GM NAV/verdict requests.
- Changed missing NAV handling so omitted server NAV ids throw an error instead of silently becoming legitimate zero-value assets.
- Added proposal-generation abort/run-id guards and capped full audit verification fan-out with `MAX_AUDIT_CANDIDATES`.
- Fixed salary-dump proposal generation so dump proposals send only negative-value contracts plus sweeteners, not unrelated positive-value players in the selected block.
- Preserved missing shared-trade assets as placeholders during share reconstruction so locked shared packages do not silently lose assets.
- Fixed verdict flag expansion keys to use stable global indices instead of `flags.indexOf(flag)`.
- Added shared pick-round formatting for trade UI surfaces so 4th+ round picks no longer display as 3rd-round picks or malformed ordinals.
- Added regression/source coverage for stale async guards, capped proposal audits, dump-package construction, share placeholders, NAV cache fidelity, VerdictPanel keys, and shared pick-round formatting.

### Verification

- `npm run test -- __tests__/trade-share.test.ts`
- Result: `5` tests passing.
- `npm run test -- __tests__/feature-canaries.test.ts`
- Result: `89` tests passing.
- `npm run test`
- Result: `222` tests passing.
- `npx tsc --noEmit`
- Result: no TypeScript errors.
- Dev server was not started in Codespace, per project instructions.

## 2026-06-18 Batch 1 Core Valuation / Trade Verdict Audit

### Completed Today

- Completed Batch 1 edits from `AUDIT.md` for the core valuation and trade verdict path.
- Made package compression monotonic so adding low-value throw-ins can no longer reduce the compressed NAV of the package being sent away.
- Moved shared trade classification rules into `app/lib/trade-classification.ts` so `/api/evaluate` and proposal generation use the same:
  - division map
  - position normalization
  - future-core / development-risk / peak-window classifiers
  - veteran-term thresholds
  - shopped-asset and premium-lottery-pick checks
- Fixed proposal pre-screen partner-needs logic so a partner trading away an unreplaced stated position need is rejected deterministically.
- Aligned generated proposal concession limits with the verdict engine by comparing compressed NAV against the verdict's 45 / 70 concession bands.
- Fixed cap-floor checks in `/api/evaluate` to use the live/requested cap ceiling instead of the static season ceiling.
- Updated contender timeline checks to compare against compressed return NAV so depth-padded packages cannot dodge future-asset vetoes.
- Guarded trade metrics against missing optional fields so picks no longer corrupt `ptsGain` or `defGain`.
- Reduced bad-starter goalie floor inflation by allowing the starter floor signal to fall to zero for genuinely poor rate performance.
- Added regression coverage for monotonic compression, partner need screening, compressed concession bands, and pick-safe evaluate metrics.

### Verification

- `npm run test -- __tests__/xnav.test.ts`
- Result: `66` tests passing.
- `npm run test -- __tests__/trade-logic-development.test.ts`
- Result: `8` tests passing.
- `npm run test -- __tests__/evaluate-route.test.ts`
- Result: `5` tests passing.
- `npm run test -- __tests__/feature-canaries.test.ts`
- Result: `85` tests passing.
- `npm run test`
- Result: `218` tests passing.
- `npx tsc --noEmit`
- Result: no TypeScript errors.
- Dev server was not started in Codespace, per project instructions.

## 2026-06-18 Replacement Callup NAV Guard

### Completed Today

- Added a replacement-level callup ceiling to skater X-NAV.
- Low-minute, tiny-sample, age-26+ skaters with no meaningful baseline/pedigree signal no longer receive meaningful positive NAV from league-minimum cap surplus alone.
- This addresses profiles like a 29-year-old injury callup playing ~3 games at ~6 minutes TOI being valued as a real trade asset.
- Corrected the exact `Zack MacEwen` case where a weak MoneyPuck baseline existed but should not count as a meaningful established-player baseline.
- Added explicit tiny-sample production handling so one point in three games does not turn into a meaningful pts/82 signal for a 6-minute replacement callup.
- Added regression tests for a `Zack MacEwen`-shaped league-minimum callup with and without weak baseline data.

### Verification

- `npm run test -- __tests__/xnav.test.ts`
- Result: `65` tests passing.
- `npm run test`
- Result: `215` tests passing.
- `npx tsc --noEmit`
- Result: no TypeScript errors.
- Dev server was not started in Codespace, per project instructions.

## 2026-06-18 Development Profile Panel Audit

### Completed Today

- Completed the `DEVELOPMENTPROFILEPANEL AUDIT 2026-06-18` in `AUDIT.md`.
- Changed dynasty scoring so draft pedigree decays with NHL sample instead of remaining a permanent 28% input.
- Added sample-adjusted pedigree outputs:
  - `effectivePedigreeScore`
  - `pedigreeWeight`
- Shifted established-player dynasty weight toward production, role, and confidence.
- Raised forward production scaling so strong NHL producers do not clamp at 100 too early.
- Added `confidenceScore` and `scoringTrajectory` to development profiles.
- Reworded trajectory rationale to separate scoring volatility from sample confidence.
- Added an explicit rationale line when draft pedigree and established production disagree.
- Expanded the Development tab panel with:
  - production, role, sample-adjusted pedigree, and experience inputs
  - pedigree sample weight and confidence
  - 3-year scoring trajectory
  - up to five rationale lines instead of three
- Added regression coverage for the Lafreniere/Jarvis issue so a more productive established NHLer is not ranked below a less productive player solely because of old draft slot.

### Verification

- `npm run test -- __tests__/development-profile.test.ts`
- Result: `10` tests passing.
- `npm run test -- __tests__/feature-canaries.test.ts`
- Result: `85` tests passing.
- `npm run test`
- Result: `213` tests passing.
- `npx tsc --noEmit`
- Result: no TypeScript errors.
- Dev server was not started in Codespace, per project instructions.

## 2026-06-18 XNAV Young Player / Goalie Audit

### Completed Today

- Completed the `XNAV / YOUNG PLAYERS / GOALIE CALCULATION AUDIT` in `AUDIT.md`.
- Reworked prospect NAV so unsupported drafted prospects are discounted for burned development time instead of receiving a blanket certainty premium.
- Added a 14-60 NHL game transition band that blends prospect pedigree into skater NAV, removing the hard 14-game valuation cliff.
- Made young-skater development risk track-record-aware by relieving the age-bucket discount when games played, role, or production supports it.
- Gated positive youth age value behind projection signals from production, role, pedigree, and sample size.
- Dampened small-sample OPS/DPS pace extrapolation so hot starts do not fully annualize through the point-share channel.
- Updated goalie NAV so young, controlled, high-rate 1B profiles can exceed the old tandem cap while veteran tandems remain capped.
- Rate-gated the starter market floor to reduce bad-volume starter inflation.
- Softened post-30 goalie aging and added a goalie `volatility` score.
- Surfaced high goalie volatility in `/api/evaluate` GM logic as an `ASSET_SHAPE_MISMATCH` warning.
- Added focused coverage for:
  - unsupported prospect discounting
  - 14-60 prospect/skater blending
  - track-record-aware development discount relief
  - signal-gated youth upside
  - small-sample point-share damping
  - ascending 1B goalie caps
  - veteran tandem caps
  - route-level goalie volatility warnings

### Verification

- `npm run test -- __tests__/xnav.test.ts`
- Result: `63` tests passing.
- `npm run test -- __tests__/evaluate-route.test.ts`
- Result: `4` tests passing.
- `npm run test`
- Result: `211` tests passing.
- `npx tsc --noEmit`
- Result: no TypeScript errors.
- Dev server was not started in Codespace, per project instructions.

## 2026-06-18 Product Split Phase 5

### Completed Today

- Added social-preview polish for shared Trade Machine links.
- Added `summarizeTradeSharePayload(...)` so share metadata and image cards use one stable summary of:
  - matchup
  - outgoing and incoming asset counts
  - locked verdict status
  - created date
  - home-team NAV swing when a locked verdict exists
- Added server-side metadata generation for `/t/[code]`.
- Shared trade links now emit richer title, description, Open Graph, and Twitter card metadata from the encoded payload.
- Added `/t/[code]/opengraph-image` as a generated share card using `next/og`.
- The share card displays:
  - The Hockey Ledger branding
  - the team matchup
  - package counts for each side
  - the locked verdict stamp
  - the creation date
- Kept the shared trade page itself on the existing read-only reconstruction flow.
- Added tests/canaries for the preview summary helper, metadata route, and generated Open Graph image route.
- Added an industry-style cap and production context pass to the focused Trade Machine.
- Reused the existing server NAV pipeline through `fetchNavMap(...)` so selected packages show X-NAV/G-NAV value before the user runs the full GM Audit.
- Added team-side summary panels showing:
  - current cap space
  - projected post-trade cap
  - cap delta
  - production delta
  - NOIV delta
  - package NAV delta
- Added a trade balance strip showing total cap in play, production in play, NAV balance, and that the GM Audit remains required.
- Kept the GM Audit as the authoritative logic layer while surfacing cap, production, NOIV, and NAV context earlier in the workflow.
- Tightened direct GM Audit verdicts for extreme NAV surplus.
- Added a lopsided-surplus `VALUE_VETO` when one side is conceding more compressed NAV than a real GM would normally tolerate.
- This prevents trades like a 90 NAV package for a 189 NAV return from being labeled as a clean `WIN`; the partner GM now rejects that structure unless the value gap stays inside a realistic concession band.
- Added real `/api/evaluate` integration coverage in `__tests__/evaluate-route.test.ts`.
- The route test now POSTs behavioral payloads directly to the handler and verifies:
  - cap-ceiling breach returns `BLOCKED`
  - untouchable partner asset returns a hard partner veto
  - balanced low-risk swap returns `FAIR`

### Phase Notes

- This completes the first Phase 5 polish slice: useful social previews for shared trades.
- This also completes the requested cap/production/statistical breakdown pass for the focused Trade Machine, using the Box Score Junkie-style trade-machine pattern as a reference while keeping The Hockey Ledger's NAV, NOIV, and GM logic model.
- The direct GM Audit now treats extreme NAV surplus as a realism problem, not just a user-side win.
- Public reactions or "who won?" voting remain intentionally unimplemented because they should not block the core share flow.
- The new preview path still works with encoded payload URLs; a future persisted compact-code backend can reuse the same summary helper.

### Verification

- `npm run test`
- Result: `204` tests passing.
- `npx tsc --noEmit`
- Result: no TypeScript errors.
- Dev server was not started in Codespace, per project instructions.

## 2026-06-18 Product Split Phase 4

### Completed Today

- Moved the broader roster-control experience to `/armchair-gm` as the canonical Armchair GM route.
- Changed `/trade` into a compatibility redirect to `/trade-machine`, matching the answered product direction that `/trade` should become the quick Trade Machine path long-term.
- Made `/armchair-gm` own the full workspace implementation instead of re-exporting `/trade`.
- Made `/armchair-gm/loading` own the full startup loader, while `/trade/loading` remains a compatibility shim.
- Updated the shared header active state so the deeper workspace highlights Armchair GM instead of Trade Machine.
- Updated admin navigation, contract admin links, cache revalidation, and cache-clear copy to point users back to Armchair GM.
- Updated Trade Proposal loading copy from "Load into Trade Machine" to "Load into Armchair GM" for the deeper proposal workflow.
- Reworded stale source comments and loading copy that referred to the deeper workspace as the trade machine.
- Updated source canaries so route-level behavior is protected under the new split:
  - Armchair GM canaries now read `app/armchair-gm/page.tsx`.
  - `/trade` is now covered as a redirect to `/trade-machine`.
  - The Armchair GM loader text is covered under the canonical route.

### Phase Notes

- Phase 4 completes the route ownership inversion started in Phase 1:
  - `/trade-machine` owns the focused one-off builder.
  - `/t/[code]` owns shared read-only trade reconstruction.
  - `/armchair-gm` owns the deeper franchise-control workspace.
  - `/trade` now preserves old links by sending users to the quick Trade Machine path.
- Trade-specific controls inside Armchair GM still use plain trade language where that is the correct local action.
- The broader product umbrella remains The Hockey Ledger, with Trade Machine and Armchair GM as distinct modes.

### Verification

- `npm run test`
- Result: `197` tests passing.
- Dev server was not started in Codespace, per project instructions.

## 2026-06-17 Startup Valuation Gate Fix

### Completed Today

- Fixed the initial player valuation readiness gate so it compares against unique asset IDs instead of raw asset row count.
- This prevents duplicate asset IDs in the league payload from producing false `Player valuation load incomplete` errors when the NAV map is actually complete.
- Improved the incomplete-load error message to report unique-value counts and include a short missing-ID sample when values are genuinely absent.
- Added a canary to keep the unique-ID readiness behavior in place.

### Verification

- `npm run test`
- Result: `196` tests passing.
- Dev server was not started in Codespace, per project instructions.

## 2026-06-17 Product Split Phase 1

### Completed Today

- Added the initial product shell for the Trade Machine / Armchair GM split.
- Added `/armchair-gm` as the branded route for the current deeper roster-management experience.
- Reused the existing trade workspace for `/armchair-gm` so the large UI is not forked during the split.
- Expanded the shared header navigation to include:
  - Player Analytics
  - Trade Machine
  - Armchair GM
- Updated the front page to position:
  - Trade Machine as the quick, one-off, share-first trade surface.
  - Armchair GM as the deeper franchise-control mode.
- Updated global metadata so The Hockey Ledger is no longer described only as an NHL Trade Machine.
- Captured the answered CHANGES.md open questions as direction for later phases.

### Phase Notes

- `/trade` still serves the current full trade workspace during Phase 1.
- `/trade` should become the quick Trade Machine route in a later phase.
- `/armchair-gm` now gives the deeper experience its long-term branded route before the current `/trade` behavior is changed.

### Verification

- `npm run test`
- Result: `192` tests passing.
- Dev server was not started in Codespace, per project instructions.

## 2026-06-17 Product Split Phase 2

### Completed Today

- Added `app/lib/trade-share.ts` as the versioned share-state contract for trade payloads.
- Defined `trade-share.v1` with:
  - team IDs
  - outgoing and incoming asset references
  - retained salary selections
  - optional locked verdict snapshot
  - optional value timeline points for future three-year tracking
- Added base64url encode/decode helpers for compact share-code style payloads.
- Added query-string serialize/parse helpers for the current `/trade` state.
- Updated the current trade workspace URL sync and cold-load reconstruction to use the new share helpers.
- Added asset reconstruction support from share references so saved selections can rehydrate from the live asset list.
- Added tests covering payload creation, locked verdict preservation, base64url round trips, query-state parsing, and asset reconstruction.
- Corrected the homepage feature grid to max out at three columns instead of four.

### Phase Notes

- Phase 2 creates the share schema and local encode/decode foundation; it does not yet add persisted public share records or a read-only replay route.
- The schema assumes locked verdicts at creation time, matching the product direction in `CHANGES.md`.
- The optional value timeline field is ready for later value-over-time display without forcing that UI into this phase.

### Verification

- `npm run test`
- Result: `196` tests passing.
- Dev server was not started in Codespace, per project instructions.

## 2026-06-17 Product Split Phase 3

### Completed Today

- Added `/trade-machine` as the focused one-off Trade Machine route.
- Added a lean Trade Machine UI for:
  - choosing the two teams
  - adding outgoing and incoming assets
  - selecting retained salary
  - running the GM Audit
  - generating a locked share link
- Added `/t/[code]` as the read-only shared trade reconstruction route.
- Shared trades decode the Phase 2 payload, rehydrate assets from the live asset list, and display the locked verdict snapshot.
- Updated public navigation and the homepage Trade Machine card to point to `/trade-machine`.
- Kept `/trade` untouched as a compatibility path while `/armchair-gm` continues to expose the deeper workspace.
- Added a canary for the focused route, shared route, and navigation link.

### Phase Notes

- This is the first usable focused Trade Machine version.
- Share links currently use encoded payloads in the URL path. A persisted compact-code backend can replace that later without changing the user-facing `/t/:shareCode` route.
- Social preview metadata is not yet personalized per shared trade because `/t/[code]` is currently a client-side reconstruction route.

### Verification

- `npm run lint`
- Result: no warnings or errors.
- `npx tsc --noEmit`
- Result: no TypeScript errors.
- `npm run test`
- Result: `197` tests passing.
- Dev server was not started in Codespace, per project instructions.

## 2026-06-14 Wrap-Up

### Completed Tonight

- Fixed the Patrick Thomas false-positive stats issue by removing surname-only skater stats fallbacks.
- Kept safer name matching for legitimate diacritic normalization cases.
- Prevented no-signal ELC/minor-league players from receiving artificial positive NAV from age/cap alone.
- Added a prospect NAV path:
  - Draft pedigree gives modest value before an NHL sample exists.
  - Stored/imported `prospectPtsPace` can raise that value.
  - No player-specific Kevin He or Stian Solberg overrides.
- Added draft-history enrichment so synced prospects can pick up `draftYear` and `draftOverall` from recent draft tables.
- Updated NAV client cache keys so prospect input changes do not reuse stale values.
- Kept the development profile layer diagnostic-only and separate from X-NAV.
- Reworded DEV rationale copy so 0-game prospects read like scouting/data notes instead of raw model arithmetic.
- Rebuilt the footer into one combined methodology/glossary surface.
- Removed the duplicate trade-page methodology block.
- Made the icon key always visible.
- Changed methodology/glossary content into wide footer dropdowns:
  - Player Valuation
  - STRAND Glossary
  - Trade Logic
  - Data & Sources
- Fixed franchise selection so the initial team picker works in one click.
- Added a stronger NAV loading status while player values are calculating.
- Stopped mobile keyboards from opening automatically when tapping add-asset buttons.

### Verification

- `npm run test`
- Result: `173` tests passing.
- Dev server was not started in Codespace, per project instructions.

## 2026-06-14 Audit Follow-Up

### Completed Today

- Wired the DEV tab to multi-season NHL history instead of current-season stats only.
  - Added `fetchCachedNhlSkaterTimelineRowsForPlayers(...)` in `app/lib/development-sources.ts`.
  - The helper fetches each recent NHL skater-summary season once, then builds a `playerId -> timeline matches` map.
  - `app/api/league/route.ts` and `app/api/league/players/route.ts` now prefer `buildDevelopmentInputFromNhlTimeline(...)` when timeline rows exist.
  - Current payload fallback remains in place when NHL history is unavailable.
  - Route timeline depth is currently `seasonCount: 5`.
- Updated the development model so career NHL experience is derived from summed NHL timeline snapshots when present.
  - This prevents established players from being treated as low-sample rookies just because the current season has fewer than 82 GP.
  - Added a Vincent Trocheck-style regression test for this behavior.
- Tightened trade approval/proposal screening.
  - Proposal pre-screen now rejects partner NAV concessions beyond a tighter band.
  - Shopped/available players are allowed a larger concession band.
  - Rebuilding/tanking teams now protect premium lottery firsts unless the return is exceptional.
  - Evaluation logic now flags large NAV gaps earlier.
- Improved shopped-player handling in GM logic.
  - Players marked `available` or `requested` bypass partner-side “can’t afford to lose” / stated-need vetoes.
- Fixed retained salary session persistence after trade execution.
  - Moved assets now carry their `retainedPct` into the post-trade roster state.
- Added dynamic post-trade team context.
  - Completing a trade recalculates team standing/phase from updated roster strength.
  - Draft picks inherit updated `teamStanding`, so pick NAV can change after major roster moves.
  - Selected home/partner team objects sync back to updated `db.teams`.
- Expanded generated draft pick inventory.
  - League routes now generate rounds 1-5 for the next three drafts: 2027, 2028, and 2029.
  - Proposal builders no longer filter out 2029 picks.
- Made generated trade proposals pass the full GM audit before display.
  - `TradeProposalEngine` still applies the fast local proposal pre-screen first.
  - Pre-screened candidates now run through `fetchTradeVerdict(...)` with the candidate partner roster.
  - Proposals with `BLOCKED` or `DECLINED` verdicts are filtered out before they can be loaded.
  - Full-audit checks run concurrently and show package-audit progress in the modal.
  - Verdict requests no longer ask the server to return NAV for full rosters on every proposal check.
- Added a startup readiness gate before the trade UI unlocks.
  - The loading screen now confirms teams, player assets, and player values.
  - Team selection/trading stays blocked until the first full `fetchNavMap(...)` pass returns values for every loaded asset.
  - Incomplete or failed initial valuation loads now surface a data-pipeline error instead of opening with partial player data.
  - The route-level `/trade` loader now matches the same readiness screen and no longer flashes skeleton bars first.
- Reworked salary retention controls for mobile.
  - Replaced the drag range slider with tap-based stepper and preset buttons.
  - Retention still moves in 5% increments from 0% to 50%.
  - A passive progress bar shows the selected retention without being draggable during scroll.
- Removed traded players from the session trade block after trade execution.
  - Moved non-pick assets keep their new team and retained-salary state.
  - Their `tradeBlockStatus` and `tradeBlockNote` are cleared session-locally so they no longer appear as active block/request entries after moving.
- Added explicit player-ID fallback for NHL skater summary stats.
  - Both league routes now store fallback stats under `id:<playerId>`.
  - Roster assembly checks player ID before position/name slug fallback when MoneyPuck stats are missing.
  - This completes the Lafreniere/accent/missing-stat inflation audit item.
- Added contract term to the select-asset screen.
  - Asset rows now show years remaining before the user adds a player to the trade.
  - Pick rows continue to show the draft year in the same metadata slot.
- Added tests/canaries for:
  - Bulk DEV timeline fetches.
  - Timeline-backed DEV route exposure.
  - Career NHL experience from snapshots.
  - Tightened proposal NAV screening.
  - Shopped-player concession exception.
  - Premium lottery pick protection.
  - Three-year, rounds 1-5 draft pick inventory.
  - Full-audit verification for generated trade proposals.
  - Proposal audit progress/concurrency and lean verdict payloads.
  - Startup gate for complete initial player valuation load.
  - Consistent `/trade` preloader with no skeleton flash.
  - Tap-based salary retention controls replacing the mobile-prone slider.
  - Session trade-block cleanup after executing a trade.
  - Player-ID fallback for NHL skater summary stats.
  - Years-remaining display in the select-asset modal.

### Verification

- `npm run test`
- Result: `186` tests passing.
- Dev server was not started in Codespace, per project instructions.

### Notes For Next Agent

- The DEV tab should now be substantially more accurate for established NHLers, but it depends on NHL stats API timeline availability and cache freshness.
- If DEV still shows limited history for a specific player, inspect whether that player has NHL `playerId` timeline matches in the recent skater-summary seasons.
- Goalies still return no DEV profile through `buildDevelopmentInputFromPlayerPayload`; the development model remains skater-focused.
- The dynamic post-trade team phase calculation is intentionally lightweight and session-local. It is not yet the full contention-quadrant model.
- `AUDIT.md` has been marked with `Done`, `Partial`, and `Open` statuses as of this pass.

### Current Audit Position

Completed from `AUDIT.md`:

- DEV tab now uses multi-season NHL timeline/career experience.
- NAV approval thresholds and contextual proposal screening are tighter.
- Retained salary persists through executed trades and affects session cap state.
- Post-trade team status and draft pick standings update session-locally.
- 2027-2029 rounds 1-5 pick inventory is generated.
- Tanking/rebuilding teams protect premium lottery firsts.
- Generated trade proposals are full-audit verified before display.
- Salary retention mobile UX no longer uses a drag slider.
- Shopped/requested players bypass the relevant partner vetoes.
- Traded players are removed from the active trade block for the current session.
- Startup loading now gates the trade UI until initial player values are complete.
- Lafreniere/accent handling is backed by explicit player-ID skater stat fallback.
- Select-asset rows show contract years remaining before adding.

Partial:

- Dynamic draft pick values update from session-local standings, but deeper projection inputs remain open.

Remaining queue:

- Contention quadrant depth weighting.
- Change-of-scenery upside logic.
- Top prospect trade reluctance.
- Lineup/simulation validation.
- Defensive defenseman valuation, including Jaccob Slavin-style profiles.
- Ledger copy UX.
- Mobile line change UX.

## Next Project

### Prospect Production Import

Build a proper prospect production import flow instead of hardcoding or guessing junior/college/European production.

Recommended first version:

- Bulk CSV or pasted table import.
- Preview changes before saving.
- Inputs:
  - `name`
  - `team`
  - `league`
  - `games`
  - `goals`
  - `assists`
  - `points`
- Convert production to `prospectPtsPace` using NHLe factors.
- Match cautiously by normalized name and team.
- Save only confident matches.
- Flag ambiguous matches for manual review.

Reasoning:

- Draft slot can be enriched automatically.
- Production value should come from real imported/stored stats.
- This lets players like Kevin He earn extra NAV from OHL production while random later-round prospects stay modest unless they also have production.

## Manual QA For Tomorrow

- Check footer layout on desktop and mobile.
- Confirm the icon key is always visible and not hidden behind a dropdown.
- Confirm methodology dropdowns are full-width and not skinny columns.
- Test first-load franchise selection in the browser.
- Test mobile add-asset drawer and confirm the keyboard opens only when tapping search.
- Review example NAVs:
  - Kevin He
  - Stian Solberg
  - a random no-signal ELC player
  - an older AHL-only player
- Revisit later-round prospect draft-pedigree curve after real production data is imported.
